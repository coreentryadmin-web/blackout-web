/** Marketing routes skip the full-screen Phosphor boot — it blocked comprehension on
 *  homepage/pricing navigations (public audit 2026-08-27). Desk routes keep root loading.tsx. */
export default function MarketingLoading() {
  return null;
}
