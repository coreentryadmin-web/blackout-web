# Launch seven tool-specific Cloud Agents — DECOMMISSIONED (scripts/tool-agents removed).
# Manual: workflow_dispatch only — exits 0 with skip message.

name: Tool agents launch

on:
  workflow_dispatch:

jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Decommissioned
        run: |
          echo "scripts/tool-agents/launch-cloud-agents.mjs was removed."
          echo "Use .github/workflows/blackout-autopilot-dispatch.yml for agent dispatch."
