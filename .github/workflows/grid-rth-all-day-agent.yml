# 0DTE Command + Market Grid — all-day RTH Cloud Agent (verify + post-close fix)
#
# SCHEDULE DISABLED 2026-08-06 — hourly verify agents were opening docs-only PRs.
# Manual only via workflow_dispatch. Runbook: docs/ops/GRID-RTH-ALL-DAY-AGENT.md

name: Grid RTH all-day agent

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Agent mode"
        required: true
        default: verify
        type: choice
        options:
          - verify
          - fix

jobs:
  launch:
    runs-on: ubuntu-latest
    steps:
      - name: Determine agent mode
        id: mode
        run: |
          set -euo pipefail
          INPUT="${{ github.event.inputs.mode }}"
          if [ -n "$INPUT" ]; then
            echo "mode=$INPUT" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          SCHEDULE='${{ github.event.schedule }}'
          case "$SCHEDULE" in
            "5 20 * * 1-5"|"5 21 * * 1-5") echo "mode=fix" >> "$GITHUB_OUTPUT" ;;
            *) echo "mode=verify" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Start Cloud Agent (Grid all-day)
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          AGENT_MODE: ${{ steps.mode.outputs.mode }}
        run: |
          set -euo pipefail
          if [ -z "${CURSOR_API_KEY:-}" ]; then
            echo "CURSOR_API_KEY not set — skipping agent launch."
            echo "Create Cursor Automations from docs/ops/GRID-RTH-ALL-DAY-AGENT.md (starts 6:30 AM PT)"
            exit 0
          fi

          if [ "$AGENT_MODE" = "fix" ]; then
            PROMPT='0DTE Command + Market Grid post-close fix agent (~1:05 PM PT). Read docs/ops/GRID-RTH-ALL-DAY-AGENT.md Section Step 4 completely. Run npm run validate:grid-rth -- --phase=post-close AND npm run validate:zerodte-logic AND npm run validate:grid-e2e. Fix EVERY Grid/0DTE finding from today: gate logic, play picking, trade management (OPEN/HOLD/TRIM/CLOSED), mergePlays UI, grid panel cron bypass, ledger PnL. Branch fix/<slug>, test, PR with CODE fixes only, merge, validate:deploy. Repeat until all three validators are zero FAIL. Do NOT open PRs that only touch docs/. Do NOT ask the user.'
            AUTO_PR=true
          else
            PROMPT='0DTE Command + Market Grid RTH verify (manual dispatch). Read docs/ops/GRID-RTH-ALL-DAY-AGENT.md verify mode. Run npm run validate:grid-rth, validate:zerodte-logic, validate:grid-e2e. Report PASS/FAIL in the agent summary ONLY. Do NOT commit, push, or open PRs for verify passes — especially no docs/ edits. Fix P0 code bugs only (fix/<slug> PR with src/ changes). Do NOT ask the user.'
            AUTO_PR=false
          fi
          export PROMPT
          export AUTO_PR

          payload=$(node -e "
            console.log(JSON.stringify({
              prompt: { text: process.env.PROMPT },
              repos: [{ url: 'https://github.com/coreentryadmin-web/blackout-web', startingRef: 'main' }],
              autoCreatePR: process.env.AUTO_PR === 'true'
            }));
          ")

          resp=$(curl -sf -X POST https://api.cursor.com/v1/agents \
            -u "${CURSOR_API_KEY}:" \
            -H 'Content-Type: application/json' \
            -d "$payload")

          echo "$resp" | node -e "
            const r = JSON.parse(require('fs').readFileSync(0,'utf8'));
            const url = r.agent?.url ?? r.url ?? '(see response)';
            console.log('Grid Cloud Agent started (' + process.env.AGENT_MODE + '):', url);
          "
