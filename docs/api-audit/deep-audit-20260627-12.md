# BlackOut Deep Platform Audit
**Run time:** 2026-06-27 12:13 ET (Saturday — market CLOSED)
**Overall Risk:** HIGH (1 net-new P1)
**Issues found:** 0 P0 | 1 P1 (net-new) | 2 P2 (carried) | 2 P3 (carried)

> Audit run manually/correctly because the SKILL's PowerShell probes use stale paths (P3-1).
> All findings below verified against source + live prod this run.

## Weekend context
Today is **Saturday 2026-06-27**. The naive RTH check (UTC hour 13–21) reports "RTH=True"
but markets are CLOSED. Empty/gated live-data responses (flows, spx pulse, anomalies, regime)
are EXPECTED today and are **not** flagged as stale-data P0s. SPX-Engine-Evaluation cron is
correctly weekday-only (`*/5 11-20 * * 1-5`, next run Monday).

## P0 — Fix Immediately
None.

## P1 — Fix Today
- **P1-NEW — Regime + Flow-Anomaly features are dead end-to-end, but have LIVE consumers.**
  `market_regime` and `flow_anomalies` have **no writer anywhere** — the only INSERTs are the
  cron-gated POST handlers of their own routes (`src/app/api/market/regime/route.ts:53`,
  `src/app/api/market/anomalies/route.ts:46`), and **nothing calls those POSTs**. The
  "market-regime-detector cron" named in `regime/route.ts:2` does not exist (no route in
  `src/app/api/cron/`, no Railway cron job). Confirmed: a full `src` grep finds zero non-migration
  writers; tables created only in `migrations/004_god_tier_features.sql`.
  **Why it's P1 not tech-debt:** the data has real consumers that silently degrade —
  - `FlowAnomalyBanner` is **mounted on the paid `/flows` page** (`src/app/(site)/flows/page.tsx:41`,
    fetches `/api/market/anomalies` at `FlowAnomalyBanner.tsx:59`) → banner can NEVER render.
  - `nighthawk-morning-confirm` cron reads regime + anomalies via `/api/platform/intel`
    (`src/app/api/cron/nighthawk-morning-confirm/route.ts:110`); `intel` defaults
    `currentRegime` to `"UNKNOWN"` and 0 anomalies forever (`platform/intel/route.ts:72,89`)
    → NH morning confirmation runs with permanently-blank market-regime context.
  Violates the standing "values must be live/correct/grounded, never blank" rule.
  **Fix:** build the regime-detector + anomaly-detector cron writers (or POST callers), OR
  remove the dead consumers (FlowAnomalyBanner mount, intel regime/anomaly fields) if the
  features are abandoned. Do not leave a paid page wired to a permanently-empty source.

## P2 — Fix This Week (carried, re-verified)
- **P2-A** `/api/market/anomalies` (→200 `{"anomalies":[]}`) and `/api/market/regime`
  (→200 `{"available":false}`) serve **unauthenticated** while sibling market routes 401.
  `regime` GET is explicitly annotated public (market-wide data, `regime/route.ts:11-12`) — OK.
  `anomalies` GET has **no guard and no public annotation** — it would leak premium-derived flow
  anomaly data (ticker/premium/direction/severity) once populated. No leak today (empty), but the
  boundary is wrong. Add `requireTierApi` or annotate intentional-public. `anomalies/route.ts:9`.
  *(Note: this overlaps P1-NEW — same two endpoints, different failure mode.)*
- **P2-C ⏳ WATCH** SPX play ledger empty all-time (`spx_open_play`=0, `spx_play_outcomes`=0).
  Most likely EXPECTED: option-chain veto confirmed disabled (`spx-play-config.ts:404`,
  `SPX_OPTION_CHAIN_REQUIRED` defaults false), open logic intact (`spx-play-engine.ts:900
  openPlay`), cron wired. Fix merged ~20 min after Friday close → no session has run with it.
  **VERIFY Mon 2026-06-29 after RTH:** re-query prod `spx_open_play`. If still 0 → escalate to P1.

## P3 — Tech Debt / Tooling (carried)
- **P3-1** deep-platform-audit `SKILL.md` probe paths are stale (`/api/market/spx-pulse`,
  `/api/flows`, `/api/nighthawk/latest-edition`, `/api/grid/news` all 404 — real paths are
  `/api/market/spx/pulse`, `/api/market/flows`, `/api/market/nighthawk/edition`, news at
  `/api/market/news`), env-var names wrong (`UNUSUAL_WHALES_API_KEY`→`UW_API_KEY`), and the
  db-handler regex `pool\.on` misses the real `livePool.on("error")` (`db.ts:113`). The script
  also can't verify freshness (all data routes are 401-gated). Fix the SKILL's probe lists.
- **P3-2** Dead tables: `spx_pulse_snapshots`, `spx_watch_setups` (+ now `market_regime`,
  `flow_anomalies` per P1-NEW) — exist with 0 rows and no writer. Drop or wire writers.

## ✅ Re-confirmed healthy this run
- TSC `--noEmit`: **0 errors** (exit 0)
- `db.ts:113` pg Pool idle-error handler present (#100, no regression); pool max=5
- `make-redis.ts:58` Redis `family:0` + `reconnectOnError` present (IPv6 fix intact)
- SPX option-chain veto neutered (`spx-play-config.ts:404`)
- #97 `SpxDarkPoolCard` mounted (`SpxDashboard.tsx:13,105`)
- #101 Clerk `user.created` handled (`webhooks/clerk/route.ts:50`); singular `/webhook/clerk`
  is an alias re-export of the canonical handler — both paths safe
- #102 Polygon WS Redis leader-election present (`ws/polygon-socket.ts:117-128`)
- TODO/FIXME: 3 matches, all false positives (var names, prompt text) — no real stubs
- Tool-route auth: all market/grid routes self-authorize except the two in P2-A (anomalies
  unguarded; regime intentionally public)
- Infra: blackout-web 5/5 running, Postgres + Redis Online, crons scheduled (Flow-Ingest 5-min)

## Phase Results
- **Live Site:** healthy (landing/auth 200; data routes correctly 401-gated; weekend)
- **Code Audit:** 1 net-new P1, core safety checks all pass
- **Cross-Tool Consistency:** not verifiable live (auth-gated + weekend); regime/anomaly
  consumers traced in source
- **Infrastructure:** all services Online, no error spike
- **Open Task Backlog:** #97/#100/#101/#102/#73/veto all confirmed FIXED
