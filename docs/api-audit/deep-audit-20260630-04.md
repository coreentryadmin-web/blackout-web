# BlackOut Deep Platform Audit
**Run time:** 2026-06-30 04:1x ET (≈11:1x UTC) — Tuesday, pre-market, RTH closed
**Method:** live data via apex (`blackouttrades.com`) + Bearer CRON_SECRET, prod Postgres (public proxy), code read, TSC
**Overall Risk:** HIGH (one carried P1 data-integrity bug; no new breakage)
**Issues found:** 0 P0 · 1 P1 · 2 P2 · 1 P3-meta

---

## P0 — Fix Immediately (User-Facing Breakage)
None. All surfaces healthy.

## P1 — Fix Today (Feature Broken or Degraded)
- **[OPEN · 🟠 DATA INTEGRITY · carried ≥4 runs · re-confirmed in code this run]** **Play outcomes mislabel profitable STOP/THESIS exits as losses.**
  `src/lib/spx-play-outcomes.ts:177-178` — the `close.was_loss || exit_action === "STOP" || exit_action === "THESIS"` branch returns `"loss"` **without checking `pnl_pts` sign**, while the THETA/SESSION branch (`:170-176`) and the TRAIL branch (`:183-186`) both check it. A profitable THESIS exit (thesis-target hit on a winner) is persisted `outcome='loss'`.
  **Live evidence (prod `spx_play_outcomes`):** 3 closed plays this session, all graded A/A+, but play **id 3 is a long with entry 7432.13 → exit 7439.43 (+7.30 pts, MFE 7.3)** — a genuine winner that the STOP/THESIS branch would store as a loss. Root cause of the public **0-for-3 win-rate** on the track-record/P&L surface.
  **Fix:** for `STOP`/`THESIS`, `pnl_pts > 0 → "win"` (mirror the TRAIL branch). One-liner.
  **Not auto-fixed** — data-integrity + behavior change wants a reviewed human commit, not a cron commit.

## P2 — Fix This Week (Wrong But Not Visible)
- **[OPEN · learning loop dormant]** **`signal_events` / `signal_outcomes` still 0 rows in prod.** Both tables have their full schema (`signal_events`: id, fired_at, signal_source, signal_type, grade, spx_price, call_wall, put_wall, confluence_score, ticker, strike, expiry, option_type, entry_mark, metadata — and `signal_outcomes`: signal_event_id, recorded_at, checkpoint, price_at_checkpoint, price_change, direction_correct, pnl_pct, outcome) but **no recorder is writing to them**, so the signal-accuracy learning loop produces nothing. Schema decision is resolved (columns exist); the remaining work is wiring the recorder at signal fire + checkpoint. Deliberately deferred per project notes — tracking so it doesn't fall off.
- **[WATCH · downstream of the P1]** SPX track record shows **0/3 win-rate**; ≥1 play (id 3, +7.3 pts) reclassifies to a win once the P1 lands. Also watch entry-timing: cold_buy MAE 7.15 pts vs watch_promote MAE ~0.9–2.5 pts — `cold_buy` entries are taking materially more heat. Re-check over the next several RTH sessions.

## P3 — Code Quality / Meta
- **[FIX THE AUDIT SKILL — recurring every run]** `SKILL.md` Phase 1/2/3 probe dead paths and stale symbols, manufacturing phantom 404/P0s:
  - Phase 1 hits **`www`** (Cloudflare strips `Authorization` → 401/404). Use **apex `blackouttrades.com` + `Bearer CRON_SECRET`**.
  - `spx-pulse` → real is `market/spx/pulse` (or read `gex .spot`); `/api/flows` → `market/flows`; `nighthawk/latest-edition` → `market/nighthawk/edition`; `grid/news` is **nonexistent** (real grid panels: analysts, catalysts, congress, dark-pool, earnings, economy, movers, sectors → news lives at `market/news`).
  - GEX-vs-desk wall-diff compares **SPY** GEX (call_wall 743 / put_wall 735, snake_case) to **SPX** desk (~7440, ÷~10.04) and uses camelCase `callWall`/`putWall` that don't exist on the payload.
  - `pool.on` grep misses `livePool.on` (db.ts:113); clerk grep reads the **singular alias** `webhook/clerk` (a 2-line re-export) not the canonical `webhooks/clerk`; `import.*SpxDarkPoolCard` misses the multiline import.

---

## ✅ Confirmed Healthy This Run
- **All real endpoints 200 via apex+Bearer:** gex-positioning (SPY 742.9, asof 11:10 UTC ~fresh, cross-validation callWall/putWall/flip all match, divergence 2) · market/flows (live items) · nighthawk/edition (3 plays) · all **8 grid panels** `available:true` · congress/economy.
- **Marketing/auth surfaces 200:** landing 119KB, sign-in, sign-up (sign-up 5.3s — cold start, watch but not flagged).
- **TSC: 0 errors.** Build clean.
- **Infra:** all 7 required prod env vars SET (Postgres, Redis, Clerk, Anthropic, Polygon, CRON_SECRET, Clerk webhook secret); no error-level lines in recent `blackout-web` logs; GEX `source:"polygon"`.
- **Resolved/verified-fixed:** db pool error handler (`livePool.on("error")` db.ts:113, max:5) · Redis `family:0` + `reconnectOnError` (make-redis.ts:58,63) · Polygon WS Redis leader-election (`polygon:indices:leader` 25s TTL, polygon-socket.ts:117-441) · Clerk `user.created`+`user.updated` (webhooks/clerk:50,66) · SpxDarkPoolCard mounted (SpxDashboard.tsx:13,104). Tasks **#97/#100/#101/#102 all RESOLVED.**
- **SPX plays opening+closing (was the long-standing "plays never open" dead pipeline):** `spx_open_play` = 3 rows, `spx_play_outcomes` = 3 rows (session 2026-06-29, grades A+/A/A+). **Pipeline is live** — the only remaining issue is the P1 mislabel above, not the open path.

## Phase Results
- **Live Site:** 0 real issues (5 script false-positives = stale paths/www)
- **Code Audit:** 0 new issues (P1 carried from code; #97/#100/#101/#102 confirmed fixed; TSC clean)
- **Cross-Tool Consistency:** 0 (GEX snake_case fields fresh & cross-validated)
- **Infrastructure:** 0 (all env set, logs clean)
- **Data Pipelines:** 1 P1 (outcome mislabel) + 1 P2 (signal_events dormant); SPX plays pipeline now LIVE
