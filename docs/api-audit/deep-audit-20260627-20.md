# BlackOut Deep Platform Audit
**Run time:** 2026-06-27 20:15 ET (Saturday — market CLOSED)
**Overall Risk:** HIGH (1 P1 carried, refined) — no P0, no regressions
**Issues found:** 0 P0 | 1 P1 (refined) | 3 P2 (carried) | 3 P3 (2 carried + 1 new)

Every finding below is backed by live prod ground truth (Postgres queried via public proxy) or a cited `file:line`. Weekend run, so RTH-only freshness/play-open checks are deferred to Monday 2026-06-29.

---

## 🔴 P0 — none

## 🟠 P1 — 1 open (refined this run — effort dropped from "build" to "deploy")

### P1-A — Regime + flow-anomaly features still dead in prod, but the writer is now BUILT and just unwired
**This is the single most actionable finding of the run.** Since the 12:13 run flagged "no writer exists," the writer was fully built in code:
- Route: `src/app/api/cron/market-regime-detector/route.ts` (writes `market_regime` + scans `flow_anomalies`)
- Registry: `src/lib/cron-registry.ts:217` (`market-regime-detector`, `*/5` market hours)
- Railway config: `railway.market-regime-detector.toml` (`cronSchedule = "*/5 11-21 * * 1-5"`)

**But prod ground truth says it has never run:**
- `market_regime`: **0 rows** (max captured_at = null)
- `flow_anomalies`: **0 rows** (max detected_at = null)
- `cron_job_runs` where `job_key='market-regime-detector'`: **0 runs in 7 days**
- `railway status`: **no `Market-Regime-Detector` service exists** in the project (23 services listed; this is not one)

**Root cause:** the `.toml` exists in the repo but its own header says *"Wire it up: create a cron service → Settings → Config-as-code."* That manual Railway-service-creation step was never done, so nothing ever hits the route.

**Live consumers still silently degrading** (unchanged from 12:13):
- `FlowAnomalyBanner` on the paid `/flows` page (`src/app/(site)/flows/page.tsx:41`, fetch at `FlowAnomalyBanner.tsx:59`) → can never render.
- `nighthawk-morning-confirm` reads regime+anomalies via `/api/platform/intel` (`cron/nighthawk-morning-confirm/route.ts:110`) → defaults `currentRegime="UNKNOWN"`/0 anomalies forever (`platform/intel/route.ts:72,89`).

**Fix (now a single deploy action, no code):** create the Railway cron service from `railway.market-regime-detector.toml` (Config-as-code), ensure `CRON_SECRET` is set on it (same value as blackout-web), and confirm a first run writes `market_regime`. _(found 12:13; refined 20:15 — writer confirmed built, only the Railway trigger is missing)_

---

## 🟡 P2 — 3 open (all carried, re-verified live)

- [ ] **P2-C ⏳ WATCH** — SPX play ledger empty all-time. Live prod: `spx_open_play`=**0**, `spx_play_outcomes`=**0**. `spx-evaluate` cron is healthy and active (333 `ok` / 63 `skipped` over last 4 days) — it runs, it just never reaches a recorded BUY. Two gate fixes shipped Sat while market closed (`5eee3ff` 6-bug gate audit + `cee2ebf` 0DTE calibration) remain unvalidated. Option-chain veto confirmed neutered (prior runs; `SPX_OPTION_CHAIN_REQUIRED` unset). **VERIFY Mon 2026-06-29 post-RTH:** re-query `spx_open_play` (expect rows). If still 0 → escalate to P1 and read `63567cb` gate diagnostics. Do NOT re-touch the veto. _(found 07:10; re-verified 20:15)_

- [ ] **P2-A → effectively annotated-resolved** — `/api/market/anomalies` and `/api/market/regime` serve 200 unauthenticated while sibling market routes 401. This run confirmed **both now carry explicit "intentionally public — market-wide, no paid data" annotations** in-code (`anomalies/route.ts:1-4`, `regime/route.ts:1-3`), which is the documented-public fix prior runs proposed. No paid-data leak (both return empty today). The substance of the concern now folds entirely into **P1-A** (the tables are empty because nothing writes them). Keeping a pointer here; the auth-boundary itself is addressed by annotation. _(found 07:10; annotation confirmed 20:15)_

- [ ] **P2-B** — `spx_signal_log` is **empty (0 rows, max null)** in prod — no writer anywhere. (Prior runs reported "last wrote 06-17"; the table is now fully empty.) If any admin/analytics surface reads it, it serves nothing. Confirm superseded by the play engine; retire the table + readers or resume writes. _(found 07:10; re-verified empty 20:15)_

---

## 🔵 P3 — 3 open (tech debt / tooling)

- [ ] **P3-1** — This audit's `SKILL.md` still produces false P0/P1 every run from stale probe data. This run it caused false 404s on 4 endpoints and a false "UNUSUAL_WHALES_API_KEY MISSING". Corrections needed in the SKILL probe lists:
  - `/api/market/spx-pulse` → `/api/market/spx/pulse`
  - `/api/flows` → `/api/market/flows`
  - `/api/nighthawk/latest-edition` → `/api/market/nighthawk/edition`
  - `/api/grid/news` → does not exist (real panels: analysts, catalysts, congress, dark-pool, earnings, economy, movers, sectors)
  - env var `UNUSUAL_WHALES_API_KEY` → `UW_API_KEY` (set in prod)
  - db error-handler regex `pool\.on` misses the real `livePool.on("error")` at `db.ts`
  - Note all market/grid/nighthawk endpoints correctly require auth → live unauthenticated probes return 401 by design, not breakage. _(found 00:12; reconfirmed 20:15)_

- [ ] **P3-2** — `spx_pulse_snapshots` and `spx_watch_setups` are dead/legacy tables (0 rows all-time, zero INSERT code refs). Drop or wire writers. _(carried from 07:10)_

- [ ] **P3-3 (NEW)** — Fail-open guard on cron-write POSTs. `market/anomalies/route.ts:38` and `market/regime/route.ts` use `if (cronSecret && auth !== \`Bearer ${cronSecret}\`)` — when `CRON_SECRET` is unset the condition short-circuits and the POST is **accepted unauthenticated**. `CRON_SECRET` is set in prod so there is no live exposure, but this is a defense-in-depth gap: prefer failing closed (`if (!cronSecret || auth !== ...)`). _(found 20:15)_

---

## ✅ GREEN — re-verified healthy this run
- **Site up:** Landing/Sign-in/Sign-up/Health all 200. Health body `{"ok":true,"db":"configured"}`. All market/grid/nighthawk data endpoints return **401 unauthenticated by design** — paid-tool gating works.
- **Build:** `tsc --noEmit` → **0 errors**.
- **DB/Redis safety:** `db.ts` pg Pool error handler present; `make-redis.ts` `family:0` + reconnect present.
- **SPX engine:** veto guard + open logic both present; veto confirmed neutered.
- **Infra:** all 23 Railway services Online/Completed (blackout-web **5/5 replicas**, Postgres + Redis Online). Continuous crons (flow-ingest, heatmap-warm, uw-cache-refresh, nights-watch-warm) alive through Sat 19:40 PT. No real errors in last 501 log lines (the only "500" matches were healthy `postgres ok — 500 rows`).
- **Env:** all required vars set — `UW_API_KEY`, `POLYGON_API_KEY`, `CLERK_SECRET_KEY`, `ANTHROPIC_API_KEY`, `CRON_SECRET`, `CLERK_WEBHOOK_SECRET`, `DATABASE_URL`, `REDIS_URL`, `LAUNCHED_TOOLS`, VAPID. Missing `UW_API_BASE`/`UW_CLIENT_API_ID`/`UW_WS_BASE` all have code defaults — non-issues.
- **Backlog fixed (re-confirmed):** #97 SpxDarkPoolCard (0 dead refs), #100 pg pool handler, #101 Clerk `user.created` (handler at `webhooks/clerk` + alias re-export at `webhook/clerk`), #102 Polygon WS leader/replica guard, #73 Largo grounding.
- **TODO/FIXME:** only 3 in `src/` — clean.

## Phase Results
- **Live Site:** 0 real issues (4 false 404s from stale SKILL paths → P3-1)
- **Code Audit:** 0 real issues (8 "ungated" flags all false positives — 6 use real helpers, 2 documented-public)
- **Cross-Tool Consistency:** deferred (endpoints auth-gated, market closed) — GEX convergence verified in prior runs
- **Infrastructure:** 0 issues (all services Online, env complete, logs clean)
- **Open Backlog:** P1-A (refined), P2-A/B/C, P3-1/2/3
