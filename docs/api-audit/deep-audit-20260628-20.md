# BlackOut Deep Platform Audit
**Run time:** 2026-06-28 20:09 ET (Sunday — market closed, no RTH data sampleable)
**Overall Risk:** HIGH (carried P1-A only; **zero net-new user-facing breakage this run**)
**Issues found this run:** 0 new P0 | 0 new P1 | 0 new P2 — all findings are RE-CONFIRMATIONS of carried opens

---

## Verdict
Platform is **GREEN on everything sampleable**. Public surfaces healthy, code source-clean,
infra fully provisioned, every secret present, all prior backlog tasks (#97/#100/#101/#102)
verified resolved in code, and the SPX play-opens veto is confirmed **opt-in / disarmed in prod**.
No new defects introduced since the 16:12 ET run. The carried opens below are unchanged.

## P0 — Fix Immediately
None.

## P1 — Fix Today (carried, operator-only)
- **P1-A (carried, STILL OPEN):** No `Market-Regime-Detector` cron service provisioned →
  `market_regime` / `flow_anomalies` writers never run. `.toml` + route code exist; needs the
  manual Railway "add service (Config-as-code)" step (deploy-risky infra, operator-only — not
  auto-created by audit). Not re-enumerated this run (market closed); carried from 08:13/12:23/16:12.

## P2 — Fix This Week (carried, code re-confirmed this run)
- **regime POST fails open** — `src/app/api/market/regime/route.ts:48`: guard is
  `if (cronSecret && auth !== \`Bearer ${cronSecret}\`)`. If `CRON_SECRET` is ever unset the guard
  is **skipped entirely** and `market_regime` becomes a public unauthenticated INSERT/injection
  endpoint. Dormant today (CRON_SECRET present in prod — verified) but should **fail CLOSED**
  (reject when secret missing). Re-confirmed in code this run.
- **Grid over-promises** — `src/app/(site)/grid/page.tsx:35` subtitle reads "News, flow, analyst
  actions, and market pulse on one board." but the page fetches only
  `analysts/catalysts/congress/dark-pool/earnings/economy/movers/sectors` — **no News panel, no
  Flow panel** (no `/api/grid/news` route exists; grid has no flow fetch). Wire the panels or fix
  the copy. Re-confirmed this run.
- **P2-C SPX play opens** — carries to **Mon 2026-06-29 RTH**. Code path is healthy
  (veto opt-in, fallback ticket opens plays), but actual open behavior is not observable
  market-closed. Re-sample after 09:30 ET Monday.
- **P2-D options-socket code=1006 loop** — carries to Mon RTH (not re-sampled; market closed).
  Watch climbing `failures` on `options-socket` shard 0 after open → promote to P1 if Night's Watch
  valuations degrade. Fix = gate reconnect/heartbeat on options-RTH
  (`src/lib/ws/options-socket.ts:453-457`).

## P3 — Audit Tooling Debt (META — re-confirmed every run)
- **The deep-audit SKILL.md generates systematic FALSE POSITIVES** and should be corrected. This
  run re-confirmed all of them:
  - Stale probe paths: `spx-pulse`→`market/spx/pulse`, `/api/flows`→`/api/market/flows`,
    `nighthawk/latest-edition`→`market/nighthawk/edition`, `grid/news`→does-not-exist (news lives at
    `/api/market/news`). The script's Phase-1 "404s" are all phantom.
  - Wrong env var name: checks `UNUSUAL_WHALES_API_KEY` (absent) — code actually reads `UW_API_KEY`
    (**present** in prod). The "P0 env var MISSING" the script would emit is false.
  - Stale grep patterns: `pool.on(` misses the real `livePool.on("error")` handler (db.ts:113);
    `SpxDarkPoolCard` is now imported+mounted (SpxDashboard.tsx:104) so the #97 heuristic is stale;
    `webhook/clerk` (singular) is a re-export of `webhooks/clerk` (plural) which DOES handle
    `user.created` (route.ts:50); Polygon WS DOES have a Redis SETNX leader election
    (polygon-socket.ts:117-148). All four backlog heuristics now fire false.
  - Several PowerShell idioms are invalid (`Select-String -Path src -Recurse`, two-arg paths).

## GREEN — Re-verified this run
| Check | Result |
|---|---|
| Live public surfaces | Landing/SignIn/SignUp/Health → **200**; protected data endpoints correctly **401** (auth-gating works) |
| TypeScript | `tsc --noEmit` → **0 errors** (source clean) |
| DB pool error handler | Present — `db.ts:113` `livePool.on("error", …)`, `max:5`, statement/query timeouts (#100 resolved) |
| Redis | `family:0` (IPv6 fix) + `reconnectOnError` + `maxRetriesPerRequest` (make-redis.ts:58,63) |
| SPX play-opens veto | **Opt-in** — `playOptionChainRequired()` defaults false (spx-play-config.ts:418); `SPX_OPTION_CHAIN_REQUIRED` **not set** in prod → plays open with index-plan fallback ticket |
| Backlog #97 | Resolved — `SpxDarkPoolCard` mounted (SpxDashboard.tsx:104) |
| Backlog #101 | Resolved — Clerk `user.created` handled (webhooks/clerk/route.ts:50); singular alias re-exports it |
| Backlog #102 | Resolved — Polygon WS cross-replica leader election via Redis SETNX (polygon-socket.ts:117) |
| Tool route auth | All `grid/*` routes auth-gated; `positions`/`terminal` dirs don't exist (script assumption stale) |
| Infra | `blackout-web` **Online, 5/5 replicas** (iad×3, US-West×2) |
| Secrets | All present: `UW_API_KEY`, `POLYGON_API_KEY`+`POLYGON_API_BASE`, `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SECRET`, `ANTHROPIC_API_KEY`, `CRON_SECRET`, `DATABASE_URL`, `REDIS_URL`, `VAPID_PRIVATE_KEY`, `CF_API_TOKEN`+`CF_ZONE_ID` |
| VAPID | Now present → gex-alerts no longer hard-inert |
| Tech debt | 19 TODO/FIXME total (no real `NOT IMPLEMENTED`/`BROKEN` stubs; "broken" matches are event-type names) |

## Phase Results
- **Live Site:** 0 real issues (public 200, protected 401 — both correct)
- **Code Audit:** 0 real issues (tsc 0, all critical-path guards present)
- **Cross-Tool Consistency:** not sampleable (market closed + auth-gated) — deferred to Mon RTH
- **Infrastructure:** 0 issues (5/5 replicas, all secrets present)
- **Open Task Backlog:** #97/#100/#101/#102 resolved; carried opens = P1-A, regime-fail-open, grid-copy, P2-C, P2-D
