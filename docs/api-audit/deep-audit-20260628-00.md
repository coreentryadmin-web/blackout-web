# BlackOut Deep Platform Audit
**Run time:** 2026-06-28 00:14 (local PT) / 07:14 UTC
**Market session:** CLOSED (Saturday night ET — RTH freshness checks N/A)
**Overall Risk:** LOW (1 P1, 1 P2, 1 P3 + 1 status change)

## Headline
The platform is healthy. Landing, auth, and health surfaces are all 200 and fast
(<600ms). All data endpoints correctly return 401 when unauthenticated. TypeScript
compiles clean (0 errors). The previously-CRITICAL infra invariants all hold:
db.ts pg Pool error handler present, Redis `family:0` + retry present, SPX play
engine has both veto guard and open logic.

**Most of the scripted check "failures" this run were false positives caused by the
audit SKILL itself using stale endpoint paths and wrong env-var names** (see P2 below).
Those are corrected in this report; the audit script should be updated.

## P0 — Fix Immediately (User-Facing Breakage)
None.

## P1 — Fix Today (Feature Broken or Degraded)
- **Entitlement leak: `/api/signals/open` is publicly reachable with no auth gate.**
  `src/app/api/signals/open/route.ts:8` — the `GET` handler runs an unguarded query
  returning up to 500 `signal_events` rows including `grade`, `ticker`, `strike`,
  `expiry`, `option_type`, `entry_mark`, `confluence_score`. Verified live:
  `GET https://www.blackouttrades.com/api/signals/open` → **HTTP 200** unauthenticated.
  Currently returns `{"ok":true,"signals":[]}` (empty — market closed, all signals
  scored to EOD), but **during RTH this exposes the day's live premium signal output
  to anyone**. Its sibling write routes (`signals/record`, `signals/outcome`) correctly
  use `isCronAuthorized`; this read route should use the same guard (it is only consumed
  by the signal-outcome-tracker cron).

## P2 — Fix This Week (Wrong But Not Visible)
- **The audit SKILL.md uses stale endpoint paths and wrong env-var names**, producing
  false positives every run. Corrections found this run:
  - `/api/market/spx-pulse` → actual `/api/market/spx/pulse`
  - `/api/flows` → actual `/api/market/flows`
  - `/api/nighthawk/latest-edition` → actual `/api/market/nighthawk/edition`
  - `/api/grid/news` → no `news` panel exists (grid panels: analysts, catalysts,
    congress, dark-pool, earnings, economy, movers, sectors)
  - `UNUSUAL_WHALES_API_KEY` → actual `UW_API_KEY` (SET; script reported MISSING)
  - `VAPID_PUBLIC_KEY` → actual `NEXT_PUBLIC_VAPID_PUBLIC_KEY` (SET)
  - The auth regex misses `requireTierApi` / `authorizeCronOrTierApi` / `isCronAuthorized`
    / `CRON_SECRET`, flagging 11 properly-gated routes as "no auth."
  Action: update the scheduled-task SKILL.md so future runs don't drown real findings.

## P3 — Tech Debt
- **`track-record/publish` fail-opens if `CRON_SECRET` is unset.**
  `src/app/api/track-record/publish/route.ts:9` — `if (process.env.CRON_SECRET && auth !== ...)`
  means an unset secret bypasses the check entirely. `CRON_SECRET` is confirmed SET in
  prod, so no live exposure, but the pattern should fail closed.

## STATUS CHANGE (informational — not a bug)
- **Web push + GEX alerts are now LIVE.** All gates are now satisfied in Railway:
  `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, and
  `GEX_ALERTS_PUSH` are all SET. The gex-alerts cron (`src/app/api/cron/gex-alerts/route.ts`)
  and push send path (`src/lib/push/send-web-push.ts`) are no longer inert — they will
  broadcast. The stale "inert until VAPID" comment at `src/lib/cron-registry.ts:132`
  should be updated. Recommend a follow-up to confirm push delivery behaves (subscriptions
  table, no send errors) now that it's armed.

## Backlog tasks — RESOLVED since last noted state
- **#97** SpxDarkPoolCard — now imported & mounted (`SpxDashboard.tsx:13,105`). CLOSED.
- **#100** db.ts pg Pool error handler — present. CLOSED.
- **#101** Clerk `user.created` webhook — handled by canonical `webhooks/clerk` (plural);
  singular `webhook/clerk` is an intentional re-export alias. CLOSED.
- **#102** Polygon WS replica guard — present in `polygon-socket.ts`. CLOSED.

## Phase Results
- **Live Site:** 0 issues (all surfaces 200/expected-401; market closed)
- **Code Audit:** 1 real (P1 signals/open), 1 P3; 11 auth false-positives dismissed
- **Cross-Tool Consistency:** not runnable externally (data endpoints auth-gated, market closed)
- **Infrastructure:** env vars present (correct names); Railway `status`/`logs` need
  `--service` with project token — skipped, not a defect
- **TypeScript:** 0 errors

## Notes / limitations this run
- Live GEX/SPX wall consistency could not be checked: endpoints require a session and
  the market is closed. Re-verify during RTH with an authenticated probe.
- Railway per-service log scan skipped (project token requires `--service`); no
  app-level error sampling this run.
