# BlackOut Deep Platform Audit
**Run time:** 2026-06-29 12:14 PT (~15:09 ET, mid-RTH, market open)
**Overall Risk:** MEDIUM — no live P0; two prior P0s RESOLVED this run; only P1 operational + P2 copy remain
**Issues found:** 0 P0 | 2 P1 | 1 P2 | 1 P3-meta

## Headline: both standing P0s cleared during this run
The 11:19-ET run (~3 h earlier, report `-08`) found a live P0 — five RTH data-writer crons dead ~7.6 h —
and noted SPX plays were stuck SCANNING downstream of it. **Both are now resolved, verified against prod Postgres:**

- **Writer crons recovered.** All five back on schedule (≤14 min ages, mid-RTH):
  `flow-ingest` 13.9m (status `skipped` = no-new-data, healthy), `uw-cache-refresh` 13.0m,
  `nights-watch-warm` 12.0m, `heatmap-warm` 12.7m, `grid-warm` 13.7m. `data-correctness` 11.1m, `spx-evaluate` 6.9m.
- **`market-regime-detector` is now RUNNING** (6.9 min) — the 04:xx run reported the service as absent. Resolved.
- **SPX plays are OPENING — first time in platform history.** Prod counts: `spx_open_play=3`,
  `spx_play_outcomes=3`, `spx_signal_log=2`; `flow_alerts` last 30 min = **113**. This is exactly the outcome
  the 11:19 run predicted once flow-ingest was restored on a clean-tape window. The long-standing
  "plays never open" P0 (`project_spx_plays_never_open`) is closed.

## P0 — Fix Immediately
None. (Both prior P0s — dead writer crons, plays-never-open — resolved this run.)

## P1 — Fix Today (operational; code is green)
- **`DISCORD_OPS_WEBHOOK_URL` still NOT SET** (Railway prod env). Cron-Staleness-Watchdog detects stale
  writers but `alert_delivered:false`; `cron-run.ts` failure-Discord can't fire either. This is *why* the
  ~7.6 h writer outage earlier today went unnoticed by a human. Operator: set the webhook (and/or enable
  watchdog self-heal) so the next outage pages someone. — env, no code change.
- **27 unpushed commits on `main`** (`git rev-list --count origin/main..HEAD = 27`, was 11 at 04:xx).
  Real fixes (incl. the signal-analytics 500 fix) are committed LOCAL only and NOT deployed to prod.
  By-design under the cron-no-push policy, but the deploy gap is widening — operator must review + `git push origin main`.

## P2 — Fix This Week (wrong but low-impact)
- **Grid page overpromises panels that don't exist.** `src/app/(site)/grid/page.tsx:13` (metadata description)
  and `:35` (subtitle) advertise "**News, flow**, analyst actions, and market pulse." The actual grid panel
  set is analysts / catalysts / congress / dark-pool / earnings / economy / movers / sectors — there is no
  `news` or `flows` grid panel (`/api/grid/news` 404s; news/flows live only under `/api/market/*`).
  Either wire the panels or fix the copy.

## P3 — Tech debt / audit-script hygiene
- **This audit SKILL.md still emits false positives.** Its Phase-1/2/4 hardcoded paths and var names are stale:
  - `/api/market/spx-pulse` → real is `/api/market/spx/pulse`; `/api/flows` → `/api/market/flows`;
    `/api/nighthawk/latest-edition` → `/api/market/nighthawk/edition`; `/api/grid/news` does not exist.
  - Env check uses `UNUSUAL_WHALES_API_KEY` (mythical) — code reads **`UW_API_KEY`** (which IS set).
  - Webhook check reads `src/app/api/webhook/clerk/route.ts` (singular) — the real `user.created`/`user.updated`
    handler is `src/app/api/webhooks/clerk/route.ts` (plural). #101 is NOT open.
  - Auth-grep (`requireToolApi|auth()|authorizeMarket`) misses `requireTierApi`/`authorizeCronOrTierApi`/`isCronAuthorized`.
  - SpxDarkPoolCard heuristic is wrong — it IS imported + mounted (`SpxDashboard.tsx:13,104`). #97 closed.
  Net: the script's own findings cannot be trusted without manual verification (done here). Fix the script.

## Verified GREEN this run
- **Build:** `tsc --noEmit` = **0 errors**.
- **Connection safety:** db.ts pg Pool error handler present (`db.ts:113`), `max:5`; redis `family:0`
  + `reconnectOnError` (`make-redis.ts:58,63`).
- **Known tasks resolved:** #97 (dark-pool card mounted), #100 (pool handler), #101 (clerk user.created, plural route),
  #102 (Polygon WS Redis SETNX leader election, `polygon-socket.ts:117`); SPX play option-chain veto correctly
  default-OFF (`spx-play-config.ts:418`, `SPX_OPTION_CHAIN_REQUIRED` unset → plays can open).
- **Infra:** `blackout-web` Online, **5/5 replicas** (iad×3, US-West×2); Postgres reachable; recent Railway logs clean.
- **Secrets:** UW_API_KEY, POLYGON_API_KEY, CLERK_SECRET_KEY, ANTHROPIC_API_KEY, CRON_SECRET, CLERK_WEBHOOK_SECRET,
  DATABASE_URL, REDIS_URL, POLYGON_API_BASE all SET. **VAPID_PRIVATE_KEY now SET** and **CF_API_TOKEN now SET**
  (memory's "gex-alerts inert until VAPID" + "CF purge needs token" assumptions are stale — re-verify push alerts live).
- **Live surfaces:** Landing/Sign-in/Sign-up/Health/Ready all 200; `/api/health` db `configured`,
  `/api/ready` db `connected`; all `/api/market/*` + `/api/grid/*` correctly 401 unauthenticated (proper gating, not breakage).

## Phase Results
- **Live Site:** 0 issues (shell healthy; gated APIs 401 as designed)
- **Code Audit:** 0 issues (tsc clean; all critical-path fixes intact)
- **Cross-Tool Consistency:** not sampleable via HTTP (auth-gated); prod-PG spot checks all healthy
- **Infrastructure:** 2 operational (Discord webhook unset, deploy gap)
- **Open Task Backlog:** #97/#100/#101/#102 + veto + plays-open + regime-detector all RESOLVED
