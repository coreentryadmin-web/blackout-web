# BlackOut Deep Platform Audit
**Run time:** 2026-06-29 08:19 PT (15:19 UTC / 11:19 ET — mid-morning RTH, weekday)
**Auditor:** autonomous deep-platform-audit (every-4h)
**Overall Risk:** 🔴 **CRITICAL**
**Issues found:** 2 P0 · 2 P1 · 1 P2 · 3 P3 (audit-script meta)

> Method note: this run did NOT execute the SKILL.md PowerShell verbatim. Several
> hard-coded endpoint paths and env-var names in the script are stale (see P3), which
> would have produced misleading "0 issues" output. Findings below are grounded against
> live HTTP, prod Postgres (read-only), Railway status, and source at file:line.

---

## 🔴 P0 — Fix Immediately

### P0-1 — Five critical RTH data-writer crons silently DEAD for ~7.6 h during the trading session
**Evidence (prod `cron_job_runs`, DB now = 15:16 UTC):**

| Cron service | Schedule (toml) | Last run | Age |
|---|---|---|---|
| `flow-ingest` | `*/2 11-21 * * 1-5` | 07:41 UTC | **455 min** |
| `uw-cache-refresh` | `*/2 11-21 * * 1-5` | 07:42 UTC | **454 min** |
| `nights-watch-warm` | `* 11-21 * * 1-5` | 07:42 UTC | **455 min** |
| `heatmap-warm` | `* 11-21 * * 1-5` | 07:41 UTC | **456 min** |
| `grid-warm` | `*/2 11-21 * * 1-5` | 07:42 UTC | **455 min** |

These run every 1–2 min during 11–21 UTC. The current RTH window (11:00→15:16 UTC) should
have produced ~130–250 executions each; **zero** have fired. The last execution (07:41 UTC =
03:41 ET) is a deploy/boot run *outside* the schedule. Meanwhile lower-frequency crons
(`spx-evaluate` */5, `data-integrity`, `data-correctness`, `cron-staleness-watchdog`,
`membership-reconcile`) are all current (0–39 min). So this is a **subset failure, not a global outage.**

**Independently confirmed by the platform's own `data-correctness` cron @ 14:37 UTC:**
> "Critical writer 'Flow Ingest' is STALE during RTH (No run in 415m, limit 15m) — a live-data writer has silently died."
> (same for UW Cache Refresh, Night's Watch Warm, Heat Maps Warm)

**Root-cause hypothesis:** all five tomls set `restartPolicyType = "never"`
(`railway.flow-ingest.toml`, `railway.uw-cache-refresh.toml`, `railway.heatmap-warm.toml`,
`railway.nights-watch-warm.toml`, `railway.grid-warm.toml`). A redeploy/restart at ~07:41 UTC
appears to have left these cron *trigger* services in a state where the Railway schedule no
longer fires, and `never` means they don't recover on their own. (The healthy crons survived
the same window — the differentiator needs a Railway-side look at the five trigger services.)

**Impact (live, user-facing):** stale HELIX flow aggregates, stale UW cache, stale GEX/heatmaps,
stale Night's Watch valuations — during active trading. Downstream of this: P1-1 and P1-2 below.

**Remediation (no push/deploy required):** re-trigger immediately via the HTTP hitters —
`node scripts/hit-cron.mjs /api/cron/flow-ingest` (+ `uw-cache-refresh`, `heatmap-warm`,
`nights-watch-warm`, `grid-warm`) with `CRON_SECRET`. Durable fix: investigate why the five
Railway cron services stopped scheduling after the 07:41 UTC restart; consider
`restartPolicyType = "on_failure"` for trigger services and/or rely on watchdog self-heal (see P0-2).

---

### P0-2 — Staleness watchdog DETECTS the outage but never escalates (`alert_delivered:false`, `self_heal_enabled:false`)
**Evidence (`cron_job_runs` job_key=`cron-staleness-watchdog`, last @ 15:00 UTC):**
```
checked:21  problems:8  rth_stale:5
problem_keys: [flow-ingest, nighthawk-outcomes, nighthawk-playbook, uw-cache-refresh,
               nights-watch-warm, heatmap-warm, grid-warm, data-correctness]
rth_stale_keys:[flow-ingest, uw-cache-refresh, nights-watch-warm, heatmap-warm, grid-warm]
alert_delivered: false      <-- detected, but NO alert sent
self_heal_enabled: false    <-- no auto-restart
self_healed: []
status logged: "ok"         <-- 8 problems, still logs OK
```
The watchdog runs every minute (72 clean runs/24h) and correctly identifies all five dead
writers — but **delivers no alert and self-heal is disabled**. Worse, it records cron status
`"ok"`, so the failure-only Discord path in `src/lib/cron-run.ts:38` (`if (status === "failed")
notifyOpsDiscord(...)`) also never fires. Net result: a multi-hour live-data outage during RTH
with **zero human notification**. The canary works; the alarm wire is cut.

**Remediation:** wire `alert_delivered` to an ops channel that is actually configured (the
`notifyOpsDiscord` webhook and/or VAPID push appear unconfigured), and/or set
`self_heal_enabled` true so the watchdog re-triggers dead RTH writers. Until then the watchdog
is observe-only.

---

## 🟠 P1 — Fix Today

### P1-1 — SPX play lifecycle produces ZERO data; SPX Slayer track record is empty (downstream of P0-1)
**Evidence:** `spx_open_play` = 0 rows ever (`max(opened_at)` NULL); `spx_play_outcomes` = 0 rows
ever; `spx_signal_log` = 0 rows ever.
The engine itself is **healthy and running** — `spx-evaluate` cron: 52 runs/24h, 0 failures,
latest 15:15 UTC. But **every tick returns `action=SCANNING`, grade C/D**, never an A-grade
approval. Recurring block reasons (from cron meta_json):
- "Tape's mixed — too many conflicting signals for clean entry"
- **"Flow data stale (11m) — tape and 0DTE signals unreliable"**

**This re-frames the previously-tracked P0 (`project_spx_plays_never_open`).** The option-chain
veto is now correctly DISABLED — `playOptionChainRequired()` defaults `false`
(`src/lib/spx-play-config.ts:417`) and `SPX_OPTION_CHAIN_REQUIRED` is unset in Railway, so
`spx-play-engine.ts:811` no longer blocks opens. The real reason no play opens today is upstream:
the engine never reaches an approval because (a) genuinely mixed tape and (b) **stale flow input
caused by P0-1's dead `flow-ingest`/`uw-cache-refresh` writers**. Fix P0-1 and re-check whether
A-grade approvals (and the first `spx_open_play` row) appear during a clean-tape window.

### P1-2 — Cross-provider GEX divergence (downstream symptom of stale heatmap writer)
**Evidence (`data-correctness` flags @ 14:37 UTC):**
- "SPX net-GEX sign (negative) CONTRADICTS UW (0DTE, positive) — dealer regime disagreement." (`net_gex`)
- "SPX King 7,300 DISAGREES with UW King 7,425 — Δ 1.69% of spot > tol." (`king`)

Consistent with SPX-side GEX being served from a **stale `heatmap-warm` cache** (dead 7.6 h)
while UW is live. Likely resolves when P0-1 is fixed; re-audit the king/net_gex agreement after
the heatmap writer is restored before treating it as an independent data bug.

---

## 🟡 P2 — Fix This Week

### P2-1 — `data-correctness` cron logs `status='failed'` whenever it raises flags
`data-correctness` recorded `failed` at 14:37 (6 flags) and 13:34 (8 flags). This is its *designed*
behavior (a raised flag → `ok:false`), but it means a genuine data-quality flag is
indistinguishable from a cron *crash* in `cron_job_runs`, and (per `cron-run.ts:38`) fires a
"critical" ops Discord on every flagged run. Consider a distinct status (e.g. `flagged`) so true
crashes stand out and flag-noise doesn't desensitize the failure alert. (Most flags today are the
P0/P1 staleness cascade, not new data bugs.)

---

## 🟢 Verified HEALTHY this run (regression guards holding)
- **db.ts pg Pool error handler present** (`src/lib/db.ts:113`), `max:5` — task #100 holds.
- **Redis `family:0` + `reconnectOnError`** present (`src/lib/make-redis.ts:58,63`).
- **SPX option-chain veto correctly disabled** — `playOptionChainRequired()` defaults false; env unset.
- **Clerk `user.created` handled** — `src/app/api/webhooks/clerk/route.ts:50`; singular `/webhook/clerk` is an intentional alias re-export. Task #101 resolved.
- **Polygon WS leader election** (Redis SETNX `polygon:indices:leader`, 25s TTL) — `src/lib/ws/polygon-socket.ts:117`. Task #102 resolved.
- **`SpxDarkPoolCard` imported & mounted** — `src/components/SpxDashboard.tsx:13,104`. Task #97 resolved.
- **TypeScript clean** — `tsc --noEmit` exit 0, 0 errors.
- **No real TODO/FIXME debt** — 3 hits, all false positives (const names / prompt strings).
- **API auth solid** — 7 "ungated" routes flagged by naive grep are all fine (5 use `requireTierApi`/`authorizeCronOrTierApi`/`isCronAuthorized`; `regime` & `anomalies` documented intentionally-public, market-wide data).
- **Infra:** `blackout-web` Online 5/5 replicas; Postgres + Redis Online; no errors in recent deploy logs; all critical env vars present (`UW_API_KEY`, `POLYGON_API_KEY`, `CLERK_SECRET_KEY`, `ANTHROPIC_API_KEY`, `CRON_SECRET`, `CLERK_WEBHOOK_SECRET`, `DATABASE_URL`, `REDIS_URL`).
- **Live HTTP:** Landing/Sign-in/Sign-up/Health all 200; all data endpoints correctly 401 (auth-gated).

---

## 🔧 P3 — Audit-script accuracy (the SKILL.md itself is stale — fix so future runs aren't misleading)
1. **Phase-1 endpoint paths are wrong**, so the script's HTTP checks 404 and silently "pass":
   - `/api/market/spx-pulse` → real `/api/market/spx/pulse`
   - `/api/flows` → real `/api/market/flows`
   - `/api/nighthawk/latest-edition` → real `/api/market/nighthawk/edition`
   - `/api/grid/news` → does not exist (grid has analysts/catalysts/congress/dark-pool/earnings/economy/movers/sectors; news is `/api/market/news`)
2. **Phase-4 required-env list uses `UNUSUAL_WHALES_API_KEY`** — code uses **`UW_API_KEY`**. Verbatim run reports a false "P0: env var not set."
3. **Phase-2e auth grep** misses `requireTierApi` / `authorizeCronOrTierApi` / `isCronAuthorized`, producing false "no auth check" positives. Also note: page-route middleware does NOT gate `/api` routes (see `src/middleware.ts:44`) — each API route self-authorizes, which is what these helpers do.

---

## Phase Results
- **Live Site:** 0 real issues (paths in script stale; real surfaces healthy)
- **Code Audit:** 0 new issues; all tracked regressions (#97/#100/#101/#102 + veto) confirmed FIXED
- **Cross-Tool / Data:** 2 P0, 2 P1, 1 P2 — dominated by the dead-writer cascade
- **Infrastructure:** main service + DBs healthy; failure is in 5 cron *trigger* services
- **Backlog:** veto/play-open root cause re-attributed to upstream cron death (P0-1), not the engine
