# BlackOut Deep Platform Audit
**Run time:** 2026-06-29 16:46 PT (23:46 UTC) — market CLOSED (post-RTH)
**Overall Risk:** MEDIUM
**Issues found (real):** 0 P0 | 2 P1 | 3 P2/observations
**Auditor note:** Most of the SKILL's Phase-1 "P0/P1" signals this run were **false positives** caused by stale endpoint paths, the `www`-strips-auth redirect, and `Invoke-WebRequest` latency artifacts. Those are documented under "Audit-script staleness" so the next run trusts the right signals. The genuine findings are below.

---

## P0 — Fix Immediately (User-Facing Breakage)
**None.** Live data surfaces verified healthy via the *correct* endpoints + Bearer auth:
- GEX positioning live & correct: SPY spot 740.85, walls call 741 / put 725, flip 745.75, `asof` fresh (23:39Z). Gamma read correctly says **short gamma** (spot 740.85 < flip 745.75) — the `net_gex>0 ⇒ range` trap is handled right.
- HELIX flow tape healthy: 14,815 `flow_alerts` total, 1,987 in last 24h, last write 20:12Z (16:12 ET, RTH tail).
- SPX plays **opening AND closing** (see "Resolved" — this was the #1 open item).

## P1 — Fix Today (Feature Broken or Degraded)

### P1-A — Play outcome misclassifies profitable exits as losses (DATA INTEGRITY)
**File:** `src/lib/spx-play-outcomes.ts:177-178`
```ts
if (close.was_loss || close.exit_action === "STOP" || close.exit_action === "THESIS") {
  return "loss";   // ← returns "loss" WITHOUT checking pnl_pts sign
}
```
This branch forces `"loss"` for any `STOP`/`THESIS` exit (or `was_loss` flag) **regardless of realized P&L** — unlike the `THETA/SESSION` branch (line 173-175) and the `TRAIL` branch (line 183-186), which correctly count `pnl_pts >= 0` as a win.

**Live proof (today's 3 plays, all A/A+ grade):**
| dir | exit_action | pnl_pts | entry → exit | recorded outcome | correct outcome |
|-----|-------------|---------|--------------|------------------|-----------------|
| long | STOP   | -7.15 | 7430.90 → 7423.75 | loss | loss ✅ |
| long | THESIS | -2.47 | 7435.05 → 7432.58 | loss | loss ✅ |
| long | THESIS | **+7.30** | 7432.13 → **7439.43** | **loss ❌** | **win** |

The 3rd play exited 7.30 pts *above* entry on a long — a real winner — but is stored as a loss. **Today's A+ track record reads 0/3 wins when it is actually 1/3.** This understates win-rate and shows a profitable trade as a loss on any track-record/P&L surface users see.
**Fix:** mirror the TRAIL/THETA logic in the STOP/THESIS branch — a trailed-into-profit stop or a green THESIS exit (`pnl_pts > 0`) is a win (or at minimum not a loss). Only classify negative-P&L STOP/THESIS exits as losses.

### P1-B — Signal learning pipeline still dormant (verified live)
**Tables:** `signal_events` = **0 rows**, `signal_outcomes` = **0 rows** (queried prod just now).
The schema is present and correct (`fired_at, signal_source, signal_type, grade, spx_price, call_wall, put_wall, confluence_score, ...`) but **nothing writes to it** — the recorder remains inert (known-deferred "pending entry/exit schema decision"). Consequence: the learning loop is dormant and any signal panel renders empty. The standing pending-items verification *"signal panel populates"* is **NOT met** as of this run. Decide the entry/exit schema and wire the recorder, or explicitly de-scope the panel.

## P2 — Fix This Week (Wrong But Not Visible)

- **P2-A — Today's play sample is all-long, all-stopped-out, 2/3 never went green.** The 3 A/A+ plays were all `direction=long`, none hit `TARGET`, two had `mfe_pts=0` (price went immediately against entry). 3 samples is too few to call a defect, but the engine opening only longs and getting immediately stopped is worth watching for an entry-timing or long-bias issue. Monitor next RTH.
- **P2-B — SPX pulse returns `available:false` / `price:0` / all-null after the close.** Expected (SPX index snapshot is RTH-only) but confirm the SPX desk UI degrades to an explicit "market closed" state rather than rendering zeros/blank metrics. `src/app/api/market/spx/pulse`.
- **P2-C — GEX endpoint TTFB ~1.7s on a Cloudflare cache HIT.** `/api/market/gex-positioning` served `Cf-Cache-Status: HIT Age:1277` but TTFB was 1.7s warm — slow for an edge HIT (origin headers `X-Railway-Request-Id` present on a HIT). Minor perf; not user-blocking.

---

## ✅ Resolved Since Prior Audits (verified live + in code this run)
| Item | Status | Evidence |
|------|--------|----------|
| **SPX plays never open** (was the #1 pending item) | **RESOLVED** | 3 plays opened+closed today; `spx_play_outcomes` last_open 18:25Z |
| #100 db.ts pg Pool error handler | RESOLVED | `livePool.on("error", …)` at `db.ts:113`; pool `max:5` (safe) |
| #101 Clerk `user.created` webhook | RESOLVED | handled at `webhooks/clerk/route.ts:50` via svix; singular `webhook/clerk` is just a re-export alias |
| #102 Polygon WS per-replica storm | RESOLVED | Redis SETNX leader election `polygon-socket.ts:117-147` (`polygon:indices:leader`, 25s TTL) |
| #97 SpxDarkPoolCard never imported | RESOLVED | imported+mounted `SpxDashboard.tsx:13,104` |
| Redis IPv6 ETIMEDOUT | OK | `family:0` + `reconnectOnError` `make-redis.ts:58,63` |

## 🛠 Audit-Script Staleness (fix the SKILL, not the platform)
These produced misleading findings this run and should be corrected in `deep-platform-audit/SKILL.md`:
1. **Phase 1 endpoint paths are stale** → all 404s are the script's fault, not the site:
   - `/api/market/spx-pulse` → real is `/api/market/spx/pulse`
   - `/api/flows` → `/api/market/flows`
   - `/api/grid/news` → **no such panel** (grid = analysts, catalysts, congress, dark-pool, earnings, economy, movers, sectors)
   - `/api/nighthawk/latest-edition` → `/api/nighthawk` or `/api/market/nighthawk/edition`
2. **Uses `www` host** → Cloudflare 301→apex and strips `Authorization` → all gated endpoints 401. Use `https://blackouttrades.com` (apex) + `Bearer $CRON_SECRET` for gated checks (per memory `apex_host_strips_auth`). 401s this run were *correct auth-gating*, not bugs.
3. **`Invoke-WebRequest` latency is unreliable** — reported the landing page at 44-59s; `curl` shows <2s warm on a Cloudflare HIT (`Age:1277`). Phase-1 "slow" P1s were artifacts. Switch latency checks to `curl.exe -w "%{time_starttransfer}"`.
4. **Phase 2c Clerk check** reads the singular alias file (only re-exports) → false "no user.created". Point it at `webhooks/clerk` (plural).
5. **Phase 5 #73** greps `src/lib/tools` (does not exist) → false "no SPX grounding tool". Largo tools live at `src/lib/largo/tool-defs.ts` + `run-tool.ts`.

## Phase Results (real issues only)
- **Live Site:** 0 real (4 false-positive "slow" + 7 false-positive 4xx from script staleness)
- **Code Audit:** 1 real (P1-A)
- **Cross-Tool Consistency:** 0 (GEX live/correct; pulse blank post-close by design; SPY-vs-SPX wall scale is by design)
- **Infrastructure:** 0 (Redis/DB guards intact; on `main`, tree has unrelated cron doc edits)
- **Open Task Backlog:** 1 still open (P1-B signal pipeline); 5 prior items verified resolved
