# BlackOut Deep Platform Audit
**Run time:** 2026-06-27 07:10 ET (Saturday — market closed, no RTH freshness expectations)
**Overall Risk:** MEDIUM
**Issues found:** 0 P0 | 0 P1 (active) | 3 P2 | 2 P3
**Auditor:** autonomous deep-platform-audit (4h cron)

> ⚠️ SELF-CORRECTION (after first draft): I initially filed the empty SPX play ledger as a
> P1 "opens broken, root cause is an upstream entry gate." That was **overstated**. Checking
> git: the fetch-bug fix `6f00a5e` ("query SPX chain root + don't let chain gate veto entry")
> merged **2026-06-26 16:20 ET — ~20 min AFTER Friday's RTH close (16:00 ET)**. So Friday's
> session ran on the OLD pre-fix code and **no trading session has run with the fix deployed
> yet.** The empty ledger is EXPECTED, not a regression, and my "entry gate rejects everything"
> claim was an unsupported inference. Correctly reclassified as **P2-C: pending first post-fix
> RTH validation (Mon 2026-06-29).** Risk lowered HIGH → MEDIUM.

> NOTE ON METHOD: The canned PowerShell endpoint URLs in the task skill are STALE
> (`/api/market/spx-pulse`, `/api/flows`, `/api/nighthawk/latest-edition`,
> `/api/grid/news` etc. all 404). Real paths verified from source:
> `/api/market/spx/pulse`, `/api/market/flows`, `/api/market/nighthawk/edition`,
> grid panels are `analysts|congress|dark-pool|earnings|economy|movers|sectors`
> (no `news`/`flows`/`catalysts` route exists). The skill's required-env-var names
> are also wrong (`UNUSUAL_WHALES_API_KEY` → code reads `UW_API_KEY`;
> `DB pool.on` → code uses `livePool.on`). These produced FALSE positives that I
> overrode by reading source + live state directly. **Action item: update the
> skill's URL/env-var lists so future runs don't chase phantom 404s.**

---

## P0 — Fix Immediately (User-Facing Breakage)
None.

## P1 — Fix Today (Feature Broken or Degraded)
None **active**. (The SPX play ledger emptiness was reclassified to P2-C below after the
git-timing correction — it is a pending verification, not a confirmed breakage.)

---

## P2 — Fix This Week (Wrong But Not Visible)

### P2-C — SPX play ledger empty → PENDING first post-fix RTH validation (Mon 2026-06-29)
**Evidence (prod Postgres, queried live this run):** `spx_open_play` = 0 rows all-time,
`spx_play_outcomes` = 0 rows all-time.

**Why this is most likely EXPECTED, not a bug:** The fetch-bug fix `6f00a5e`
("query SPX chain root + don't let chain gate veto entry") merged **2026-06-26 16:20 ET — about
20 min AFTER Friday's RTH close (16:00 ET)**. Friday's session therefore ran on the OLD pre-fix
code, and **no trading session has run with the fix deployed.** With zero post-fix RTH ticks,
an empty ledger is the expected state. The option-chain veto is confirmed disabled
(`SPX_OPTION_CHAIN_REQUIRED` unset; `playOptionChainRequired()` defaults false at
`spx-play-config.ts:404`), and diagnostic tracing for the open path is already committed
(`63567cb` "Add diagnostic logging to spx-play-engine to trace why plays never open").

**The cron path is correctly wired:** `SPX-Engine-Evaluation` (`*/5 11-20 * * 1-5`, Online) →
`/api/cron/spx-evaluate` → `runSpxEvaluator` → `evaluateSpxPlay({ mutate: true })`
(`src/lib/spx-evaluator.ts:41`) + heartbeat. The "ALL GATES PASSED → openPlay" branch
(`spx-play-engine.ts:833` → `openPlay()` → INSERT `db.ts:1242`) exists and is reachable.

**ACTION — verify Monday 2026-06-29 after RTH close:** re-query prod `spx_open_play`
(should have ≥1 row if the fix worked) and `spx_play_outcomes` (populates on close). **IF still
0 rows after Monday's full session**, escalate to P1 and read the `63567cb` diagnostic logs to
find which entry gate (confluence grade / Claude verdict / mtf / entryGates) rejects — and do
NOT re-touch the veto, it is fixed. Until Monday this is a watch item, not an active breakage.

---

### Other P2

### P2-A — Two market endpoints serve unauthenticated empty payloads instead of 401
`src/middleware.ts` explicitly documents that **API routes are NOT protected by middleware —
each route must self-authorize**. Live test of the 8 routes lacking an in-route auth grep match:
`lotto/today`, `platform/snapshot`, `spx/play`, `ticker-search` correctly return **401**;
`spx/commentary` returns 405 (POST-only); `health` is intended-public. But:
- `/api/market/anomalies` → 200 `{"anomalies":[]}` (no auth check)
- `/api/market/regime` → 200 `{"available":false}` (no auth check)

Neither leaks paid data *today* (both return empty), but they are inconsistent with their
siblings and would leak if/when they start returning real payloads. Add the same
`requireToolApi`/entitlement guard the sibling market routes use, or confirm they are
intentionally public and annotate them.
Files: `src/app/api/market/anomalies/route.ts`, `src/app/api/market/regime/route.ts`.

### P2-B — `spx_signal_log` stale 10 days (legacy surface)
`spx_signal_log` last wrote 2026-06-17. If anything still reads it (admin/analytics), it is
serving 10-day-old signals. Confirm it is fully superseded by the play engine and either
resume writes or retire the table + its readers.

---

## P3 — Tech Debt
- `spx_pulse_snapshots` and `spx_watch_setups` exist in prod but have **zero INSERT code
  references** in `src/` and 0 rows all-time → dead/legacy tables. Drop them or wire the
  writers if they were meant to back a feature. (Not a bug; cleanup only.)
- Audit skill's PowerShell endpoint URLs + env-var names are stale (see top note) — they
  generate false 404/MISSING findings every run.

---

## Backlog Status (#70–#107) — verified RESOLVED this run
| Task | Item | Status | Evidence |
|------|------|--------|----------|
| #100 | db.ts pg Pool error handler | ✅ RESOLVED | `livePool.on("error")` at `src/lib/db.ts:113`; pool max default 5 |
| #101 | Clerk `user.created` webhook | ✅ RESOLVED | handled at `src/app/api/webhook/clerk/route.ts:77` |
| #102 | Polygon WS replica guard | ✅ RESOLVED | Redis SETNX leader election `src/lib/ws/polygon-socket.ts:117-148` |
| #97  | SpxDarkPoolCard never imported | ✅ RESOLVED | imported + mounted `src/components/SpxDashboard.tsx:13,86` |
| #73  | Largo SPX grounding tools | ✅ PRESENT | `src/lib/largo/{spx-desk-cache,tool-defs,run-tool}.ts` (skill's `src/lib/tools` path was wrong) |
| —    | SPX option-chain veto (project_spx_plays_never_open) | ✅ Veto fixed; ⏳ opens pending Mon 06-29 RTH validation | see P2-C |

---

## Phase Results
- **Live Site:** Landing/SignIn/SignUp/Health all 200 & fast (<600ms). All data endpoints
  properly 401 (auth-gated) — no public data leakage. Could not verify data freshness/cross-tool
  consistency: unauthenticated + weekend (market closed).
- **Code Audit:** db.ts ✅ redis (family:0 + reconnectOnError) ✅ veto fixed ✅
  **TypeScript: 0 errors (tsc --noEmit clean)** ✅ 0 real TODO/FIXME ✅ auth coverage OK (2 P2 consistency gaps).
- **Cross-Tool Consistency:** Not runnable unauthenticated; weekend → no live GEX/pulse to compare.
- **Infrastructure:** ✅ All Railway services Online/Completed. blackout-web Online, Postgres Online,
  Redis Online. Crons scheduled (SPX-Engine `*/5 11-20 * * 1-5`, Flow-Ingest `*/5 * * * *`,
  NightHawk-Outcomes, etc.). Critical env vars present under correct names
  (`UW_API_KEY`, `POLYGON_API_KEY`+`POLYGON_API_BASE`, `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SECRET`,
  `ANTHROPIC_API_KEY`, `CRON_SECRET`, `DATABASE_URL`, `REDIS_URL`).
- **Backlog:** 5/5 checked known tasks RESOLVED; 1 pending verification (SPX opens, P2-C — fix deployed post-Friday-close, validates Mon 06-29).
