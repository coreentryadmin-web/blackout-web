# BlackOut Deep Platform Audit
**Run time:** 2026-06-30 ~08:1x PT (≈11:1x ET / 15:1x UTC) — Tuesday, **RTH OPEN** (market live)
**Method:** live data via apex (`blackouttrades.com`) + Bearer CRON_SECRET, Railway prod env (names only), full code read, TSC
**Overall Risk:** HIGH (one carried P1 data-integrity bug; **no new breakage**; platform serving live correctly)
**Issues found:** 0 P0 · 1 P1 · 4 P2 · 1 P3-meta

> Continuation of `deep-audit-20260630-04.md`. The 04:00 run's findings all still stand; this run re-confirms them against **live RTH data** and adds infra/env detail. Net change since 04:00: VAPID now fully configured, a 4th play outcome closed today, PG connection-ceiling quantified.

---

## P0 — Fix Immediately (User-Facing Breakage)
**None.** Every real surface is healthy and serving live data during RTH (see Confirmed Healthy).

## P1 — Fix Today (Feature Broken or Degraded)
- **[OPEN · 🟠 DATA INTEGRITY · carried ≥5 runs · re-confirmed in code + live this run]** **Play outcomes mislabel profitable STOP/THESIS exits as losses.**
  `src/lib/spx-play-outcomes.ts:177-178` — `if (close.was_loss || exit_action === "STOP" || exit_action === "THESIS") return "loss"` returns loss **without checking `pnl_pts` sign**, while THETA/SESSION (`:173-174`) and TRAIL (`:184-185`) both branch on the sign. A profitable THESIS exit is persisted `outcome='loss'`.
  **Live evidence this run (prod `/api/market/spx/outcomes`):** `total_closed:4, wins:0, losses:4, win_rate:0`. Today's id 4 (long 7475.64→7462.02, −13.62, STOP) is a *legitimate* loss, but the carried id 3 (+7.30 THESIS) is the mislabel → the public **0% win-rate** track record understates true performance (~25% on this sample).
  **Fix:** for `STOP`/`THESIS`, `pnl_pts > 0 → "win"` (mirror the TRAIL branch). One-liner.
  **Not auto-fixed** — data-integrity + behavior change wants a reviewed human commit, not a cron commit.

## P2 — Fix This Week (Wrong But Not Visible)
- **[OPEN · learning loop dormant · carried]** **`signal_events` / `signal_outcomes` still produce nothing.** The SPX *play* signals store (`/api/market/spx/signals` = 4 rows) is live, but the separate signal-accuracy learning tables remain unwired (recorder deferred per project notes). Tracking so it doesn't fall off.
- **[NEW · infra headroom]** **Postgres connection ceiling has no buffer.** Railway `PG_POOL_MAX=15` × **5 replicas = 75 backend connections** against a ~97 hard ceiling, with **no PgBouncer** in front (`db.ts:92` PgBouncer comment is aspirational, not live). A 6th replica, a deploy overlap, or a manual `psql`/migration session can exhaust the pool. Code default is `5` (`db.ts:102`) — prod overrides to 15. Lower `PG_POOL_MAX` toward 10 (→50 conns) or stand up PgBouncer before scaling replicas.
- **[NEW · perf]** **`/api/market/spx/desk` is heavy and intermittently times out (>15 s).** The desk aggregates ~80 fields (price/VWAP/GEX/flow/breadth/macro/news in one payload) and returned 200 with full fresh data twice but timed out twice in this run's sampling. Under UI contention this risks a slow/blank desk load. Consider a cached snapshot read or splitting the heavy sub-bundles.
- **[NEW · safety opt-in unset]** **AI spend hard-cap disarmed.** `DAILY_AI_SPEND_KILL_USD` is unset in prod, so the kill-switch is opt-in-and-off (by design, but there is no hard ceiling on AI spend). Arm it with a dollar value if a backstop is wanted.

## P3 — Code Quality / Meta
- **[FIX THE AUDIT SKILL — recurring every run, manufacturing phantom P0s]** `SKILL.md` Phases 1–5 probe dead paths, wrong host, wrong env-var names, and stale freshness math. The raw script output this run showed **8 false 404/401s and would have emitted ≥3 false P0s**. Concrete fixes:
  - **Host:** Phase 1 hits `www` → Cloudflare strips `Authorization` → false 401 on every gated endpoint. Use **apex `blackouttrades.com` + `Bearer CRON_SECRET`**.
  - **Paths:** `spx-pulse` → `market/spx/desk` (or `market/spx/pulse`); `/api/flows` → `market/flows`; `grid/news` is **nonexistent** (real panels: analysts, catalysts, congress, dark-pool, earnings, economy, movers, sectors); verify `nighthawk/latest-edition`.
  - **Freshness math (this generates a false P0 EVERY run):** `[DateTime]$x - [DateTime]::UtcNow` parses a `…Z` string to **Local** kind then subtracts `UtcNow`, injecting the local-UTC offset (**+7 h on PT**) → falsely reports "SPX desk data is ~420 min stale during RTH" as P0. Live truth this run: desk `as_of` age ≈ **0.8 min**. Fix: `[DateTimeOffset]::Parse($x).UtcDateTime`.
  - **Env var names:** checks `UNUSUAL_WHALES_API_KEY` (real: **`UW_API_KEY`** — SET) and `VAPID_PUBLIC_KEY` (real: **`NEXT_PUBLIC_VAPID_PUBLIC_KEY`** — SET) → false "env var NOT SET P0".
  - **Code greps:** `pool.on` misses `livePool.on` (db.ts:113); clerk grep reads the **singular alias** `webhook/clerk` (2-line re-export) not canonical `webhooks/clerk`; Largo grep reads `src/lib/tools` (**nonexistent** — real dir `src/lib/largo`); `import.*SpxDarkPoolCard` misses the multiline import.

---

## ✅ Confirmed Healthy This Run (live RTH ground truth via apex + Bearer)
- **Marketing/auth:** landing 200 (118 KB; cold first-load 8.5 s — watch, not flagged), sign-in 200/215 ms, sign-up 200/257 ms, `/api/health` 200.
- **Market data live & fresh during RTH:** `market/flows` 200 (26 KB, latest flow ~fresh) · `spx/desk` 200 (full ~80-field payload, `as_of` age ≈0.8 min, `feed_stalled` false) · `gex-positioning` 200 (heatmap now launched).
- **All 8 grid panels `available:true`:** analysts, catalysts, congress, dark-pool, earnings, economy, movers, sectors.
- **SPX play pipeline LIVE:** `spx/outcomes` = 4 closed (the long-dead "plays never open" path is fixed — `playOptionChainRequired()` defaults false, `SPX_OPTION_CHAIN_REQUIRED` unset in prod); engine currently correctly idle (grade D / score 29 / SCANNING).
- **TSC: 0 errors.** Build clean. `TODO/FIXME` in `src`: ~1 (very clean).
- **Resolved/verified-fixed (tasks #97/#100/#101/#102):** db pool error handler (`livePool.on("error")` db.ts:113) · Redis `family:0` + `reconnectOnError` (make-redis.ts:58,63) · Polygon WS Redis leader-election (`polygon:indices:leader` 25 s TTL, polygon-socket.ts:117-441) · Clerk `user.created`+`user.updated` (webhooks/clerk:50,66) · SpxDarkPoolCard wired into SpxDashboard.tsx.
- **Infra:** `blackout-web` ● Online, **5/5 replicas** (iad×3 · US-West×2). Required env all SET: `UW_API_KEY`, `POLYGON_API_KEY` (`POLYGON_API_BASE=api.massive.com`), `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SECRET`, `ANTHROPIC_API_KEY`, `CRON_SECRET`, `DATABASE_URL`, `REDIS_URL`. **VAPID now fully configured** (both `NEXT_PUBLIC_VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY` SET → push/alerts no longer inert). `LAUNCHED_TOOLS=heatmap,nighthawk,largo,grid`.

## Phase Results
- **Live Site:** 0 real issues (8 script false-positives = www host + dead paths)
- **Code Audit:** 0 new (P1 carried; #97/#100/#101/#102 confirmed fixed; TSC clean)
- **Cross-Tool Consistency:** 0 (desk fresh; grid panels all available)
- **Infrastructure:** 2 P2 (PG connection ceiling 75/97, AI kill-switch unset); env clean; VAPID now complete
- **Data Pipelines:** 1 P1 (outcome mislabel, root cause of 0% win-rate) + 1 P2 (signal_events dormant); SPX play pipeline LIVE
- **Railway logs:** not sampled this run (CLI `railway logs` exit 255 non-interactively — non-blocking)
