# BlackOut Deep Platform Audit
**Run time:** 2026-06-27 ~16:10 ET (Saturday — market CLOSED)
**Overall Risk:** HIGH (1 carried P1; no new P0/P1 regressions this run)
**Issues found:** 0 P0 · 1 P1 (carried) · 3 P2 (1 refined) · 2 P3 (carried)

> Weekend run: live RTH data endpoints are correctly auth-gated (401) and the market is
> closed, so this pass is a code + infrastructure + prod-DB-state audit rather than a
> live-data-freshness audit. No new defects introduced since the 12:13 run; the SPX ledger
> picture is materially clearer (see P2-C).

---

## P0 — Fix Immediately
None.

## P1 — Fix Today (carried, unchanged)
- **P1-A** `market_regime` + `flow_anomalies` features dead end-to-end but with LIVE consumers.
  No writer cron exists (confirmed again this run — `railway status` lists no regime/anomaly cron;
  the "market-regime-detector cron" named in `market/regime/route.ts:2` does not exist). Consumers
  that silently degrade: `FlowAnomalyBanner` on the paid `/flows` page, and
  `nighthawk-morning-confirm` reading `/api/platform/intel` (defaults regime=UNKNOWN / 0 anomalies).
  Fix: build the detector writer cron, or remove the dead consumers. _(carried from 12:13 run)_

## P2 — Fix This Week
- **P2-C ⏳ WATCH (refined this run)** SPX play ledger still empty all-time: `spx_open_play`=0,
  `spx_play_outcomes`=0 (re-verified live in prod). **New evidence this run:** the engine isn't
  just failing to *open* — it never reached a BUY in the last 3 active days. `cron_job_runs`
  action distribution for `spx-evaluate`: **198 SCANNING · 24 WATCHING · 0 BUY/APPROVE · 42 skipped**.
  So the cause is no longer the option-chain veto (confirmed disabled — `SPX_OPTION_CHAIN_REQUIRED`
  not set in Railway env, `playOptionChainRequired()` defaults false at `spx-play-config.ts:417`)
  but the confluence/Claude gates never approving. Two fresh fixes landed **today while market was
  closed**: `5eee3ff` "unblock play entries — 6-bug gate audit" (12:35 PT) and `cee2ebf`
  "0DTE signal calibration" (12:47 PT) — both **unvalidated** until Monday RTH. Cron path is
  correctly wired (`spx-evaluator.ts:41` → `evaluateSpxPlay({mutate:true})` → `openPlay` →
  `insertOpenSpxPlay`). **VERIFY Mon 2026-06-29 after RTH:** re-query `spx_open_play` (expect rows)
  + `cron_job_runs` for any `play_action=BUY`. If still 0 BUY after a full Monday session →
  escalate to P1 and read the `63567cb` diagnostic logs to find the rejecting gate. Do NOT
  re-touch the veto. _(was "likely expected, wait for Monday"; now: engine demonstrably never
  approved over 3 days, and a deeper gate fix was just shipped today — still gated on Monday)_
- **P2-A** `/api/market/anomalies` and `/api/market/regime` serve unauthenticated while sibling
  market routes 401. No paid-data leak today (both empty) but would leak once they return real
  payloads. Add the sibling entitlement guard. _(carried from 07:10 run)_
- **P2-B** `spx_signal_log` empty in the last 3 days (0 rows, last wrote 2026-06-17, ~10d stale).
  Consistent with 0 BUY signals. Confirm superseded by the play engine; resume writes or retire
  the table + readers. _(carried)_

## P3 — Tech Debt / Tooling (carried)
- **P3-1** This SKILL's probe lists still produce false 404/P0 noise every run: stale paths
  (`/api/market/spx-pulse`→`/api/market/spx/pulse`, `/api/flows`→`/api/market/flows`,
  `/api/nighthawk/latest-edition`→`/api/market/nighthawk/edition`, `/api/grid/news` doesn't exist),
  wrong env-var names (`UNUSUAL_WHALES_API_KEY`→`UW_API_KEY`, `VAPID_PUBLIC_KEY`→
  `NEXT_PUBLIC_VAPID_PUBLIC_KEY`), and a `pool\.on` regex that misses `livePool.on("error")`
  (`db.ts:113`). _(carried)_
- **P3-2** Debris in repo root: `__dbcheck.cjs` (untracked leftover from a prior audit run) plus
  legacy 0-row tables `spx_pulse_snapshots` / `spx_watch_setups` (no INSERT code refs). Clean up.
  _(carried; not committed by this audit — `git add` is scoped to the report + OPEN-ISSUES only)_

---

## ✅ Re-verified GREEN this run
- **TSC:** `npx tsc --noEmit` → 0 errors.
- **DB safety:** pg Pool idle-error handler present (`db.ts:113`); `max:5` (≤ Railway limit).
- **Redis safety:** `family: 0` IPv6 fix (`make-redis.ts:58`) + `reconnectOnError` + `maxRetriesPerRequest`.
- **Env (prod, names only):** all required present — `UW_API_KEY`, `POLYGON_API_KEY`,
  `POLYGON_API_BASE` (Massive), `CLERK_SECRET_KEY`, `CLERK_WEBHOOK_SECRET`, `CRON_SECRET`,
  `ANTHROPIC_API_KEY`, `DATABASE_URL`, `REDIS_URL`, `LAUNCHED_TOOLS`.
- **🆕 VAPID now SET in prod** (`NEXT_PUBLIC_VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY` +
  `VAPID_SUBJECT`) → push alerts are **no longer inert** (resolves the long-standing
  "alerts inert until VAPID" note).
- **#97** SpxDarkPoolCard imported + rendered (`SpxDashboard.tsx:13,105`).
- **#100** pg Pool error handler — fixed.
- **#101** Clerk `user.created` handled (`webhooks/clerk/route.ts:50`); singular
  `webhook/clerk` is a deliberate alias re-export — no duplication bug.
- **#102** Polygon WS Redis leader election (`ws/polygon-socket.ts:117-128`).
- **SPX option-chain veto** confirmed disabled in code + env.
- **Railway:** `blackout-web` Online 5/5; Postgres + Redis Online; all cron services present.
  The only `cron_job_runs` non-`ok` rows in the last 2 days are benign weekend
  `skipped` no-ops ("Outside market hours" / "Outside RTH cron window") — **no real cron failures.**
  (Dashboard showed `NightHawk-Morning-Confirm: Failed` and `Positions-Expiry-Cron: Offline`,
  but the DB run-log has no corresponding failure — consistent with known SDLC-redeploy
  dashboard churn; re-check Monday.)

## Phase Results
- **Live Site:** 0 real issues (public surfaces 200/fast; data endpoints correctly 401-gated).
- **Code Audit:** 0 new issues (tsc clean; db/redis/veto/auth all intact).
- **Cross-Tool Consistency:** not testable anonymously (auth-gated) + market closed; deferred to Monday.
- **Infrastructure:** 0 real issues (all services Online; no genuine cron failures).
- **Backlog (#73/#97/#100/#101/#102):** all confirmed fixed.
