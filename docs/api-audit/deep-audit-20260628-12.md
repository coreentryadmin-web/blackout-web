# BlackOut Deep Platform Audit
**Run time:** 2026-06-28 12:23 ET (Sunday — market closed; RTH-only checks deferred to Mon 2026-06-29)
**Overall Risk:** HIGH (1 standing P1 infra gap; no P0; no new breakage)
**Issues found this run:** 0 P0 | 1 P1 (carried) | 2 P2 (carried, market-closed deferred) | 1 P3-meta

## Executive summary
Platform is **GREEN on everything sampleable today.** All public surfaces 200, all paid/admin
endpoints correctly 401-gated, `tsc` clean (0 errors), zero errors in live deploy logs,
blackout-web 5/5 replicas Online, Postgres + Redis Online, all 13 cron services Online/Completed.
No net-new issues. The open items are all **carried from prior runs** — one real infra gap (P1-A)
plus two market-closed-deferred behaviors (P2-C, P2-D). The P1-B paid-signal leak fixed at 08:13
is **re-verified still closed.**

## P0 — Fix Immediately
None.

## P1 — Fix Today (Feature Broken or Degraded)
- **P1-A (STILL OPEN, re-confirmed):** No `Market-Regime-Detector` service among the 13 live cron
  services (`railway status`, this run). The `.toml` + writer code exist but the Railway service
  was never created → `market_regime` / `flow_anomalies` writers never run, so any consumer of
  regime/anomaly data reads stale/empty. **Needs the manual Railway "add service (Config-as-code)"
  step** — deploy-risky infra, not auto-created by the audit. Left for operator.

## P2 — Deferred to Monday RTH (not sampleable market-closed)
- **P2-D (STILL OPEN, re-confirmed live):** `options-socket` shard 0 stuck in a `code=1006`
  reconnect loop. Live logs cycle every 60s: `shard 0 connected (2 contracts)` →
  `reconnect in 60000ms (code=1006, failures=77)`. The `failures` counter dropped 531→77 since
  04:08 (a redeploy reset it), but the **loop pattern is unchanged** — server-initiated 1006
  closes, not the stall watchdog. Benign off-hours (market closed, `MAX_CONNECTIONS=1` slot just
  churning + log noise) BUT masks a potential RTH failure. **ACTION: re-check after 09:30 ET Mon**
  — if `failures` resets toward 0 once quotes flow it's cosmetic off-hours churn; if it climbs,
  Night's Watch live valuations degrade → promote to P1. Suggested fix: gate
  `scheduleReconnect`/heartbeat on options-RTH like the stall watchdog already is
  (`src/lib/ws/options-socket.ts:453-457`).
- **P2-C (carried):** SPX play opens + signal panel population — engine approves setups but
  open-path needs live RTH sampling to confirm `spx_play_outcomes` populates. Re-verify Mon RTH.

## P3 — Audit Tooling Quality (meta)
- **P3-META (re-confirmed, already logged 08:13):** the audit SKILL.md's own PowerShell checks
  throw **systematic false positives** and should be corrected so future runs don't cry wolf:
  - Stale endpoint paths: `spx-pulse` → `market/spx/pulse`; `flows` → `market/flows`;
    `nighthawk/latest-edition` → `market/nighthawk/edition`; `grid/news` → `market/news`
    (grid panels are analysts/catalysts/congress/dark-pool/earnings/economy/movers/sectors).
  - Regex misses real names: pg `livePool.on('error')` (handler IS present, `db.ts:113`);
    Clerk `user.created` lives in the **plural** `webhooks/clerk/route.ts:50` (audit checks the
    singular alias `webhook/clerk` which is just a re-export); `Select-String -Recurse` is not a
    valid param (the dead-code/import scans silently returned 0).

## Verified GREEN this run
- **Live site:** Landing 200 (cold-start 7s → warms to ~1.4s); Health 200; all of
  spx/pulse, flows, nighthawk/edition, news, gex-positioning, spx/desk correctly **401** unauth;
  admin/grid/congress/economy correctly **401**.
- **P1-B re-verified closed:** `GET /api/signals/open` → **401** (was 200 leak pre-08:13).
- **Code:** `db.ts` pool error handler present + live `max:5` (probe pool `max:1`) +
  statement/query timeouts; `make-redis.ts` `family:0` + reconnect present; SPX engine veto guard
  AND open logic both present; **0 real TODO/FIXME** (only `wall_broken` identifiers);
  **`tsc --noEmit` = 0 errors, exit 0.**
- **Backlog re-verified RESOLVED (audit-script flagged them as false positives):**
  #73 Largo grounding (`get_spx_structure`/`get_gex`/`get_spx_confluence` in `largo/tool-defs.ts`);
  #97 `SpxDarkPoolCard` imported + mounted (`SpxDashboard.tsx:13,105`);
  #100 db Pool error handler (`db.ts:113`); #101 Clerk `user.created` (`webhooks/clerk:50`);
  #102 Polygon WS replica guard present.
- **Infra:** blackout-web Online 5/5 (iad×3 + US-West×2); Postgres + Redis Online; 13 cron
  services all Online/Completed with valid weekday-RTH schedules; **0 error lines** in deploy logs.

## Phase results
- Live Site: 0 real issues (5 script-path 404s were stale-path false positives, not breakage)
- Code Audit: 0 issues (tsc clean)
- Cross-Tool Consistency: not sampleable (all endpoints 401 unauth + market closed)
- Infrastructure: 0 issues (1 missing service = P1-A above)
- Backlog: all script-checked tasks resolved; 1 real infra gap (P1-A) + 2 deferred remain
