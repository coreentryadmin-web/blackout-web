# BlackOut Deep Platform Audit
**Run time:** 2026-06-29 04:xx ET (Mon, pre-RTH — markets closed, UTC 11, RTH = UTC 13–21)
**Overall Risk:** HIGH
**Issues found:** 0 *new* P0 | 3 carried P1 + 1 deploy-gap P1 | 1 carried P2

> **Context:** Markets closed, all live-data endpoints are 401-gated to unauthenticated probes, so
> data-freshness and cross-tool consistency are **not sampleable** this run. Value came from code
> read, Railway infra, env audit, and git deploy-state. This run **corroborates the 00:14 ET run**
> and adds one sharpened finding (the unpushed-fix deploy gap).

---

## TOP-LINE: the platform CODE/RUNTIME is GREEN. The open risk is OPERATIONAL (deploy + provisioning).

The audited code is healthy and every previously-tracked code task is resolved & re-verified.
What is *not* resolved are three operator-only items that no code change can fix from inside this repo.

---

## P0 — Fix Immediately
**None.** blackout-web is Online (5/5 replicas), landing/auth/health all 200, all data endpoints
correctly auth-gated (no public data leak), tsc = 0 errors.

## P1 — Fix Today (operator action required)

- **[DEPLOY GAP] 11 commits committed locally but UNPUSHED → real fixes are NOT live in prod.**
  `git rev-list origin/main..HEAD = 11`. Includes:
  - `ffbed27 fix(admin): signal-analytics 500 — gates_blocked column does not exist` — **the admin
    `/api/admin/signal-analytics` 500 is STILL live in production** until this is pushed. Code is
    correct locally (`signal-analytics/route.ts:197,199,200,263` now use `gates_blocked_json`).
  - `bcaa3cf fix(audit): sweep fail-open cron write guards to fail-closed`
  - `64a15ea fix(audit): force-dynamic on engine/health`
  Per the cron-no-push policy this is **by design** (crons commit local, user reviews & pushes), but
  the practical effect is that ~4 days of fixes are queued undeployed. **Action: operator review +
  `git push origin main`.**

- **`DISCORD_OPS_WEBHOOK_URL` unset in Railway → ops blind to cron-failure alerts.**
  Confirmed MISSING (only `GEX_ALERTS_PUSH` related var present). Cron-Staleness-Watchdog emits
  "ALERT NOT DELIVERED" — nobody is paged when a cron goes stale. **Action: operator set the webhook.**

- **`Market-Regime-Detector` cron service not provisioned in Railway.**
  Confirmed ABSENT from service list (present: Cron-Staleness-Watchdog, DB_CLEANUP, blackout-web,
  UW-Cache-Refresh-New, Night's Watch-Warm-New). The `market_regime` / `flow_anomalies` writers
  (`/api/market/regime` POST) are never invoked → those tables go unwritten. **Action: operator adds
  the Railway service/cron trigger.**

## P2 — Fix This Week

- **Grid overpromise — UI promises panels that don't exist.**
  `src/app/(site)/grid/page.tsx:35` subtitle and `:13` metadata both advertise
  *"News, flow, analyst actions, and market pulse"* but Grid only serves
  `analysts / catalysts / congress / dark-pool / earnings / economy / movers / sectors`. There is
  **no News or Flow panel.** Either build them or soften the copy. (This is also the root of the
  audit script's `/api/grid/news` 404.)

## Pending — verify at today's 09:30 ET RTH open (not sampleable now)
- **P2-C SPX plays opening:** engine veto is now *conditional* (`spx-play-engine.ts:811`
  `optionTicket.blocked && playOptionChainRequired()` only blocks when chain is required AND blocked),
  and `openPlay()` is reachable at `:900`. Logic looks fixed — confirm `spx_play_outcomes` actually
  populates during live RTH.
- **P2-D options-socket 1006 reconnect loop** — re-check at RTH.
- **Cross-tool GEX/pulse wall consistency** — auth-gated + market closed; carry to RTH.

---

## RESOLVED / RE-VERIFIED GREEN this run
- **Task #73 (Largo grounding):** WIRED — `src/lib/largo/run-tool.ts:4` imports `computeSpxConfluence`
  + `getLargoSpxLiveDesk`; tools `get_spx_structure/get_spx_play/get_gex/get_greek_flow` defined.
  (Audit's `src/lib/tools/` path is stale — real location is `src/lib/largo/`.)
- **Task #97 (SpxDarkPoolCard):** RESOLVED — imported `SpxDashboard.tsx:13`, rendered `:104`.
- **Task #100 (db Pool error handler):** PRESENT — `db.ts:113` `livePool.on("error", …)`; `max:5`.
- **Task #101 (Clerk user.created):** HANDLED — `webhooks/clerk/route.ts:50`; singular
  `webhook/clerk` is a thin alias re-exporting the canonical handler (no duplication).
- **Task #102 (Polygon WS replica guard):** PRESENT — Redis leader-election lock
  `polygon-socket.ts:117` (`polygon:indices:leader`, 25s TTL).
- **P2 regime fail-open → now fails CLOSED:** `market/regime/route.ts:47` `if (!isCronAuthorized(req)) 401`.
- **Redis:** `make-redis.ts:58` `family:0`, `:63` `reconnectOnError`, `maxRetriesPerRequest` present.
- **tsc:** 0 errors. **TODO/FIXME:** 0 real (only `WALL_BROKEN_TICKERS` domain term).
- **Tool gating:** all routes under terminal/positions/grid require auth.
- **Secrets present:** `UW_API_KEY`, `POLYGON_API_KEY`, `CLERK_SECRET_KEY`, `ANTHROPIC_API_KEY`,
  `CRON_SECRET`, `CLERK_WEBHOOK_SECRET`, `DATABASE_URL`, `REDIS_URL` all SET (55 vars total).
  `UW_API_BASE` / `MASSIVE_API_KEY` unset but **optional/by-design** (code defaults `UW_API_BASE`;
  GEX uses `POLYGON_API_KEY` as the Massive key).

---

## P3-META — the audit SKILL.md is still emitting systematic FALSE POSITIVES (recurring; please fix the script)
Every 404 and the lone env "MISSING" this run were the *audit script's own stale assumptions*, not
platform bugs. The SKILL must be corrected so future runs are trustworthy:
- **Endpoint paths (all 404 because they don't exist):**
  - `/api/market/spx-pulse` → real: `/api/market/spx/desk` (or `/spx/merged`)
  - `/api/flows` → real: `/api/market/flows`
  - `/api/nighthawk/latest-edition` → real: `/api/market/nighthawk/edition` (+ `/api/nighthawk/play-status`)
  - `/api/grid/news`, `/api/grid/flows`, `/api/grid/analyst` → don't exist; real grid set is
    `analysts/catalysts/congress/dark-pool/earnings/economy/movers/sectors`
- **Env name:** script checks `UNUSUAL_WHALES_API_KEY` (false "P0 MISSING") — code uses **`UW_API_KEY`** (SET).
- **Code path:** script reads `src/lib/tools/` (empty) — Largo tools live in `src/lib/largo/`.
- The Phase-1 freshness/consistency PowerShell silently `ConvertFrom-Json`s 401 bodies → garbage; it
  should branch on status before parsing.

## Phase Results
- **Live Site:** 0 real issues (4× 404 = stale audit paths; 8× 401 = correct auth-gating)
- **Code Audit:** 0 issues (tsc=0, all guards present)
- **Cross-Tool Consistency:** not sampleable (auth-gated + market closed)
- **Infrastructure:** 2 operator gaps (DISCORD webhook, Market-Regime-Detector cron) + 1 deploy gap (11 unpushed commits)
- **Open Task Backlog:** #73/#97/#100/#101/#102 all RESOLVED & re-verified
