# 0DTE "Night Hawk" DEEP-DIVE AUDIT — 2026-07-25 (Saturday, market closed / after-hours)

Read-only audit. No production or member-board writes. The live board is **idle** (off-hours),
so every market-dependent stage is proven by **replaying REAL recent session data offline against
real Polygon + Unusual Whales**, plus the shipped unit logic, plus the served authenticated UI —
never by assertion. Each engine below was exercised end-to-end (produce → grade) on real bars.

Environment: repo `/home/user/blackout-web`, branch `claude/wall-beads-data-validation-4re5wo`.
Present: `CLERK_SECRET_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `POLYGON_API_KEY`,
`POLYGON_API_BASE`. **AWS creds absent/expired → the INFRA/ECS config stage cannot run
(reported as "needs AWS creds", not guessed).** Headless browser is blocked → **no pixels /
clicks / rendered layout** (served HTML only). Member board read WITHOUT `?sim=1` (sim feed left
untouched).

---

## VERDICT TABLE

| System | Verdict | One-line evidence | What turns AMBER → GREEN |
|---|---|---|---|
| **FLOW (①/② accumulation)** | 🟢 GREEN | Real 2026-07-24 replay: 3183 UW alerts → 229 directional → 10 graded plays, 4 doubled / 5 stopped / 1 time-stop, WR 40% on real minute bars | — |
| **BREAKOUT (whole-market)** | 🟢 GREEN | Production `screenBreakoutMovers` ran live: 12,410 stocks → real ranked names; recall probe graded 40 movers on real bars | — (engine proven; note: EV was negative on 07-24's low-momentum tape, a market outcome, not a break) |
| **PIN (wall fade)** | 🟡 AMBER | Origin fully wired (`DiscoveryOrigin` incl. PIN, `evaluatePinRegime`/`pinScore`), `pin-source.test.ts` 7/7 pass — but intraday GEX snapshots are a server-side UW product NOT reachable offline | Observe live at Monday's open (or feed a `--snapshots` intraday export) |
| **IRON CONDOR** | 🟢 GREEN | Shipped `selectIronCondor(target=80)` graded 45 real sessions: **97.8% WR**, 6.7% breach; WR-by-width table monotonic on real bars; 17+34 unit tests pass | — (negative-skew caveat documented, not a defect) |
| **COMMIT / GATES** | 🟡 AMBER | Gate spine logic proven: `gates` 72/72, `governor` 32/32, `cortex-gate` 20/20 pass, and the FLOW replay exercised the full gate→plan path — but 0 committed plays off-hours, so a LIVE commit landing in the ledger is unverified | Observe a commit land in the ledger at Monday's open |
| **LIVE MARKS / P&L** | 🟡 AMBER | `/marks` API well-formed (`idle:true`, `cap`, `marks:[]`); `marks-math`/`live-marks` logic shipped — but no OPEN play to mark right now | Observe live marks on an open play Monday's open |
| **EXITS (trim / scale)** | 🟢 GREEN | Exit engine graded on real 07-24 bars: ratchet 4.1% vs trim-scale 11.7% vs hold 22.8%; MFE green-exit availability (+25% 80%, +50% 70%); banger mechanical scale-out graded; `exit-engine.test.ts` 47/47 | — |
| **GRADING / RECORD** | 🟢 GREEN | Healthcheck stage G GREEN: record arithmetic 35W+65L+0BE=100 = 100 graded, WR 35% / 16 sessions; FLOW + condor + banger all graded on real bars | — |
| **DATA CORRECTNESS** | 🟢 GREEN | data-validator 13 PASS / 0 FAIL: SPY/SPX/VIX exact vs Polygon (Δ0.000%), GEX/greek signs consistent, 0 malformed floats | — |
| **PANELS / UI (served HTML)** | 🟢 GREEN | Authenticated `/nighthawk` 200 (39,610 B), all 10 panel markers present; board/marks/record/calibration APIs return well-formed JSON of the right shape | Pixels/clicks/live tab-switching CANNOT be verified from this env — needs a browser/device |
| INFRA / ECS config | ⚪ N/A | Needs AWS creds (absent) — stage A SKIPPED, not guessed | Re-run with valid `AWS_ACCESS_KEY_ID`/`SECRET` |

**Legend:** 🟢 proven working on real data · 🟡 wired + logic-tested but needs market-open (or a data
export) to prove LIVE · ⚪ cannot verify from this environment.

---

## PER-SYSTEM EVIDENCE (captured real numbers)

### 1. FLOW / accumulation — `npm run sim:0dte -- --grade=2026-07-24`  🟢
Runs the REAL pipeline (`flowAccumulationByTicker` → magnet pick → `filterPlaysByMaxDte` →
`gradePlanFromBars`) against REAL data.

```
[1] UW multi-day flow: 3183 directional alerts · 3 days [07-22,07-23,07-24] · 280 tickers
[2] Accumulation:      280 tickers → 229 directional (48 neutral/mixed dropped)
    Top magnets: IWM bull(87), QQQ bear(65), NVDA bull(65), IREN bear, SNDK bull($12.98M),
                 MU bear, GOOGL bull, SPCX bull, META bull, EWY bear ...
[3] BACKTEST GRADE (stop -50% · target +100% · time-stop 15:30 ET) on REAL minute bars:
    QQQ 690P doubled +100 · IREN 39P doubled +100 · MU 950P doubled +100 · GOOGL 320C doubled +100
    IWM 295C stopped -50 · NVDA 207.5C stopped -50 · SNDK 1535C stopped -50 · SPCX/META stopped
    EWY 170P time_stop +77.69
  → 10 gradeable · 4 doubled · 5 stopped · 1 time-stop · WR 40.0% · avg P/L 22.8% · 0 no-data
  RATCHET (shipped default):   win 50.0% · avg 4.1%
  TRIM-SCALE (E5, default-off): win 60.0% · avg 11.7%
  GREEN-EXIT MFE avail: +10% 90% · +25% 80% · +50% 70% · +100% 50% · never-green 10%
```
**Proves:** the FLOW/accumulation engine discovers, builds, filters, and grades real plays on
real bars end-to-end. GREEN.

### 2. BREAKOUT whole-market  🟢
`npm run scan:bangers -- --grade=2026-07-24` — **12,410 stocks screened** → 10 ranked candidates
(SOXS, CHTR, PATH, RNG, TEAM, MBLY, VZ, CLF, IP, FRMI). Graded on real bars: mean maxRet 1.36x,
hold-to-expiry 1.00x, REALIZED scale-out 0.76x → **EV −24% on this specific low-momentum session**
(touched ≥2x 10%, ≥3x 0%). The tool produces + grades correctly; the negative EV is the real
07-24 tape, not an engine fault.

`discovery-recall-probe.mjs --grade=2026-07-24` — production `screenBreakoutMovers` ranking ran
and returned real names: **40 qualifying movers**, production keeps top-6 by $-vol.
- KEPT (rank 1–6):   n=6  WR **33.3%**  avg maxRet 0.6%
- DROPPED (rank 7–60): n=34  WR **50.0%**  avg maxRet 1.2%  → 17 dropped favorable-first winners
  the cap never saw (IP, RKLZ, CSQR, NAIL, IREZ, FAF, SLM, REXR, NBIZ…).

**Proves:** BREAKOUT discovery produces real ranked names and grades. GREEN. (The leaky
$-volume cap is a **known INTENTIONAL-DESIGN item**, evidence-for-a-decision, not a break — see
FINDINGS 2026-07-25.)

### 3. PIN wall  🟡
`wall-temporal-stability.mjs` → **INSUFFICIENT DATA** (clean, honest): no `--snapshots` export;
historical intraday GEX snapshots are a **server-side UW product not reachable from this sandbox**.
Code path IS wired and unit-proven:
- `src/lib/zerodte/board.ts`: `DiscoveryOrigin = "FLOW" | "BREAKOUT" | "PIN"`, canonical order + origin maps.
- `src/lib/zerodte/pin-source.ts`: `evaluatePinRegime()` / `pinScore()` build PIN directional-fade
  candidates; gated OFF by default behind `ZERODTE_SRC_PIN === "1"`.
- `pin-discovery.ts` calls both; `pin-source.test.ts` **7/7 pass**.

**Verdict AMBER:** logic wired + unit-tested, but live production (real intraday GEX walls) needs
market-open. → GREEN by observing it live Monday, or feeding a `--snapshots` intraday poll export.

### 4. IRON CONDOR — `npm run wr:condor -- --tickers=SPY,QQQ,IWM --days=15`  🟢
SPY,QQQ,IWM × 15 sessions (2026-07-06 → 07-24), WIN = close inside both shorts, real minute bars:

```
short width   n    WIN%
±0.40%       45   64.4
±0.60%       45   77.8
±0.80%       45   91.1
±1.00%       45   95.6
±1.50%       45  100.0
SHIPPED selectIronCondor(target=80, wing=0.005): graded 45 · WIN RATE 97.8% · intraday breach 6.7%
```
Monotonic WR-by-width (geometry sane), shipped geometry grades 97.8% with an honest 6.7% breach
tail. `iron-condor.test.ts` 17/17, `condor.test.ts` 34/34 pass. **Proves:** the condor engine
selects valid 4-leg geometry and the WR/EV claim holds on real bars. GREEN. (Negative-skew
warning is documented in `iron-condor.ts` — WR is real, profitability needs credit + breach stop
+ small size.)

### 5. DATA CORRECTNESS — `node scripts/audit/data-validator.mjs`  🟢
**13 PASS / 0 FAIL / 3 INFO** (temp user minted + deleted, DELETE 404 verified):
- SPY app 738.93 vs Polygon 738.93 Δ **0.000%**; gex.spot match; quote==gex cross-check.
- SPX app 7411.98 Δ 0.000%; VIX 18.58 Δ 0.000%; SPX/SPY ratio **10.031** (~10 ✓).
- Wall ordering put_wall 740 < call_wall 760, max_pain 741, flip null (ordered ✓).
- dex posture=long (net_dex +33.35B), vanna=positive (net_vex +210B), net_gex sign matches UW.
- Malformed scan: **0 suspect floats** across 3 payloads.
- (INFO: `/api/market/zerodte/board` returned empty on that specific validator call — legitimate
  idle/off-hours board; re-fetched fine in stage 6 below.)

### 6. PANELS / SERVED UI (headless HTTP — NO pixels/clicks)  🟢
Authenticated ONCE as a temp admin+premium Clerk user (FAPI ticket → `__session`), fetched the
MEMBER `/nighthawk` desk (no `?sim`), then deleted the user.
- `GET /nighthawk` → **HTTP 200, 39,610 bytes.** All 10 markers PASS: title "Night Hawk",
  overnight/playbook copy, `nighthawk-page-shell-native` class, NOT ComingSoon, NOT signed-out,
  radar backdrop, record/track strip, pre-market/session label, deck/board container, RSC payload.
- APIs (member board) — well-formed JSON, correct shape:
  - `board`: `available, as_of, upstream_ok, session, setups, ledger, covered_elsewhere, governor, allocation`
  - `marks`: `available, as_of, session_date, idle, cap, marks`
  - `record`: `methodology, window, plays, total_flagged, graded, ungraded, wins, losses, breakeven, win_rate_pct, avg_pnl_pct, by_outcome`
  - `calibration`: `methodology, window, total_rows, graded_plays, gates, score_bands, score_floor, blocked_value, tier_record, accumulation_alignment, confluence_tiers, origin_bands`

**CANNOT be verified headlessly (needs browser/device):** button/tab clicks (open/watch/closed
segment switching), live SSE marks streaming, rendered pixels/layout, hover/modal interactions.
The served markup confirms the panels/components are PRESENT and the APIs feed them the right
shape; only the client interaction layer is unproven from here.

### 7. LIVE e2e healthcheck — `npm run healthcheck:0dte -- --json`  (overall AMBER, off-hours)
| Stage | Verdict | Note |
|---|---|---|
| A INFRA/CONFIG | ⚪ SKIPPED | AWS creds absent/placeholder |
| B DISCOVERY ×3 | 🟡 AMBER | 0 live FLOW/BREAKOUT/PIN setups, heat=CLOSED — **board producing GREEN (`upstream_ok=true`)** |
| C COMMIT/LEDGER | 🟡 AMBER | 0 committed plays (legit pre-open/no-commit) |
| D LIVE MARKS+P&L | 🟡 AMBER | live lane idle=true, 0 tracked contracts |
| E EXIT MGMT | 🟡 AMBER | 0 ledger rows — no lifecycle to assert this run |
| F IRON CONDOR | 🟡 AMBER | empty board — no live name to carry geometry |
| G GRADING/RECORD | 🟢 GREEN | 35W+65L+0BE=100 = 100 graded, WR 35% / 16 sessions |

All AMBERs here are the expected off-hours "idle board" state — the offline replays above prove
the same engines produce + grade correctly on real data.

---

## WHAT IS PROVEN vs WHAT NEEDS MONDAY vs WHAT NEEDS A BROWSER

**Proven working on REAL data (offline replay + shipped unit logic):**
- FLOW accumulation discovery + plan + grade (07-24: 10 plays, WR 40%, real bars).
- BREAKOUT whole-market discovery (`screenBreakoutMovers`, 12,410 stocks, real ranked names) + grade.
- IRON CONDOR geometry + WR/breach (97.8% / 6.7% shipped, 45 real sessions).
- EXIT engine trim/scale/ratchet grading on real bars (+ 47/47 tests).
- GRADING/RECORD arithmetic (100=100, WR 35%).
- DATA correctness (13/0 PASS, prices exact, greeks consistent, 0 malformed).
- Gate/governor/cortex spine logic (72/72, 32/32, 20/20).
- Served UI: `/nighthawk` desk HTML renders all panels authenticated; 4 APIs well-formed.

**Needs Monday's market-open to prove LIVE (wired + logic-tested, cannot exercise off-hours):**
- PIN wall live production (real intraday GEX walls — not reachable offline).
- A live COMMIT landing in the ledger (commit/persist path).
- LIVE marks / running P&L on an OPEN play.
- (All three: watch them at Monday's open, or feed the PIN probe a `--snapshots` export.)

**Needs a browser/device (cannot verify from this environment):**
- Rendered pixels, panel layout, button/tab clicks, live SSE mark streaming, modals/hover.

**Needs AWS creds:**
- INFRA/ECS config stage (A) — re-run with valid `AWS_ACCESS_KEY_ID`/`SECRET`.

---
*Generated 2026-07-25. Read-only; no production/member-board mutations. Temp Clerk users minted
for auth were deleted (verified 404). Commands: `sim:0dte`, `scan:bangers`,
`discovery-recall-probe.mjs`, `wall-temporal-stability.mjs`, `wr:condor`, `data-validator.mjs`,
`healthcheck:0dte`, plus a one-shot served-HTML panels probe.*
