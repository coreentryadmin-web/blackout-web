> **kind:** FINDING

## Ask Largo swing play-brief: final 3 files (play-brief.ts, play-brief-diff.ts, play-brief-ladder.ts) had the same toFixed-vs-roundFloats price-level mismatch — completes the 6-file sweep from #5380 — fix/swing-remaining-price-level-rounding — 2026-09-21

| **Status** | FIXED |
|---|---|

- **What was broken:** the fourth, fifth and sixth files in the disclosed follow-up sweep from PR #5380 (`play-brief-narrative.ts`), #5383 (`play-brief-narrative-coaching.ts`), and #5384 (`play-brief-intel.ts`) — the remaining `n.toFixed(2)` price-LEVEL call sites in `play-brief.ts` (evidence-line nearest-wall/γ-flip), `play-brief-diff.ts` (spot-drift, option-mark-shift, structural-level-shift, adverse-drift narration — the live "what changed since last refresh" diff engine), and `play-brief-ladder.ts` (cross-desk Vector-vs-GEX-matrix gamma-posture disagreement note). This completes all 6 files identified by #5380's own repo-wide grep.
- **A wrinkle found in `play-brief-diff.ts`**: that file already imports `roundFloats` and its own `briefContentKey` doc comment documents a PRIOR fix (2026-09-11) for exactly this bug class — but that earlier fix only covered the internal SSE-dedupe key (`briefContentKey`, never member-visible), not the actual member-visible diff narration functions (`narrateSpotShift`, `narrateMarkShift`, `narrateStructuralLevelShift`, `synthesizeThesisAndPrice`'s adverse-drift clause), which still called raw `.toFixed(2)` directly. This is exactly the gap the earlier fix's own reasoning should have closed but didn't — confirms the bug class recurs even within a file that already knows about it, when a fix addresses one call site of a pattern without a shared, reused helper for the rest.
- **A display-convention note, not a bug**: `play-brief-diff.ts`'s price lines are `$`-prefixed (e.g. `"Spot drifted higher — $152.04"`), unlike every other file in this sweep which renders spot/wall/flip bare (no `$`). This is a pre-existing convention difference in that file, left untouched here — the fix only corrects the ROUNDING of the number inside the existing `$${...}` template, not the prefix convention itself (out of scope for a rounding-consistency fix).
- **Evidence:** same floating-point mechanism as #5380/#5383/#5384's repros — `(152.035).toFixed(2) === "152.03"` while `Math.round(152.035*100)/100 === 152.04`. Reproduced through the real `diffBriefSnapshots` call path: a spot move from 100 to 152.035 renders `"Spot drifted higher — $152.04"` post-fix vs. would have rendered `$152.03` pre-fix.
- **Blast radius:** all remaining price-LEVEL sites in the 3 files are fixed. `play-brief.ts`'s greek deltas (Δ/Γ/θ/ν) and `play-brief-diff.ts`'s signed delta values (`d.toFixed(2)`, the +/- change between two snapshots) are deliberately left untouched — different units, not price levels, never duplicated as a raw price number in `envelope.levels`. This closes the entire 6-file sweep #5380 disclosed.
- **Fix rationale:** mechanical `n.toFixed(2)` → `fmtPriceLevel(n)` replacement, identical shape to the prior three PRs, reusing the already-shared helper (`play-brief-diff.ts` and `play-brief-ladder.ts` needed a fresh import; `play-brief.ts` already imported the sibling `fmtOptionUsd`). No logic changes; byte-identical output for any value not sitting on a rounding boundary (confirmed: existing suites for all three files pass unchanged).
- **Test:** `src/lib/swing/play-brief-diff.test.ts` — 1 new test (`diffBriefSnapshots`, spot `100 → 152.035`, asserts the spot-drift line shows `$152.04`, not `$152.03`) through the real call path. RED→GREEN proven via `git stash` on all three implementation files together: 1/155 failure pre-fix (exactly the new test), 155/155 post-fix. `npx tsc --noEmit` clean. Full `src/lib/swing/*.test.ts` suite: 1457/1457 — no collateral breakage. `play-brief.ts`'s and `play-brief-ladder.ts`'s own fixes are covered by tsc + their existing (unchanged, still-passing) test suites rather than a dedicated new test each — same mechanical, behavior-preserving-except-at-rounding-boundaries shape as the already-proven `play-brief-diff.ts` fix, and a full ctx/play fixture for `play-brief-ladder.ts`'s `buildStructureLadder` was disproportionate for one line in a 6-file sweep already carrying four independent regression tests across the series.
