## 2026-08-31 — [FINDING, P4 audit-hygiene] The homepage prev-bar `no-store` finding (2026-08-30, line ~831) is marked OPEN but was fixed the same day two entries later — CORRECTED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this corrects** | `docs/audit/FINDINGS.md`'s 2026-08-30 entry **"Homepage's `revalidate = 3600` is intermittently defeated by a nested `no-store` fetch — OPEN, write-up only"** (heading + its own `**Status**` row both say OPEN). Two entries later in the same file, same date, sits **"Homepage `revalidate = 3600` defeated by nested `no-store` prev-bar fetch — FIXED"** — same root cause (`fetchSpotFromPrevBar` → `fetchPreviousDayBar` → `polygon-largo.ts`'s `polygonGet` hardcoding `cache: "no-store"`), same fix shape the OPEN entry's own "two options" field proposed (thread an optional cache override so the last-resort prev-bar lookup doesn't defeat the page's ISR). Not to be confused with the OTHER, still-genuinely-open sibling entry immediately above it in the file ("#3187's homepage ISR fix was necessarily narrow — two more no-store call sites... OPEN") — that one covers TWO DIFFERENT call sites (`fetchIndexSnapshot`'s primary path, and a UW spot-exposures call) that this fix does not touch, and its Polygon half is still genuinely unresolved (its UW half has already been separately retracted and re-traced correctly in this session's PRs #3236 and #3243) — this correction is narrowly about the prev-bar entry only. |
| **Confirmed live in the current codebase this pass** | `src/lib/providers/polygon-options-gex.ts:1229` — `fetchSpotFromPrevBar` now calls `fetchPreviousDayBar(symbol, { next: { revalidate: 3600 } })` (with an in-code comment explaining why: aligning the last-resort spot fallback with the homepage's own ISR window). `src/lib/providers/polygon-largo.ts:45-56` — `polygonGet`'s `buildPolygonLargoFetchInit` accepts an optional `fetchInit`; when `fetchInit.next` is set it uses that instead of the default `cache: "no-store"` (with an explicit comment noting `cache` and `next.revalidate` are mutually exclusive in Next's fetch patch — `cache: "no-store"` wins if both are present, so the override must omit `cache` entirely, which it does). All other `polygonGet` callers keep the default `no-store` behavior — the fix is correctly scoped to this one call site, exactly as the FIXED entry claims. |
| **Why this is a hygiene correction, not a new investigation** | Same shape as the two other same-day self-corrections already in this file (SPX Slayer Largo coverage; the session-anchor `nowMs` and Meridian `earnings_week_analytics` findings this session already corrected in PRs #3249/#3251): the fix and its evidence already exist in `FINDINGS.md` itself, a couple headings below — this doesn't add new evidence, it points the OPEN entry at the FIXED one that already supersedes it, re-verified against live `main` rather than trusted on the FIXED entry's word alone. |
| **Action taken** | None to the product — already fixed and confirmed live. This entry exists so the next `findings-fold-staging.mjs` pass carries the corrected status forward and nobody re-threads a cache override that's already there. |
| **Status** | CORRECTED — the 2026-08-30 homepage prev-bar `no-store` finding is FIXED (superseded same-day by the entry two headings below it; re-verified live against current `main` in this pass). The sibling "two more no-store call sites" entry immediately above it remains genuinely OPEN (Polygon half) / already separately corrected (UW half, via PRs #3236 and #3243) and is untouched by this correction. |
