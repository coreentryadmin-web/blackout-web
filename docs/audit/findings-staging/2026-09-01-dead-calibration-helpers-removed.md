## 2026-09-01 — [cleanup, 0DTE calibration] dead calibration helper exports removed (rail-priors, rail-graduation, Beta-binomial stats layer) — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Context** | PR 4 of 6 from the dedicated dead-code cleanup audit. |
| **Root cause** | Four independent pieces of dead code, each with a confirmed live sibling doing the real work instead: (1) `calibrationRailPriorsEnabled()` (`calibration-rail-priors.ts`) — the real live consumer, `calibrationPriorBlendFactor()`, calls the private `priorsModeFromEnvLocal()` directly rather than this exported wrapper. (2) `computeShadowRailPriorsFromRows()` — a "convenience for tests" wrapper the test file's own import block never pulled in; the real shadow-priors refresh path calls `computeShadowRailPriorsFromOriginBands()` directly with data from `buildZeroDteCalibrationReport()`, not raw rows. (3) `primaryRailFromOriginLabel()` (`calibration-rail-graduation.ts`) — zero test coverage at all, not even test-only use; the live bucketing logic (`bucketOriginRegime`) calls `readOriginLabel` directly instead. (4) `betaBinomialPosterior`/`JEFFREYS_PRIOR`/`BetaPrior`/`BetaPosterior` (`calibration-stats.ts`) — the module's own doc comment calls this layer "SUPPLEMENTARY... the graduation ladder gates on the Wilson lower bound, never on this posterior"; `calibration.ts` (the one real consumer of this file) imports only `wilsonInterval`/`proportionDiffCI`, confirmed by reading its import line directly. |
| **Evidence** | Whole-repo grep for every symbol name (re-run immediately before deleting, not just trusted from the audit): each hit only inside its own defining file or its own test. Independently confirmed each claimed live sibling by reading it directly: `calibrationPriorBlendFactor` → `priorsModeFromEnvLocal()`; `bucketOriginRegime` → `readOriginLabel` (imported); `calibration.ts:32` → `import { wilsonInterval, proportionDiffCI } from "./calibration-stats"` (no Beta-binomial names). |
| **Fix** | Removed all four. In `calibration-rail-priors.ts`, removing the dead `computeShadowRailPriorsFromRows()` wrapper also left its `analyzeOriginBands` import unused (its only other use was inside the deleted function) — removed that import too; `buildZeroDteCalibrationReport` (the file's other import from `./calibration`) stays, still used by the real refresh path. Updated `calibration-stats.test.ts` to drop the `betaBinomialPosterior`/`JEFFREYS_PRIOR` import and their one dedicated test; the `wilsonInterval`/`proportionDiffCI` tests are untouched. |
| **What was deliberately NOT changed** | `computeShadowRailPriorsFromOriginBands()` and `winRateDeltaToRailWeight()` (`calibration-rail-priors.ts`) — the real shadow-priors blend path, confirmed deployed (`ZERODTE_CALIBRATION_RAIL_PRIORS=shadow`) and load-bearing for `market-state-engine.ts`'s `rail_weights`, untouched. `wilsonInterval`/`proportionDiffCI` (`calibration-stats.ts`) — live, imported by `calibration.ts`, untouched. `recommendRailGraduation` and the rest of `calibration-rail-graduation.ts` — untouched. |
| **Blast radius checked** | Grepped every removed symbol across the whole repo — zero surviving references. Confirmed `analyzeOriginBands` (still exported from `calibration.ts` itself) has other real callers elsewhere in the codebase — only this file's now-dead import of it was removed, not the function itself. |
| **Regression guard** | `calibration-rail-priors.test.ts` + `calibration-rail-graduation.test.ts` + `calibration-stats.test.ts` + `calibration.test.ts`: 60/60 pass. Full `src/lib/zerodte/*.test.ts`: 1211 pass / 0 fail / 1 pre-existing skip (Node 20). `npx tsc --noEmit` clean. |
| **Status** | FIXED — PR pending (4 of 6 from the dead-code cleanup plan). |
