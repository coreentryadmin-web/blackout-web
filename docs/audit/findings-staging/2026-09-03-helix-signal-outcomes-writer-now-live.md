## 2026-09-03 — [FINDING, P4 stale-docs, Helix] `helix-signal-outcomes` — CLAUDE.md's `helix-score-signal.mjs` note claimed the ledger "has no writer" (dated 2026-08-23) — the writer now exists and is firing successfully — corrected

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Standing "re-verify old FINDINGS.md/CLAUDE.md claims" sweep. CLAUDE.md's audit-toolkit entry for `scripts/audit/helix-score-signal.mjs` states the signal ledger the script's own design map (HELIX-MAP §9.7) named as the right test instrument "has no writer": `helix-signal-outcomes` was "fully registered in `cron-registry.ts` yet absent from blackout-infra's deployed `cron-jobs.json` (verified 2026-08-23)". Checked whether this ~11-day-old claim still holds. |
| **Investigation** | `grep helix-signal-outcomes src/lib/cron-registry.ts` confirmed it is still registered there. Checked the DEPLOYED side directly via `boto3` (this sandbox has live AWS creds this session): `events.list_rules` found `blackout-production-helix-signal-outcomes`, state `ENABLED`, schedule `cron(*/15 11-21 ? * MON-FRI *)` (every 15 min, RTH) — the deployment gap CLAUDE.md documented no longer exists. `events.list_targets_by_rule` confirmed it correctly targets the `hit-cron` Lambda with `{"key":"helix-signal-outcomes","path":"/api/cron/helix-signal-outcomes"}`, the same wiring every other working cron uses. Live CloudWatch Logs on `/aws/lambda/blackout-production-hit-cron` (exact-phrase filter `"helix-signal-outcomes"` — an unquoted filter on this log group matched unrelated `helix-discord-digest` lines instead, the same CloudWatch filter-pattern trap this session already hit once today; the quoted exact-phrase re-run was the one that actually proved this) found **9/9 successful `[hit-cron] /api/cron/helix-signal-outcomes -> 200` invocations, 17:30–19:31 UTC**, correctly stopping once RTH closed (20:00 UTC) rather than continuing to fire — the schedule and the route are both live and working, not just registered. |
| **Why this matters** | The script's own doc note used the "no writer" fact to justify why it measures each print's underlying forward on Polygon minute bars as a PROXY, rather than grading against the real signal ledger the map identified as the correct instrument. That proxy methodology is still valid and its 2026-08 findings (`SPREAD WITHOUT ORDER` at every horizon) are unaffected — but the stated REASON for using a proxy is now factually stale, and a re-run against the now-live ledger (the instrument the script's own design doc calls correct) is a natural, not-yet-done follow-up. |
| **Fix** | Corrected the CLAUDE.md bullet with a dated "UPDATE (2026-09-03, verified live)" note (matching the pattern already used in this file / `docs/HEATMAP_DATA_CONTRACT.md` for prior stale-claim corrections this session) stating the writer now exists, citing the EventBridge rule + Lambda log evidence, and flagging the ledger re-run as the natural follow-up — without touching the script's own methodology or its 2026-08 findings, which remain correct as reported. |
| **Blast radius** | One doc file (`CLAUDE.md`), one bullet. No code changed. |
| **Gates** | Doc-only change — no test/build gates apply. |
