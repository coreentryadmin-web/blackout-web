## 2026-09-01 — [cleanup, 0DTE governor] dead governor measure-only display fields removed (concentration measure + would-block budget reasons) — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Context** | PR 3 of 6 from the dedicated dead-code cleanup audit. |
| **Root cause** | Five fields on `ZeroDteGovernorSummary` (`correlated_concentration`, `max_correlated_same_dir`, `would_block_concentration`, `would_block_premium_budget`, `would_block_gamma_budget`) plus the function that solely fed the concentration trio, `maxCorrelatedSameDirection()`, were computed every board build and read by nothing — no UI component, no Largo tool, no admin route. Independently confirmed by reading `ZeroDteBoard.tsx`'s full `GovernorStrip` render body (~120 lines) end to end: it reads `premium_at_risk`/`max_premium_at_risk`/`short_gamma_open`/`max_short_gamma_open` (the raw numbers — kept) but never touches any of the 5 removed fields. `maxCorrelatedSameDirection`'s own doc comment claimed it was "used both by the board measure and by the per-candidate evaluator, so the 'what counts as concentration' logic lives in ONE place" — this was stale/wrong: `concentrationReasonForCandidate` (the REAL, enforced gate, `GOVERNOR_ENFORCE_CONCENTRATION` default `true`) has its own independent inline reimplementation of the same "largest same-direction correlated cluster" logic rather than calling this function — confirmed by reading it directly. `maxCorrelatedSameDirection` had exactly one caller anywhere: `summarizeGovernorForBoard`, feeding only the dead display fields. |
| **Evidence** | Grepped all 5 field names + the function name across the whole repo before touching anything — zero hits outside `governor.ts` and `governor.test.ts`. Read `ZeroDteBoard.tsx`'s `GovernorStrip` render body directly to confirm no consumer, rather than trusting an absence-of-grep-hits alone. |
| **Fix** | Removed the 5 dead fields from the `ZeroDteGovernorSummary` type and their population in `summarizeGovernorForBoard`. Removed `maxCorrelatedSameDirection()` entirely (its stale, incorrect doc comment goes with it). |
| **What was deliberately NOT changed** | `premium_at_risk`/`max_premium_at_risk`/`short_gamma_open`/`max_short_gamma_open` — genuinely rendered in `GovernorStrip`, kept untouched. `premiumBudgetReason()`/`gammaBudgetReason()` — the functions that computed the now-removed `would_block_*` reason strings — themselves stay: `premiumBudgetReason` has a real second caller (`refreshGovernorPremiumBudgetBlocks`, `gates.ts`, wired at `scan.ts`, with its own test coverage), so only their now-unused call sites inside `summarizeGovernorForBoard` were removed, not the functions. `GOVERNOR_MAX_CORRELATED_SAME_DIR` constant — kept, still read by the live `concentrationReasonForCandidate`. `concentrationReasonForCandidate()` itself and its gate wiring (`governor_concentration` block) — the actually-enforced concentration check, completely untouched. |
| **Blast radius checked** | Grepped every removed symbol across the whole repo — zero surviving references. Confirmed `concentrationReasonForCandidate`'s own test ("fires when the candidate would exceed the cap; null under it") still passes unmodified, proving the real gate path is unaffected. |
| **Regression guard** | `governor.test.ts`: 53/53 pass (down from ~62; removed 5 tests fully dedicated to the deleted fields/function, kept everything else). Full `src/lib/zerodte/*.test.ts`: 1206 pass / 0 fail / 1 pre-existing skip (Node 20). `npx tsc --noEmit` clean. |
| **Status** | FIXED — PR pending (3 of 6 from the dead-code cleanup plan). |
