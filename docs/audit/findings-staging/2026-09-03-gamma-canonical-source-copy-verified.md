## 2026-09-03 — [FINDING, P3 product-copy/architecture, Thermal/SPX Slayer/Vector/Largo] Public gamma-preview copy claim ("the same structural read members trade on Thermal") verified accurate — ALL traced consumers share one canonical `fetchGexHeatmap` matrix, not independent re-derivations — NEGATIVE RESULT

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | User QA report (2026-09-02) flagged a P3 copy-consistency question: Thermal's own copy says "cross-validated against the live SPX rail," while the free public gamma preview (`/`, `HomeGammaPromo.tsx`, built on `src/lib/public-gex-snapshot.ts`) claims its structural read is "the same structural read members trade on Thermal." The ask was to verify these aren't two separately-maintained calculations that happen to usually agree — ideally there is ONE canonical gamma service every consumer (Slayer/Thermal/Vector/public snapshot/Largo) reads identically. |
| **Investigation** | Traced the actual import graph for `fetchGexHeatmap` (`src/lib/providers/polygon-options-gex.ts`) across every consumer named in the ask, confirming each one imports and calls the exact same function (not a copy, not a re-derivation): <br>• **Thermal's own route** — `src/app/api/market/gex-heatmap/route.ts:3` imports `fetchGexHeatmap`; its own code comment (line 289) states "The matrix is computed ONCE in fetchGexHeatmap and shared (in-memory + Redis)". <br>• **Public gamma snapshot** — `src/lib/public-gex-snapshot.ts:1,296` imports and calls `fetchGexHeatmap(ticker)` directly; `GammaSnapshotWidget.tsx:84`'s own comment already documents this ("the underlying matrix (fetchGexHeatmap, the same one Thermal reads)"). <br>• **SPX Slayer desk** — `src/features/spx/lib/spx-desk.ts:21,392` calls `fetchGexHeatmap("SPX")` and derives `gex_net`/`gamma_flip`/`gex_walls`/`above_gamma_flip` from that single call's output (the variable is even named `canonicalGex` in the surrounding code, lines 1636-1738). <br>• **Vector universe** — `src/features/vector/lib/vector-universe.ts:1,115` calls `fetchGexHeatmap(ticker)`; the narrowed-horizon wall totals (`vector-narrowed-walls-from-cells.ts`) are a pure in-memory arithmetic reduction over that SAME matrix's own per-expiry `cells` (its own comment: "the sweep already fetched the WHOLE matrix for this ticker in one `fetchGexHeatmap` call... no network, no cache, no provider"), not an independent recomputation. <br>• **Largo's gamma tool** — `src/lib/largo/gex-heatmap-for-largo.ts:4,149` calls `fetchGexHeatmap(sym)` the same way. <br>Found the exact copy under audit: `src/components/landing/HomeGammaPromo.tsx:184-185` — "The gamma flip, call wall, and put wall that pin or accelerate SPX — the same structural read members trade on Thermal, distilled into a free snapshot." |
| **Verdict** | **NEGATIVE RESULT — the copy is accurate.** All five traced consumers (Thermal's route, the public snapshot, SPX Slayer, Vector, Largo) call the identical `fetchGexHeatmap` function, which is computed once per ticker and shared via in-memory + Redis cache — not five separately-maintained calculations that happen to agree. This is genuinely ONE canonical gamma service, matching the architecture the QA report hoped for rather than the "separate caches with separately-maintained logic" it worried about. |
| **Not confused with** | CLAUDE.md's own `gex-depth-validate.mjs` finding (raw Black-Scholes-vs-provider gamma disagreement, 9-22% on SPY/QQQ/IWM due to dividend yield) is a DIFFERENT, already-understood, deliberately-anchored discrepancy — it's an internal consistency check of the depth ladder's own closed-form approximation against the provider's raw number, both of which are subordinate to and anchored on this SAME shared matrix's own `gex.total` at spot. It is not evidence of two competing canonical sources; confirmed by re-reading that section before writing this finding, per this investigation's own instructions to avoid re-flagging an already-understood discrepancy as new. |
| **Action taken** | None — no code change, no copy change. Documenting the call-graph evidence here so this specific consistency question doesn't need re-investigating by a future audit pass. |
