## 2026-09-05 — [FINDING, P2 performance, cron] `desk-warm` ran 77 times via `force=1` in 4 hours on a Saturday (market closed) from 54 distinct source IPs, median runtime 29s — the caller-identity logging added specifically for this investigation finally caught it, but the caller itself is still unidentified — OPEN, write-up only

> **kind:** `FINDING`

| Field | Value |
|-------|-------|
| **Status** | OPEN — flagged for the coordinator/infra lane owner; no code change made here |
| **Found by** | DISCOVERY lane, per the STANDING PERFORMANCE/LATENCY AUDIT MANDATE's "measure before guessing" method — pulled live `AWS/ApplicationELB` `TargetResponseTime` for `blackout-production-app`'s target group over the last 4h (real AWS creds present this session), found the exact "low average, high p99/Max" tail-latency signature this mandate names explicitly, then grepped CloudWatch Logs for `elapsed=` to find the driver. |
| **Today is a Saturday** | 2026-09-05 — market closed, effectively zero real member trading activity expected. |
| **ALB evidence (13:17–17:02 UTC, 15-min buckets, `TargetResponseTime` on `blackout-production-app`)** | p50 consistently 24–36ms (excellent) and p90 0.8–1.6s (healthy) throughout — but Max spiked to 9–95 SECONDS and p99 to 4–9s in **every single 15-minute bucket** across the full 4h window, with no bucket clean. Single worst: **94.545s Max** at the 13:17 bucket. |
| **Log evidence — the driver** | `[cron/desk-warm] background done` completions in the same 4h window: **77 total**, elapsed times min 510ms / **median 28,976ms (~29s)** / p90 57,841ms / **max 101,116ms (101s)**. 70 of the 77 (91%) exceeded 8s; 24 exceeded 40s. This one cron accounts for essentially the entire tail-latency signature above — the timestamps of the worst ALB Max buckets (13:17, 13:53, 15:38 UTC) line up directly with desk-warm's own worst individual runs. |
| **Why this is happening on a weekend at all** | `desk-warm`'s normal EventBridge schedule is weekdays 11–21 UTC only, and its in-app backup trigger (`rth-warm-leader`) is gated behind the same weekday/RTH-hours check — neither should fire even once on a Saturday. The only path that can run this cron outside that window is `?force=1`, a **complete, by-design bypass** of `shouldRunCacheWarmer`'s hours gate (`cache-warmer-gate.ts`) — documented as intentional for legitimate on-demand/debug warms, available to any `CRON_SECRET` holder. |
| **The caller-identity logging (added for exactly this investigation) finally has an answer, and it's bigger than expected** | `cache-warmer-gate.ts`'s own header comment already documents TWO prior rounds chasing this exact symptom: `CACHE_WARM_ALWAYS` (fixed, a leftover staging env var), then an unidentified `force=1` caller (a 60s rerun cooldown added as a rate floor, plus caller-IP/UA logging specifically so "the next investigation isn't a dead end"). That logging (`callerInfoFromRequest`) is what made this finding possible: `grep`ing `"force=1 bypassed the hours gate for 'desk-warm'"` over the same 4h window returns **182 bypass log lines from 54 DISTINCT source IPs** (`13.x`/`18.x`/`3.x`/`34.x`/`35.x`/`44.x`/`50.x`/`52.x`/`54.x` — spanning multiple AWS regions' ranges, not one host), **every single one carrying User-Agent `node`** (never a browser). This is not one leftover script or one misbehaving session — the caller diversity strongly suggests **many independent automated callers** (consistent with this repo's own documented reality of a large concurrent fleet of cloud agent sessions, several of which were independently running production-facing audit/validation work throughout today per this session's own observed git history) each separately deciding to hit this endpoint with `force=1`, unaware of each other. |
| **The existing 60s cooldown IS working, but the residual harm is still real** | `RERUN_COOLDOWN_KEY` (`desk-warm/route.ts`) correctly caps actual dispatch to roughly once per 60s regardless of how many `force=1` requests arrive — 182 bypass-log lines produced only 77 real completions (not 182), confirming the cooldown is doing its job and is NOT the gap. The gap is that **something keeps re-arming it**: at a ~77-per-4h cadence (~once every 3 minutes), the cooldown's 60s floor is being hit essentially continuously all day, all weekend, for zero member benefit — burning UW/Polygon API budget and ECS CPU on data nobody is viewing, and it is the direct, confirmed cause of the live ALB tail-latency pattern above. |
| **A second, distinct observation buried in the same data** | The MEDIAN desk-warm run (not just the tail) is ~29 seconds. The route's own top-of-file comment describes the work it does as "UW-bound (~2–5s cold)." A 29s median is roughly 6–15× that documented expectation, independent of the force=1/weekend story — worth the owning lane checking separately whether this is a genuine regression in `loadMergedSpxDesk`/`fetchGexHeatmap`/`loadBootstrapBundle`/`warmFlowsMemberCaches` (the 5-way fan-out this route awaits) or whether the "~2–5s cold" comment was simply never accurate/has gone stale. |
| **Why write up, not fix directly** | The prior author who added the 60s cooldown explicitly considered and rejected touching the hours gate itself ("`force=1` is documented, intentional, always-on behavior for legitimate on-demand warms... removing it would break real debug/deploy-validation use") — that reasoning still holds; a blunt fix (blocking `force=1` on weekends, or lengthening the cooldown much further) risks breaking exactly the debug/validation workflows this mechanism exists to serve, without knowing WHO the 54 callers actually are or whether any of them has a real reason to need weekend access. Given "never fix from a guess when the metric is one API call away," identifying the actual caller(s) — coordinating with whichever lane(s)/sessions are running `scripts/site-latency-audit.mjs` / `latency-burst-audit.mjs` / `compare-latency-envs.mjs` / `validate-deploy.mjs` (the repo's own documented candidates for constructing this exact request shape) or any other `force=1`-issuing tool against production this weekend — is the correct next step, not a code change made from this evidence alone. |
| **Suggested next step** | (1) Coordinator/operator: check whether any currently-running or recently-scheduled automation (the 360° cross-exam initiative, autopilot work loops, or any lane's own validation pass) is deliberately probing production with `force=1` this weekend, and if so, whether it needs to — a single coordinated pass beats 54 uncoordinated ones. (2) If the callers can't be identified/stopped operationally, consider (as a follow-up PR, not here) lengthening `RERUN_COOLDOWN_SEC` specifically outside `isEtExtendedWarmHours` (i.e., a stricter floor only when the normal window is already closed, leaving weekday/RTH-adjacent debug cadence untouched) rather than removing the `force=1` escape hatch entirely. (3) Separately, the owning SPX/infra lane should check whether the ~29s median runtime is expected or a regression — if the latter, it would independently make every one of these bypasses more expensive than the 60s-cooldown design assumed. |
