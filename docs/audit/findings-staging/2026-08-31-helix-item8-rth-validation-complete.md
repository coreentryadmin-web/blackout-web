## 2026-08-31 — [FINDING, P3 Helix ops] HELIX Phase 1 "Item 8" completed its one remaining requirement — an actual RTH-hours run — closing out Phase 1 certification entirely

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Context** | Item 8 (`scripts/audit/helix-rth-measurement.mjs`) was fixed 2026-08-29 (PR #3170, sibling of #3169's Item 7 fix) after being discovered as a stub that measured nothing and always exited 0. That fix was verified live but only off-hours (WEEKEND) — the script itself honestly labels an off-hours run as not satisfying Item 8's original purpose ("confirm the weekend baseline holds under LIVE market conditions"), so a real RTH-hours run remained outstanding. A one-shot trigger was scheduled for the next weekday RTH window to close this out. |
| **Run** | 2026-08-31 14:19:32 UTC (10:19 AM ET) — `NODE_USE_ENV_PROXY=1 node --import tsx scripts/audit/helix-rth-measurement.mjs --json` against `https://blackouttrades.com`. The script's own output confirmed `"market_phase": "RTH (9:30 AM - 4:00 PM ET)"` and `"is_rth": true` — the RTH condition is read from the run's own wall-clock check, not assumed from when the trigger fired. |
| **Result** | Every metric the 2026-08-24 doc predicted for a post-#2723 RTH run was met, none regressed: signal eligibility 100% (predicted exactly); GEX proximity 14.5% (predicted ~15%); IV distribution stayed fractional (median 0.16, max 54.29, no bimodal tail); route breakdown (68.4% UNREPORTED / 31.1% REPEAT / 0.5% FLOOR / 0.1% SWEEP) closely tracked the 2026-08-29 off-hours run (67.7/31.8/0.4/0.1), confirming that split is a stable property of the feed rather than an off-hours artifact. Writer-group split (1582 A / 3418 B) and real-print span (70.5h, narrower than the weekend baseline's 168h because RTH's higher print rate fills the 5000-row cap faster) both moved in the expected direction with no anomaly. |
| **Action** | Updated `docs/audit/HELIX-PHASE1-COMPLETION-STATUS.md` in place: Item 8's section now shows the full baseline/expected/live comparison table and marks the item fully closed (not just "implemented"); Item 7's section — found stale while touching this same doc, still describing PR #2841's original "framework ready" claim and listing capabilities that were never actually implemented (only 3 of the 10 checks the docstring describes are real code) — was also corrected to state what #3169 actually fixed and what remains unbuilt (overlap/clip/tap-target/panel-state/keyboard-nav/deep-link/network-validation are still not implemented, a separate follow-up); the document header, executive summary, and coordinator handoff notes were updated to reflect that Phase 1 certification has no open items. |
| **Blast radius** | Documentation only. No code changed in this pass — the fix was entirely in PR #3170 (2026-08-29); this run is the verification that fix actually completes the item, plus a correction to the record that had drifted stale again in a different section of the same file. |
| **Status** | CLOSED. HELIX Phase 1 certification (all 9 items, including the two that turned out to have never been executed) is now fully verified against live production. |
