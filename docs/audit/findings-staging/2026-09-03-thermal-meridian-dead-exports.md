## 2026-09-03 — [FINDING, P4 dead-code, Thermal/Meridian] A batch of tested-but-unwired and abandoned exports across both lanes, same "designed-ahead, not yet wired" pattern already documented for X-intel — flagged rather than deleted — OPEN, write-up only

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | A dead-code sweep of Thermal and Meridian (areas not yet swept this session), part of the standing DISCOVERY mandate. `composeThermalRead` — the single most consequential item found — got its own separate write-up (`2026-09-03-bie-composer-family-wiring-gap.md`) since it turned out to be one instance of a much larger cross-BIE pattern, not a Thermal-only gap. Everything below is the remaining, more ordinary per-export dead code, each confirmed via `grep -rn "\b<name>\b" src scripts` excluding the export's own definition file and its own `.test.ts`. |
| **Thermal — `src/features/thermal/lib/thermal-desk-state.ts`** | Nine exports with zero external references, each individually exercised only by `thermal-desk-state.test.ts`: `MATRIX_LIVE_MS`, `OVERLAY_LIVE_MS`, `OVERLAY_STALE_MS`, `THERMAL_MATRIX_CORE_TICKERS`, `shouldForceBlankMatrixRefresh`, `parseThermalLens`, `keyLevelsFootnote`, `honestLevelEmpty`. Checked whether the module's evident intended consumer, `GexHeatmap.tsx`, should be importing these instead of its own local copies: it has its own local `MATRIX_STALE_MS = 15_000`, which happens to equal `thermal-desk-state.ts`'s own `MATRIX_STALE_MS` — but `GexHeatmap.tsx`'s value is independently derived (its own comment: "~3× the 5s poll/TTL window... was 40s/'20s' copy after the TTL dropped to 5s, FINDINGS 2026-07-28") rather than copied, so this is NOT the same "silently drifted duplicate" bug shape fixed elsewhere this session (SPX's stale poll-cadence comment) — forcing a shared import here would couple two independently-reasoned constants that only coincide today. Left untouched. |
| **Thermal — `src/lib/thermal-discord-card.ts`** | `fmtDeskExpiry`, `resolveDiscordDeskExpiry`, `resolveDiscordZeroDteExpiry` — tested, unused parallel expiry-resolution helpers, apparently superseded by whatever the live Discord card path uses now. `thermalDiscordCaptionMode` — untested, unused, always returns the literal `"NEAR"`. `thermalDiscordCaption` — already self-marked in its own doc comment: `@deprecated Use \`buildThermalDiscordEmbed\` — legacy plain-text caption kept for reference.` The explicit "kept for reference" language means a prior author already made the keep-vs-delete call deliberately — not something to second-guess here. |
| **Thermal → Meridian — `src/lib/meridian/meridian-thermal-scope.ts`** | `scopesAreMixed`: tested, zero callers. Small, but genuinely two-lane-adjacent (thermal-scope logic living under the meridian directory), so flagged for whichever lane owns cross-scope Thermal/Meridian logic rather than assumed. |
| **Meridian — `src/lib/meridian/meridian-play-suggestions.ts`** | `suggestedPlayFromThermal`: a full 112-line, tested module generating a call/put strike-pair suggestion from GEX walls + expected move for an earnings event. The file's own header comment already says *"Omitted: detailed calibration pending Coordinator approval of design questions"* — this is the clearest "designed ahead, deliberately not yet wired" signal of the whole batch, the exact shape of the X-intel `scoreAttachmentPackage` precedent. Zero references anywhere. |
| **Meridian — `src/lib/meridian/meridian-benzinga-earnings-core.ts`** | `mergeBenzingaTimelineRows` (untested, uncalled) calls `mergeEarningsTimelineSources` (tested, but ALSO has zero external callers) — an unused wrapper around what reads like an unused intended replacement for something. Worth the Meridian lane checking whether `mergeEarningsTimelineSources` was meant to replace whatever the live path currently does for timeline merging. |
| **Meridian — smaller, lower-signal items** | `surpriseDistributionFromRows` (`meridian-benzinga-analytics.ts`, tested, uncalled beat/miss aggregator); `catalystHeadlinesFromBundle` (`meridian-catalyst-enrich.ts`, untested, uncalled); `ORBITAL_LABEL_CHAR_PX`/`ORBITAL_LABEL_LINE_PX` (`meridian-spatial-core.ts`, tested layout constants, unused elsewhere); `pctAlongCss`/`momentumSignal`/`anomalySignal`/`hoursUntilIso` (`meridian-viz-core.ts` — four dead corners inside an otherwise large, actively-used file; `MV_RAIL_LABEL_LIFT_PX`/`LETTER_SPACING_EM` in the same file were checked and are NOT dead — they're deliberately asserted against the live stylesheet in tests as CSS-drift guards). |
| **Why write up, not delete** | Per the same reasoning as the 2026-09-02 X-intel entry this mirrors: several of these are real, non-trivial bodies of work (the play-suggestion module especially, explicitly deferred pending approval) where "abandoned" vs "built ahead of the caller that will wire it in" is a call only the owning lane can make. The smaller, more clearly-superseded items (the Discord card's parallel expiry helpers, the Benzinga merge wrapper) are lower-risk to delete but still bundled here rather than acted on unilaterally, since they sit in lanes with active, in-flight development this session (Thermal/Meridian were deliberately chosen for this sweep precisely because they had NOT shown up in this session's `git log`, but that can change any hour). |
| **Suggested next step** | For the Thermal lane: confirm whether the `thermal-discord-card.ts` parallel helpers and `thermal-desk-state.ts`'s 8 remaining unused exports are safe deletes (they look like it) or still-planned wiring. For the Meridian lane: decide `suggestedPlayFromThermal`'s fate now that "Coordinator approval" may have since been granted or declined, and check whether `mergeEarningsTimelineSources` was meant to replace the live timeline-merge path. |
| **Status** | OPEN — flagged for the Thermal and Meridian lane owners; no code change made here. |
