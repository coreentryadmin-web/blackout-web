## 2026-09-04 — [FINDING, P4 stale-docs, Thermal] 2 of 4 `thermal-followup-pr-*` stubs described already-shipped work as open; a 3rd was partially stale — corrected

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Dead-code/stale-docs sweep of `src/features/meridian/` and `src/features/thermal/` (fresh territory this session — Largo/db.ts/SPX-NightHawk-Vector were already swept). Found `docs/audit/followup-pr-stubs/thermal-followup-pr-{1,2,3}-*.md` describing planned work that, on inspection, is already implemented. |
| **PR-1 (spot provenance) — fully stale** | Verified `spot_source` exists on `GexHeatmap` (`polygon-options-gex.ts:479`), wired through `buildGexHeatmapUncached` (`:3771`) and the public snapshot (`public-gex-snapshot.ts:214`) — exactly the fields/call sites the stub's own implementation sketch named. |
| **PR-2 (flip_reason to Largo) — fully stale** | Verified `flip_reason` is computed in `polygon-options-gex.ts` (316/1490/3718) and published through the Largo contract in `src/lib/largo/gex-heatmap-for-largo.ts` (25/163/224). |
| **PR-3 (chain truncation indicator) — partially stale** | The server-side field (`chain_truncated` on `GexHeatmap`, `polygon-options-gex.ts:3772`/`:481`, published via `public-gex-snapshot.ts:215`) is done. But the member-facing UI badge the stub calls for, and Largo-tool surfacing (the same treatment `flip_reason`/`spot_source` already got), are confirmed absent — zero references to `chain_truncated` anywhere in `src/features/thermal/` or `gex-heatmap-for-largo.ts`. This is a real, live backlog item, not dead paper — only the "add the field" half of the stub is done. |
| **Why this matters** | A stub describing already-shipped work as an open PR plan wastes the next reader's time re-deriving what's already true from source, and for PR-3 specifically risks the real remaining work (UI badge + Largo surfacing) being deprioritized because the doc reads as if the whole thing shipped when only half did. |
| **Fix** | Added a dated `STATUS (2026-09-04, verified live)` note at the top of each stub (matching this session's established pattern for correcting stale doc claims, e.g. `HEATMAP_DATA_CONTRACT.md`, `CLAUDE.md`'s `helix-signal-outcomes` correction) rather than deleting — PR-1/PR-2 marked fully SHIPPED (kept for historical context only), PR-3 marked PARTIALLY SHIPPED with the narrowed remaining scope spelled out explicitly. `thermal-followup-pr-4-client-walls-constraint.md` was checked and found NOT stale — left untouched. |
| **Blast radius** | Three doc files, one status note each. No code changed. |
| **Also found in the same sweep (write-up only, not fixed here)** | 3 genuinely dead exports with zero importers anywhere (`meridian-viz.tsx`'s `MeridianSignalChip`/`MeridianHalo` React components, `thermal-compare-presets.ts`'s `isThermalComparePresetId`) and 3 built-and-tested-but-never-wired-into-a-component functions (`thermal-intensity.ts`'s `thermalIntensityBlockCount`, `thermal-desk-state.ts`'s `shouldForceBlankMatrixRefresh`/`honestLevelEmpty`). Not deleted — same reasoning as this session's earlier Largo dead-code finding: these are complete, tested pieces that look like UI-hookup work that never landed, not accidental leftovers, so the call on wiring-in vs. retiring is better made by whoever owns Meridian/Thermal desk-state than decided unilaterally here. Flagging for that lane's next pass rather than a separate staged finding, since no code action was taken. |
| **Gates** | Doc-only change — no test/build gates apply. |
