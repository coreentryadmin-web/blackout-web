## 2026-08-30 — [FINDING, P4 marketing/public-site] #3187's homepage ISR fix was necessarily narrow — two more no-store call sites in the same build path still recur — OPEN, write-up only

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this follows up on** | `docs/audit/findings-staging/2026-08-30-homepage-isr-defeated-by-nostore-fetch.md` (this cycle's earlier write-up) → fixed same-day in PR #3187, which threaded an optional `next: { revalidate: 3600 }` override through `polygon-largo.ts`'s `polygonGet`/`fetchPreviousDayBar` and applied it at exactly one call site, `fetchSpotFromPrevBar` (the last-resort spot fallback in `polygon-options-gex.ts`). That fix is correct and live-confirmed: zero recurrences of that specific error signature (`.../v2/aggs/ticker/.../prev`) since 05:09 UTC, well after #3187 merged. |
| **New live evidence — the same defect, different call sites** | A fresh CloudWatch sweep (2026-08-30, ~08:20-10:20 UTC window) found the identical `Error: Page changed from static to dynamic at runtime /` symptom recurring at **08:32:05** and **08:32:25 UTC** — but this time naming two DIFFERENT upstream URLs: `https://api.massive.com/v3/snapshot/indices?ticker.any_of=I%3ASPX` and `https://api.unusualwhales.com/api/stock/SPX/spot-exposures/expiry-strike?expirations%5B%5D=2026-08-31&limit=500`. #3187's fix does not cover either — it only touched the prev-bar fallback path. |
| **Traced to source** | `fetchGexHeatmap` (`polygon-options-gex.ts`, called by the same homepage → `buildPublicGexSnapshot` → `fetchGexHeatmap` chain the original finding traced) reaches these in its PRIMARY (non-fallback) spot-resolution path — not the last-resort one #3187 fixed: (1) `fetchIndexSnapshot` (singular; `polygon.ts:427`), called at `polygon-options-gex.ts:1280` and `:1314`, itself calling the shared `polygonGet` which hardcodes `cache: "no-store"`; (2) `fetchUwSpotExposuresExpiryStrike` (`unusual-whales.ts:2244`, exact param match confirmed against the log's `expirations[]`/`limit=500`), calling the shared `uwGetSafe`, which also hardcodes `cache: "no-store"` (`unusual-whales.ts:161`). |
| **Why not fixed directly this cycle** | Same reasoning as the original finding, now confirmed for two more functions: `fetchIndexSnapshot` has **7 non-test callers** across the live quote route, NightHawk option-chain prompts, Vector live candles, correctness verifiers, and option-trades — genuinely live-sensitive paths where `no-store` freshness is almost certainly intentional. `fetchUwSpotExposuresExpiryStrike` has 2 external callers (`largo/run-tool.ts`, `live-api-integrations.ts`). Threading a scoped cache override into either shared helper needs the same call-site-specific opt-in #3187 already demonstrated — but doing it correctly for two more functions, in a shared provider layer with real-money-adjacent 0DTE/Largo consumers, is exactly the kind of repeat shared-infrastructure edit that should go through the owning lane, not be guessed at twice in one sweep session. |
| **Suggested fix shape** | Mirror #3187 exactly: add the same optional `PolygonLargoFetchInit`-style override to `fetchIndexSnapshot` (thread through to `polygonGet`) and an equivalent optional override to `fetchUwSpotExposuresExpiryStrike` (thread through to `uwGetSafe`), then have `fetchGexHeatmap`'s primary spot-resolution branch (lines ~1280/1314) and whatever UW spot-exposures call feeds the homepage's build path pass `{ next: { revalidate: 3600 } }` — same pattern, same rationale (last-resort AND primary GEX-snapshot spot reads for a page whose own `revalidate = 3600` should hold). |
| **Status** | OPEN — flagged for the owning lane (public/marketing site + shared Polygon/UW provider layer), no code change made |
