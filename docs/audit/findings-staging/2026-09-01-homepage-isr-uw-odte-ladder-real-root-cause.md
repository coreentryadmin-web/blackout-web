## 2026-09-01 — [FINDING, P3 marketing/public-site] The real source of the homepage's recurring UW `expiry-strike` no-store fetch, found — it's the SPX 0DTE dealer-gamma overlay, not a dead-code false match — OPEN, write-up only (shared-infra risk, not self-contained)

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this continues** | `docs/audit/FINDINGS.md`'s 2026-08-31 correction ("The 'two more no-store paths' finding's UW root-cause claim does not hold under a full call-graph trace") retracted the earlier claim that `fetchUwSpotExposuresExpiryStrike` was the source of a recurring `Error: Page changed from static to dynamic at runtime /` naming `https://api.unusualwhales.com/api/stock/SPX/spot-exposures/expiry-strike?...` — that function's only two callers are a Largo terminal tool and a dead registry entry, neither in `/`'s render tree. The correction concluded "the true source of this recurring `expiry-strike` call inside `/`'s render is not identified" and named two unattempted next steps: (1) check whether Next.js can misattribute a concurrent request's fetch to route `/`, (2) a live authenticated trace. This entry does neither — it re-reads the call graph with the right function name and finds the real, live caller directly. |
| **The correction's grep missed a sibling function hitting the SAME URL** | `unusual-whales.ts` has two functions that both call `/api/stock/{ticker}/spot-exposures/expiry-strike` with an `expirations[]` param: `fetchUwSpotExposuresExpiryStrike` (line 2284, the one the retracted claim named — genuinely dead in `/`'s tree) and `fetchUwOdteSpotExposuresByStrike` (line 444, doc-commented `"0DTE strike GEX ladder — same expiry-strike feed, strike-level rows for gamma walls"`). The 2026-08-31 correction's own text says it traced `fetchUwOdteGexLadder`'s callers (a Helix-side consumer of `fetchUwOdteSpotExposuresByStrike`) and found none, then concluded the function was "fully dead" — but `fetchUwOdteSpotExposuresByStrike` has a SECOND, separate direct caller the correction never checked: `spx-odte-uw-ladder.ts:79`'s `fetchSpxOdteScopedUwLadderRest`. |
| **The real, live, unbroken call chain from `/`** | `src/app/(marketing)/page.tsx` → `buildPublicGexSnapshot("SPX")` → `fetchGexHeatmap("SPX")` (`polygon-options-gex.ts`) → for `root === "SPX"`, `applySpxOdteGexUwOverlay(heatmap)` (`polygon-options-gex.ts:1560`/`:3736`, confirmed via `polygon-options-gex.test.ts:133`'s own regex assertion `if (root === "SPX")[\s\S]*applySpxOdteGexUwOverlay`) → `getSpxOdteScopedUwLadderMap(expiry)` (`spx-odte-uw-ladder.ts:137`, 5s in-process cache, miss on most requests) → `fetchSpxOdteScopedUwLadder` → `fetchSpxOdteScopedUwLadderRest(sym, expiry)` (line 74) → `fetchUwOdteSpotExposuresByStrike(sym, expiry)` (`unusual-whales.ts:444`) → `uwGetSafe` → `uwGet` (`unusual-whales.ts:161`), which hardcodes `cache: "no-store"` on every request, same pattern as `polygonGet`'s hardcoded `no-store` that #3202 fixed for the Polygon half. Every link in this chain is confirmed by reading the source, not inferred. |
| **Live evidence the timing lines up, not just the call graph** | Correlated 7 fresh occurrences (2026-09-01, 12:01–13:15 UTC) of the `Page changed from static to dynamic ... unusualwhales.com/.../expiry-strike` error against the surrounding CloudWatch log lines (±5s): **all 7** are immediately preceded (30–100ms before, same request) by `[spx-odte-overlay] ladder unavailable for <date> — serving UN-OVERLAID SPX matrix` (the exact warning `spx-odte-gex-uw-overlay.ts:198-200` logs right after `getSpxOdteScopedUwLadderMap` returns empty) — not a coincidence at that tightness across 7/7 samples. Two of the 7 are also immediately followed by `[gex-heatmap] 0 contracts for I:SPX @ ... trying UW strike-exposure fallback`, a second, independent UW touchpoint in the same request confirming this is genuinely the SPX GEX-heatmap build path executing, not an unrelated concurrent request. |
| **Why the outside-RTH context matters for reading this** | All 7 samples logged `[zerodte-breakout] outside the RTH commit window` / `[spx-play-gates] halt channel stale` alongside them (pre-market/after-hours 2026-09-01), and the overlay's own `ladder_unavailable` branch is expected off-hours (no live 0DTE ladder to overlay with pre-open) — so the SPECIFIC no-store fetch firing here is the ladder-fetch ATTEMPT itself (which still runs, gets an empty/absent result, and STILL counts as a no-store fetch against Next.js's static-generation tracking even though it returns nothing usable) — not evidence this only happens off-hours. RTH would fire the same `uwGet` call, just usually resolve non-empty. |
| **Why not fixed directly this cycle — this is NOT the #3202 pattern** | #3202 threaded a `revalidate` override through `polygonGet` for exactly one last-resort, previous-day-close fallback where staleness plainly doesn't matter. This ladder is different in kind: its own doc comment calls it *"the single source of truth for SPX King alignment"* used identically by the correctness oracle and by member-facing SPX Slayer/Largo/Thermal surfaces (`fetchGexHeatmap` is the shared heatmap builder for all of them, not a homepage-only function) — the file's own commit history (the `recomputeNearTermGexStrikeTotals` comments) documents real production incidents from serving a STALE or inconsistent version of exactly this ladder (6-point gamma-flip staleness, three different wall pairs served within a minute). Threading a `revalidate` override into `uwGet`/`uwGetSafe`/`fetchUwOdteSpotExposuresByStrike` to fix the *marketing homepage's* cosmetic ISR annotation would change ladder freshness for every caller sharing that fetch path, which is exactly the "shared-infrastructure edit... a materially larger and riskier change" the 08-31 correction already declined to guess at for the Polygon half of this same finding — it applies with more force here, since this ladder is explicitly the King-alignment source of truth, not a last-resort fallback. |
| **What the owning lane actually has to decide (this file does not choose for them)** | Two directions, neither attempted here: (1) **Scope the freshness override to the homepage caller specifically** — e.g. give `buildPublicGexSnapshot`'s public/marketing GEX build a flag that skips the SPX 0DTE UW overlay entirely (the public snapshot is a coarse public-facing display, not the precision King-alignment surface Largo/SPX Slayer serve — losing 0DTE ladder precision there may be an acceptable trade the member-facing paths cannot make). (2) **Accept `/` as intentionally dynamic for SPX** and drop or reword the `revalidate = 3600` annotation on `(marketing)/page.tsx`, since a page whose live GEX snapshot depends transitively on a real-time 0DTE gamma ladder may not be a realistic ISR candidate at all — matching the original 2026-08-30 finding's own alternate suggestion #2 ("if ISR was never realistically achievable... drop the annotation rather than leave one that silently doesn't hold"). |
| **Status** | OPEN — the previously-unidentified root cause is now identified and confirmed (call graph + 7/7 live timing correlation), but the fix requires a design decision about a shared, freshness-sensitive, member-facing data path (SPX King alignment), not a same-shape repeat of the #3202 precedent — flagged for whoever owns the SPX GEX/0DTE ladder surface (`polygon-options-gex.ts` / `spx-odte-uw-ladder.ts`) or the marketing homepage build. No code change made this cycle. |
