## 2026-08-30 — [FINDING, P4 dead-code, Largo] The 2026-08-10 "clean follow-up deletion" of orphaned `bie/*` files is not as clean as it read — OPEN, write-up only

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this follows up on** | The 2026-08-10 finding "Largo's BIE answer-ROUTER was already bypassed but still compiled in... REMOVED" left a note: *"`bie/router.ts`, `bie/composers.ts`, `bie/decompose.ts` and `bie/dynamic-format.ts` now have zero non-test consumers outside `src/lib/bie/` itself. Left in place in this PR to keep the blast radius to Largo; they are a clean follow-up deletion."* That deletion was never done. Went to pick it up as a small, well-scoped DISCOVERY cleanup; it isn't as clean as the note implies. |
| **`router.ts` — NOT dead, keep** | Verified via precise import-statement grep (`from ["'].*bie/router["']`, no substring false positives): `type { BieRoute }` is imported from `router.ts` by `live-data-enrich-detect.ts`, `live-data-enrich.ts`, `largo-status.ts`, `response-shape.ts`, and `dynamic-format.ts` — all live, non-test files inside `src/lib/bie/`. This is a real dependency the 2026-08-10 note's "zero non-test consumers" claim missed (likely because the check at the time only looked for consumers of the deleted `tryBieRoute`/`classifyBieIntent` *functions*, not the still-used `BieRoute` *type* the same file exports). `router.ts` must not be deleted. |
| **`decompose.ts` — genuinely dead by static analysis** | Zero imports anywhere in `src/` except its own `decompose.test.ts` (verified with an exact-path grep, no false positives). No production file references it. |
| **`composers.ts`/`dynamic-format.ts` — dead by static analysis, but one live reference is unverifiable here** | `composers.ts` (1021 lines) has zero real import statements anywhere in `src/` in any form (aliased, relative, `require`, or dynamic `import()`) — **except** a `mock.module("./bie/composers", {...})` call inside `src/lib/largo-terminal.test.ts`, which mocks a `composeBieAnswer` export and carries a comment claiming *"this suite exists to prove the real router decision reaches the persisted row."* `dynamic-format.ts` is imported only by `composers.ts` and its own test. |
| **Why this blocks a confident deletion** | `largo-terminal.test.ts` does not run in this sandbox — confirmed live: `npx tsx --test src/lib/largo-terminal.test.ts` on Node 20.20.2 fails all 13 tests with `import_node_test.mock.module is not a function`, the same pre-existing runner gap already documented elsewhere in this file (`node:test`'s `mock.module` needs a flag/Node version this sandbox doesn't have). That test is the ONLY place in the entire codebase that references `composers.ts` at all, and its own comments describe exercising a "real router decision" — language that reads as testing a live path, not a historical one. I cannot confirm from static analysis alone whether CI (where this test presumably does run) actually reaches `composers.ts`/`dynamic-format.ts` through some path invisible to a grep-based check (e.g. the test's own mock setup causing a dynamic resolution, or a code path gated behind a runtime flag). Deleting a 1000+ line file on the strength of "grep found nothing" when the one test that could prove it is unrunnable here is exactly the kind of confident-but-wrong static-analysis conclusion this repo's own tooling notes (`largo-payload-hygiene.mjs`'s "the instrument is a model" caveat, the truncation-probe saga) repeatedly warn against. |
| **What was safe to conclude, and what wasn't** | Safe: `router.ts` is definitely still needed (real type imports, verified). Not safe from here: whether `composers.ts`/`decompose.ts`/`dynamic-format.ts` can be deleted without breaking `largo-terminal.test.ts` in CI — that needs either running the suite where `mock.module` actually works, or a maintainer confirming the "real router decision" language in the test is itself stale. |
| **Recommended next step** | Whoever owns Largo/CI should run `largo-terminal.test.ts` where `mock.module` works (CI, or a newer local Node with the right test flags), confirm whether it still exercises a real `composers.ts` code path or is itself testing dead scaffolding, and only then delete `composers.ts` + `decompose.ts` + `dynamic-format.ts` + their test files together (not `router.ts`, which stays). |
| **Status** | OPEN — corrects the 2026-08-10 finding's "clean follow-up deletion" framing; flagged for the Largo lane, no code change made |
