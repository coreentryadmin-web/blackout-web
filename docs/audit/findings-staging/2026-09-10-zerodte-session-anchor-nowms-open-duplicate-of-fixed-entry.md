## 2026-08-30 — `currentZerodteSessionAnchor`'s `nowMs`-ignored finding — CORRECTED: duplicate entry, already fixed same day (PR #3190)

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | DISCOVERY-cycle re-verification of older `FINDINGS.md` claims, continuing the pattern already found twice this session (2026-08-18 Meridian estimates/history, #4667; 2026-08-30 Meridian week-analytics, #4668) — a write-up-only `OPEN` finding sitting unremoved next to its own `FIXED` sibling entry for the identical bug. |
| **The duplicate** | `## 2026-08-30 — [FINDING, P3 Night Hawk/0DTE] currentZerodteSessionAnchor's nowMs is silently ignored for session_state/session_note — OPEN, write-up only` (status: "OPEN — flagged for the Night Hawk/0DTE lane, no code change made") is immediately followed in `FINDINGS.md` by `## 2026-08-30 — [FINDING, P3 0DTE/Largo] currentZerodteSessionAnchor ignored nowMs for session_state — FIXED`, describing the identical root cause and the exact fix shape the `OPEN` entry itself suggested ("Add an optional `now: Date` parameter to... `todayEt()`... and `etNowParts()`... `currentZerodteSessionAnchor` calls both with `new Date(nowMs)`"). |
| **Verification** | Read the current `src/lib/zerodte/session-phase.ts:54-65` — `currentZerodteSessionAnchor(nowMs)` now does `const now = new Date(nowMs); ... const { hour, minute } = etNowParts(now);` — exactly the fix the `OPEN` entry's own "Suggested fix shape" described, and exactly what the `FIXED` entry claims. `session_state`/`session_note` (derived from `hour`/`minute` via `sessionHeat`) now genuinely depend on the injected `nowMs`, not the real wall clock. Traced to `git log`: commit `873716fc8`, **"fix(zerodte): thread nowMs through session anchor phase" (PR #3190)**. |
| **Pattern note for the coordinator** | This is the **third** instance this session of the identical shape: a correctly-filed write-up-only `OPEN` finding, picked up and fixed by the owning lane quickly, with a fresh `FIXED` entry added alongside rather than the original `OPEN` entry being updated or removed. See also the two companion corrections filed today (findings-staging entries for the 2026-08-18 Meridian estimates/history finding and the 2026-08-30 Meridian week-analytics finding). Worth the coordinator considering whether `findings-fold-staging.mjs` or the write-up-finding template itself should encourage/require a "supersedes #NNNN" backlink when a lane closes out a write-up-only finding, to stop this recurring. |
| **Status** | Documentation-only correction. No code change — the underlying bug is already fixed and already correctly documented once. |
