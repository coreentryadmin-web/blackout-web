## 2026-09-03 — [FINDING, P4 docs/SPX] `SPX_MATRIX_POLL_RTH_MS`'s comment claimed a "5–8s" server matrix cache TTL — the real TTL is a flat 5s — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Just-merged #3384 (GEX cross-product calculation envelope) flagged, but deliberately did not fix, a stray comment: `spx-desk-poll-ms.ts:27` claimed "server matrix cache 5–8s" while the actual server TTL is a flat 5s default for all tickers including SPX — noted there as "doc drift, not a behavior bug" and out of that PR's scope. Picked it up directly this cycle since it's exactly the kind of small, self-contained, already-diagnosed fix this lane handles without a write-up. |
| **Root cause** | `SPX_MATRIX_POLL_RTH_MS`'s doc comment (`src/features/spx/lib/spx-desk-poll-ms.ts:28`) said `(server matrix cache 5–8s; client reads cache)`. Confirmed via `polygon-options-gex.ts` that this stopped being true a while ago: line 1656's own comment says *"SPX Slayer / desk hot path — same 5s TTL as the global default now"*, and lines 2865-2867 explicitly document the history — *"this comment used to say the base TTL was 'a flat ~20s'... That has not been true since the base dropped to 5s (`GEX_HEATMAP_CACHE_SEC ?? 5`)... every ticker is served on a 5s TTL."* The poll-cadence comment was never updated when the server TTL changed, so it kept quoting a stale range describing behavior that no longer exists. |
| **Why it matters, even at P4** | This is the one comment an engineer reads when deciding whether the 2s client poll cadence is fast enough relative to the server cache — a "5–8s" range makes 2s polling look closer to real-time-relative-to-cache than it is; the true flat 5s TTL is the number that actually matters for that judgment. Doc drift like this compounds: it's the same failure shape (a comment describing an outdated system) as several other entries this file already tracks, just smaller blast radius. |
| **Fix** | Rewrote the comment to state the flat 5-second TTL and point at `polygon-options-gex.ts`'s own note as the source of truth, rather than re-asserting a number that can drift again silently. |
| **Blast radius** | Single comment, single file. No other call site quotes this range — `grep -rn "5.8s\|5–8s" src` after the fix returns nothing. |
| **Regression guard** | New `spx-desk-poll-ms.test.ts`: a source-text assertion that the file no longer contains the stale `5-8s`/`5–8s` pattern. Confirmed RED against the pre-fix comment (`git stash` the source change, keep the test — 1/1 fails) and GREEN post-fix (1/1 passes). |
| **Gates** | `npx tsc --noEmit` clean (Node 20.20.2). Full `npm test`: 11849 passed / 0 failed / 2 skipped. |
