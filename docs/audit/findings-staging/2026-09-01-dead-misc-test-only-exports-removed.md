## 2026-09-01 — [cleanup, 0DTE misc] dead test-only exports removed (board/market-state/earnings-print-window) — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Context** | PR 6 of 6 (final) from the dedicated dead-code cleanup audit (`nighthawk-0dte-deadcode-cleanup` workflow synthesis). |
| **Root cause** | Three independent exports whose only callers were their own dedicated unit tests, never production code: (1) `EngineCard` type + `rankEngineCards()` (`board.ts`) — a deterministic engine-card sort helper (ACTIVE/ARMED/SCANNING/DONE/OFF ordering with a power-hour-window tiebreak) with no route, component, or other module ever importing it. (2) `applyRailWeightToScore()` (`market-state-engine.ts`) — a single-origin rail-weight scorer; the module's actually-deployed scorer is the sibling `weightedScoreForMerge()` (max-of-origins variant, imported by `discovery-events-persist.ts` and elsewhere), which reimplements its own origin→rail lookup inline rather than calling this function or its helper. Removing `applyRailWeightToScore()` left `primaryRailFromOrigins()` (its sole internal caller) with zero remaining callers, so that went too — a direct cascading consequence, not a separate finding, the same pattern as PR 5's `envFlag()`. (3) `tallyPrintWindows()` + `PrintWindowTally` type (`earnings-print-window.ts`) — a verdict-tally helper over `assessPrintWindow()`. The one place this logic is genuinely exercised outside its own test is `scripts/audit/g11-print-window-outcome.mjs`, and CLAUDE.md's own toolkit entry for that script says the classifier there is "mirrored in `lib/print-window-eval.mjs`... it's a copy not an import so a plain `.mjs` script avoids TS path aliases" — i.e. the audit tooling deliberately does NOT import this module, so there was never a second real caller. |
| **Evidence** | Whole-repo grep for every symbol name, re-run immediately before deleting: `EngineCard`/`rankEngineCards` — hits only in `board.ts`'s own definition and `board.test.ts`'s import + one dedicated test block. `applyRailWeightToScore` — hits only in `market-state-engine.ts`'s own definition and `market-state-engine.test.ts`'s import + one dedicated test. `primaryRailFromOrigins` — hits only its own definition and its one call site inside `applyRailWeightToScore` (not even imported by the test file, which pulls `applyRailWeightToScore, buildMarketState, rawRailWeightsForStructure, weightedScoreForMerge` only). `tallyPrintWindows` — hits only its own definition and `earnings-print-window.test.ts`'s import + one dedicated test; zero hits under `scripts/`. |
| **Fix** | Removed the `EngineCard` type, `rankEngineCards()`, and the "Engine card ranking" section header comment from `board.ts`; removed the corresponding `rankEngineCards` import and "engine ranking" test block from `board.test.ts`. Removed `applyRailWeightToScore()` and its now-orphaned helper `primaryRailFromOrigins()` from `market-state-engine.ts`; removed the corresponding import and dedicated test from `market-state-engine.test.ts`. Removed `tallyPrintWindows()` and the `PrintWindowTally` type from `earnings-print-window.ts`; removed the corresponding import and dedicated test from `earnings-print-window.test.ts`. |
| **What was deliberately NOT changed** | `weightedScoreForMerge()` (`market-state-engine.ts`) — the real, deployed multi-origin scorer, completely untouched, including its own independent inline origin→rail mapping (it does not call the removed `primaryRailFromOrigins`). `assessPrintWindow()` and `PrintWindowVerdict`/`PrintWindowInput` types (`earnings-print-window.ts`) — the live per-row classifier every other test and the audit script's mirrored logic actually exercises, untouched. All other `board.ts` exports (dossier enrichment, setup derivation, etc.), untouched. |
| **Blast radius checked** | Grepped every removed symbol across the whole repo (`src/`, `scripts/`) — zero surviving references after the edit. Confirmed no route or component under `src/app/` imports any of the three removed symbols. |
| **Regression guard** | `board.test.ts` 126/126, `market-state-engine.test.ts` 5/5, `earnings-print-window.test.ts` 9/9 — pass individually. Full `src/lib/zerodte/*.test.ts` (run with the project's actual `--experimental-test-module-mocks` flag, matching `scripts/run-tests.mjs`): 1209 pass / 0 fail / 1 pre-existing skip (Node 20). `npx tsc --noEmit` clean. |
| **Status** | FIXED — PR pending (6 of 6, final PR from the dead-code cleanup plan). |
