## 2026-08-31 — [FINDING, P3 CI/tooling] `largo-stress-nightly.yml`'s scheduled fire time drifted from ~55min late to 5.5-12.5 HOURS late, starting 2026-08-27 — OPEN, write-up only

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Following up on PR #3227 (the "Open ops issue on failure" permissions fix) — waiting for the first real nightly run to confirm it end-to-end. As of this writing (2026-08-31 ~08:16 UTC), no run has fired yet against today's `30 6 * * *` (06:30 UTC) schedule — nearly 1h47m late. Pulling the full run history to check whether this is normal GitHub scheduling jitter surfaced a much bigger, real pattern. |
| **The data** | `mcp__github__actions_list` (`list_workflow_runs`, `largo-stress-nightly.yml`) — all 18 runs, `created_at` vs the nominal `06:30 UTC` fire time: runs **#1-14** (2026-08-13 through 08-26) landed **36-90 minutes late** (mean ~55min) — normal GitHub Actions scheduling jitter, consistent day to day. Runs **#15-18** (2026-08-27 through 08-30) landed **5h38m to 12h25m late**: #15 (08-27) +11h21m, #16 (08-28) +12h25m, #17 (08-29) +6h17m, #18 (08-30) +5h38m. This is not noise — it's a clean step-change on a specific date, sustained across 4 consecutive days. |
| **What changed around 2026-08-27** | This DISCOVERY lane and the fleet's other lanes ramped up commit/PR volume sharply starting around that date — this session alone has pushed 15+ PRs in a single day (2026-08-30), and `git log` shows dozens of same-day merges across 9 lanes. Every merge to `main` triggers `verify`/`CodeQL`/`Analyze`/`ecr-push-production` — a large, sustained increase in this repo's total GitHub Actions run volume. GitHub's own documentation states scheduled (`schedule:`-triggered) workflow runs are lower-priority and "may be delayed during periods of high load" on the repo/org's Actions usage — this is presented as a hypothesis consistent with the timing, not confirmed against GitHub's internal queue state (not observable from here). |
| **Why this matters** | A "nightly" stress test that actually fires 6-12 hours after its scheduled time has degraded value as an early-warning signal — a regression introduced overnight (like #3203's `decompose.ts` deletion, which the earlier related finding traced) is discovered a half-day later than the schedule implies, and any human/agent skimming "ran at 06:30, nightly cadence intact" would be trusting a cadence that hasn't actually held for 4+ days. |
| **Scope — one workflow confirmed, not yet checked broadly** | Only `largo-stress-nightly.yml` (a single fixed daily `schedule:` slot, `30 6 * * *`) was checked with this level of detail — its one-fire-per-day shape makes drift highly visible. Two OTHER scheduled workflows (`cron-audit-query.yml`, `off-hours-health.yml`) were spot-checked in an earlier DISCOVERY cycle and looked healthy (running every few hours, all green) — but that check only confirmed they were running and succeeding, not whether THEIR fire times have also drifted from nominal schedule (a multi-hour delay is much less visible on a workflow that already fires every 4-6 hours). Whether this is `largo-stress-nightly`-specific or a repo-wide `schedule:` trigger degradation is unconfirmed. |
| **Why not fixed directly this cycle** | There's nothing in this repo to fix — the delay is in GitHub's own Actions scheduler queue, not in `largo-stress-nightly.yml`'s config (already verified correct: `cron: "30 6 * * *"`, valid, unchanged). The only actionable levers are: (a) reduce the repo's overall scheduled+triggered Actions volume (a fleet-wide velocity/process question, not a code fix), or (b) migrate time-sensitive scheduled jobs off GitHub Actions' `schedule:` trigger onto something with real-time guarantees (e.g. the same EventBridge→Lambda dispatch pattern this repo's own production crons already use per CLAUDE.md, rather than GitHub Actions `schedule:`). Both are process/infrastructure decisions for whoever owns the CI/tooling surface, not a DISCOVERY-lane unilateral build. |
| **Suggested next step** | Whoever owns CI tooling: (1) confirm/deny the GitHub Actions queue-priority hypothesis (GitHub support, or watching whether drift recovers once fleet commit velocity drops), (2) decide whether `largo-stress-nightly` (and any other single-daily-slot scheduled workflow) needs to move off GitHub Actions `schedule:` for time-sensitivity, or whether a multi-hour "nightly" window is acceptable given what it checks. |
| **Status** | OPEN — flagged for the CI/tooling owner, no code change made (there is no code-level fix available) |
