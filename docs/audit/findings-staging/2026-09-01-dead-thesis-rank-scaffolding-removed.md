## 2026-09-01 — [cleanup, 0DTE thesis pipeline] confirmed-dead thesis-rank scaffolding removed (ARCHETYPE_EMOJI, rank-calibration.ts, unused barrel) — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Context** | User asked for a real dead-code audit across the 0DTE stack after raising concern that Cortex/BIE/gates/calibration felt like unnecessary complexity making the codebase hard to work in. Launched a dedicated 8-subsystem hunt-and-adversarially-verify workflow (17 agents) separate from the architecture-redesign audit, specifically to find and remove genuinely unreachable code rather than opine on architecture. This is PR 1 of 6 from that workflow's synthesized removal plan — the confirmed-dead thesis-rank scaffolding. |
| **Root cause** | Three independent pieces of dead code, each independently re-verified against the live repo before removal (not just trusted from the workflow's report): (1) `ARCHETYPE_EMOJI` (`thesis/archetype.ts`) — a pure data constant with zero readers anywhere in the repo; its sibling `ARCHETYPE_LABEL` (same shape, defined 11 lines below) IS genuinely imported and rendered (`ZeroDteCommandPanel.tsx:362`), which rules out "the grep just isn't finding JSX usage" as an explanation for the emoji map's silence. (2) `thesis/rank-calibration.ts` (`analyzeThesisRankCalibration` + 5 supporting exports) — a self-contained, side-effect-free rank-tier calibration module with zero importers outside its own file and its own test. `docs/audit/FINDINGS.md` contains a "FIXED" entry claiming thesis-first data "feeds rank-calibration.ts's live calibration report" — re-checked and traced to a naming collision with the *audit script* `scripts/audit/thesis-rank-calibration.mjs`, which reimplements its own ~15-line WR-bucketing directly against raw `/record` JSON rather than importing this module. That documentation claim does not survive a grep check. (3) `thesis/index.ts` — a barrel re-exporting 20 submodules, with zero importers repo-wide; every real consumer (`pipeline.ts`, `ZeroDteCommandPanel.tsx`, `ThesisRankCard.tsx`, etc.) imports specific submodule paths directly, confirmed by reading their actual import lines. |
| **Evidence** | Independently re-ran an unrestricted, whole-repo grep for every symbol name (not scoped to the thesis directory) immediately before deleting anything: `ARCHETYPE_EMOJI` — one hit, its own definition. `analyzeThesisRankCalibration`/`ThesisRankCalibrationReport`/`ThesisRankBucket`/`ThesisRankRecommendation`/`THESIS_RANK_CALIBRATION_MIN_N`/`THESIS_RANK_CALIBRATION_MIN_DELTA_PTS` — every hit inside `rank-calibration.ts` or `rank-calibration.test.ts`. `thesis/index.ts` importers — zero hits for any import form (`from "./thesis/index"`, `from "./thesis"`, `from ".../zerodte/thesis"`) across `src/`. |
| **Fix** | Deleted the `ARCHETYPE_EMOJI` constant from `thesis/archetype.ts` (kept `ARCHETYPE_LABEL`, which is live). Deleted `thesis/rank-calibration.ts` and `thesis/rank-calibration.test.ts` entirely. Deleted `thesis/index.ts` (the unused barrel) entirely. |
| **What was deliberately NOT changed** | Every other file in `thesis/rails/*`, `pipeline.ts`, `live-pipeline.ts`, `archetype-gates.ts`, `contract-engine.ts` — all confirmed live and load-bearing by the same audit, untouched. The `rank_tier` duplicate-logic divergence between `pipeline.ts`'s inline computation and `live-pipeline.ts`'s `resolveThesisRankTier` (a separate, real bug-shaped finding — the inline version is missing the solo-breakout-corroboration guard) is a distinct, slightly riskier fix (touches production gating logic, not pure deletion) — left for a follow-up PR rather than bundled here. |
| **Blast radius checked** | Grepped every symbol removed across the whole repo (not just `src/lib/zerodte/`) — zero references survive. Confirmed no script (`scripts/audit/*.mjs`), no cron-registry string-key, no dynamic import references any of the three removed files. |
| **Regression guard** | Full suite: `src/lib/zerodte/*.test.ts` + `src/lib/zerodte/thesis/*.test.ts` — 1260 pass / 0 fail / 1 pre-existing skip (Node 20, `--experimental-test-module-mocks`). `npx tsc --noEmit` clean. |
| **Status** | FIXED — PR pending (1 of 6 from the dead-code cleanup plan). |
