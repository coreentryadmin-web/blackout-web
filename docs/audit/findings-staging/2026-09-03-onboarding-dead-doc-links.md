## 2026-09-03 — [FINDING, P3 docs/onboarding] `ONBOARDING.md` pointed at 3 deleted docs and one dead glob, and described adding a cron with a broken "sync to EventBridge" hand-wave — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Continuing this cycle's stale-Railway-doc sweep (after `SDLC_AUTOMATION_PLAN.md` and `HEATMAP_DATA_CONTRACT.md`) into `ONBOARDING.md` itself — the doc that names both of those as pointers, and one of the highest-traffic docs a fresh reader (human or agent) actually opens first. |
| **Dead links, confirmed via direct file checks and `git log --diff-filter=D`** | `docs/BLACKOUT_FULL_AUDIT.md` and `docs/NIGHTS_WATCH.md` were both deleted in `chore(cleanup): Phase 2 dead code removal + doc archive (AWS prep)` (#680) — `NIGHTS_WATCH.md` specifically because the "Night's Watch" product itself was removed and absorbed into Night Hawk (`feat(nighthawk): absorb 0DTE Command from Grid; remove Night's Watch`, confirmed via the matching `feat/nighthawk-absorbs-0dte-remove-nights-watch` branch ref). Both were still referenced in `ONBOARDING.md`'s quick-reference table (§11, "Full system audit") and its doc index (§14, "Architecture/integration" and "Per-tool"). Separately, the `NIGHT_HAWK_AUDIT_*.md` and `docs/api-audit/deep-audit-*.md` globs in §14 match zero files today — the real current Night Hawk audit docs live under `docs/audit/NIGHTHAWK-*.md` (8 files, different naming convention entirely), and the closest live-health docs are `docs/api-audit/PLATFORM-INTELLIGENCE.md`/`SYNTHESIS.md`. |
| **The "Add a cron" row was actively wrong, not just stale-linked** | §11's quick-reference table said: `new api/cron/<x> route + railway.<x>.toml + sync to blackout-infra EventBridge` — vague enough to not be an outright lie, but missing the one concrete, checkable step (`src/lib/cron-registry.ts` registration) that this session's own `cron-writer-target-fresh.ts` fix (`git log` this cycle) and `HEATMAP_DATA_CONTRACT.md` fix both had to go verify by hand. |
| **A near-miss worth recording: the `railway.<x>.toml` files are NOT dead** | Before editing the cron row, checked whether `railway.*.toml` files (still present at the repo root, e.g. `railway.gex-alerts.toml`) were themselves a stale reference like the `HEATMAP_DATA_CONTRACT.md`/`SDLC_AUTOMATION_PLAN.md` cases. They are not: `.github/workflows/railway-cron-config-check.yml` (itself `[DEPRECATED]`, triggers disabled) has its own header comment stating plainly *"TOML schedule catalog files (railway.\*.toml) are still used as the cron schedule source-of-truth for EventBridge sync (blackout-infra), but the Railway-specific validation in this workflow is no longer needed."* `railway.gex-alerts.toml`'s own `cronSchedule = "*/5 11-21 * * 1-5"` matches the live EventBridge rule's schedule exactly. So the TOML file format was deliberately RETAINED as the schedule-definition convention after the Railway decommission — only the Railway-hosted validation/deploy tooling around it is gone. The fix keeps the TOML reference, adds the missing `cron-registry.ts` step, and notes the filename is historical. |
| **Fix** | `ONBOARDING.md` §11: corrected the "Add a cron" row (keeps `railway.<x>.toml`, adds `cron-registry.ts` registration, notes the naming is historical); corrected "Full system audit" to point at `docs/audit/FINDINGS.md` with an explicit note that `BLACKOUT_FULL_AUDIT.md` has no direct successor. §14: dropped the dead `BLACKOUT_FULL_AUDIT.md` reference from "Architecture/integration"; replaced `NIGHTS_WATCH.md`/`NIGHT_HAWK_AUDIT_*.md` in "Per-tool" with the real `docs/audit/NIGHTHAWK-*.md` glob plus a note on why Night's Watch is gone; replaced the dead `deep-audit-*.md` glob in "Live health" with the two docs that actually exist there today. A `grep -rn` for each dead filename across `docs/` and `src/` turned up one more live instance of the same `NIGHTS_WATCH.md` dead link: `docs/README.md`'s own "Features & systems" doc table (a near-duplicate of `ONBOARDING.md`'s doc index) — fixed the same way, pointing at `docs/audit/NIGHTHAWK-MAP.md` with the same removal note. |
| **Blast radius** | Two files (`ONBOARDING.md`, `README.md`), both pure doc-index/reference tables. The other grep hits (`docs/audit/0DTE-SYSTEM-DEEP-AUDIT-2026-07-18.md`, `docs/audit/FINDINGS.md`, `docs/api-audit/PLATFORM-INTELLIGENCE.md`) were checked and are NOT dead links — two are substring false-positives (a filename containing "DEEP-AUDIT", an audit-run label "deep-audit-04" unrelated to the deleted `deep-audit-*.md` glob) and `FINDINGS.md`'s 4 hits are historical mentions inside already-resolved entries, correctly untouched (it's an append-only log, not a live doc index). |
| **Regression guard** | None — documentation-only fix, matching the precedent of this session's other doc-staleness fixes. |
| **Gates** | N/A — no source files changed. `findings-hygiene`/`findings-fold-staging` tests pass. |
