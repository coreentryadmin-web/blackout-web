## 2026-09-02 — [FINDING, P4 docs/onboarding] `SDLC_AUTOMATION_PLAN.md` describes a superseded local-Windows-cron automation scheme, still linked from `ONBOARDING.md` with no deprecation notice — OPEN, write-up only

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Checking for other stale-Railway doc references beyond the already-fixed `ONBOARDING.md` (`FINDINGS.md`, 2026-08-30, "still described Railway as live prod infra"). Most other hits (`CLOUDFLARE_CONFIG.md`, `CLOUDFLARE_SETUP.md`, `PGBOUNCER-SETUP.md`) already self-flag their own staleness with an explicit header note ("Railway decommissioned...", "Railway references in this doc are stale..."). `SDLC_AUTOMATION_PLAN.md` does not — it opens with `## ⚡ AGGRESSIVE MODE — ACTIVE (set 2026-06-25)` and describes its automation as a live, running system, with no deprecation marker anywhere in the file. |
| **Symptom** | `ONBOARDING.md`'s own "Infra" doc list (§ pointing readers to further docs) names `SDLC_AUTOMATION_PLAN.md` alongside `CLOUDFLARE_CONFIG.md`/`CLOUDFLARE_SETUP.md`/`PGBOUNCER-SETUP.md` with no distinction — but unlike those three, this one gives no signal that what follows is legacy. A fresh reader (human or AI) following `ONBOARDING.md`'s own pointer, exactly the failure mode the earlier `ONBOARDING.md` fix already corrected once for Railway specifically, lands on a plan describing infrastructure and a job roster that do not exist today. |
| **What's actually described, and why it's stale** | The doc's "GLOBAL GUARDRAILS" section names the working repo as `C:/Users/raidu/blackout-cron` — an "ISOLATED cron clone" on a **local Windows machine** ("the dir name has NO 'blackout-web' substring, so git runs cleanly via the Bash tool") — and says this local clone "pushes to the same GitHub origin and **Railway still deploys** from `origin/main`." Per this repo's own standing operating notes (CLAUDE.md, and the already-fixed `ONBOARDING.md`), Railway was fully decommissioned 2026-07-25; there is no Railway deploy target anymore, and — separately from the Railway question — this repo's actual automation today runs as hourly-fired Claude Code Remote sessions (this very session included) against cloud environments, not a local Windows-machine cron clone. |
| **Confirmed the described job roster doesn't run today, not just the Railway line** | The plan's own "Log:" convention says every job run appends to `docs/auto/<job>-<YYYY-MM-DD>.md`, and names ten specific jobs: `green-build-test-gate`, `visual-render-sweep`, `error-triage`, `e2e-interaction-sweep`, `security-dep-scan`, `performance-audit`, `accessibility-audit`, `ui-enhancement-audit`, `backlog-groomer`, `api-integration-audit`. `docs/auto/` today contains exactly **three** files, none matching any of those ten names: `data-correctness-2026-08-28.md`, `-08-29.md`, `-08-30.md` — a job type this plan never mentions. So this isn't just a stale mention of a decommissioned host; the entire job roster and schedule this document prescribes has either been replaced by a differently-named, differently-configured automation (the data-correctness cron, or the current agent-fleet lanes this session is itself part of) or simply stopped running, and the doc gives no indication of either. |
| **Why not fixed directly this cycle** | Unlike the earlier `ONBOARDING.md` fix (correcting two isolated stale lines against an otherwise-accurate document), this is a whole-document staleness question: is the described automation fully retired (→ archive/delete or add a clear deprecation banner, same pattern as `PGBOUNCER-SETUP.md`), partially superseded by the current fleet model (→ rewrite to describe what actually runs today), or does some of it still run from a machine/location this sandbox simply can't see (→ leave as-is, just add the missing deprecation note)? That's a real editorial decision about the document's purpose, not a mechanical correction — belongs to whoever owns onboarding/operating docs, per this lane's standing "write up bigger findings rather than unilaterally build/rewrite them" policy. |
| **Suggested next step** | At minimum, add the same kind of header disclaimer `CLOUDFLARE_SETUP.md`/`PGBOUNCER-SETUP.md` already carry, so a reader knows before relying on it. If the ten-job local-cron scheme is confirmed fully retired, consider whether `ONBOARDING.md`'s own doc-list entry should instead point at whatever DOES describe the current automation model (this repo's `CLAUDE.md` audit-lane section, or a fresh doc for the Claude Code Remote fleet), rather than a doc describing a machine and deploy target that no longer exist. |
| **Status** | OPEN — flagged for the onboarding/operating-docs owner; no code or doc change made this cycle. |
