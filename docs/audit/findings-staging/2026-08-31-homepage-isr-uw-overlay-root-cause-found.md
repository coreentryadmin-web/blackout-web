## 2026-08-31 — [FINDING, P3 marketing/public-site] The recurring `expiry-strike` no-store fetch that defeats homepage ISR — root cause now fully traced, call-graph-verified hop by hop — OPEN, write-up only

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this follows up on / corrects** | The 2026-08-31 "CORRECTED, still unresolved" entry above this one retracted an earlier claim that `fetchUwSpotExposuresExpiryStrike` (`unusual-whales.ts:2284`) was the source of a recurring `Error: Page changed from static to dynamic at runtime /, reason: no-store fetch https://api.unusualwhales.com/api/stock/SPX/spot-exposures/expiry-strike?...` — correctly: that specific function really is unreachable from `/`. That correction also checked a second, similarly-shaped function, `fetchUwOdteSpotExposuresByStrike` (`unusual-whales.ts:444`), and stated it "is reachable only via `fetchUwOdteGexLadder`, which itself has zero callers anywhere in the codebase — fully dead." **That statement itself has a gap**: `fetchUwOdteSpotExposuresByStrike` has a SECOND, separate call site the earlier grep missed — `src/lib/providers/spx-odte-uw-ladder.ts:79` — which is very much alive and sits on `/`'s render path. This entry traces that path completely, hop by hop, with every file read directly (not URL/parameter pattern-matching, the exact mistake the earlier retraction was written to correct). |
| **The verified call chain (every hop read directly this pass)** | `src/app/(marketing)/page.tsx:28` — `LandingPage()` calls `await buildPublicGexSnapshot("SPX")` → `public-gex-snapshot.ts:79` — on a `sharedCacheGet` miss, calls `await fetchGexHeatmap(ticker)` → `polygon-options-gex.ts` — **every** return path of `fetchGexHeatmap` (`:2858` mem hit, `:2867` redis hit, `:2886` stale serve, `:3849`/`:3860`/`:3876`/`:3879`/`:3912` fresh-build paths) is wrapped in `finalizeHeatmapForServe(cacheKey, data)` (`:1551-1565`) → for `pruned.underlying === "SPX"` (unconditional, no flag) it calls `await applySpxOdteGexUwOverlay(pruned)` → `spx-odte-gex-uw-overlay.ts:157-209` — after an RTH/0DTE-expiry-exists gate (`:192`, correctly no-ops outside RTH — this is WHY the error is intermittent, not constant), calls `await getSpxOdteScopedUwLadderMap(expiry)` (`:194`) → `spx-odte-uw-ladder.ts:137-152` — a 5-second in-process cache (`SPX_GEX_HEATMAP_CACHE_SEC`, default 5000ms); on a miss calls `fetchSpxOdteScopedUwLadder("SPX", expiry)` (`:148`) → `:117-125` calls `fetchSpxOdteScopedUwLadderRest(sym, expiry)` (`:122`) → `:74-88` calls `await fetchUwOdteSpotExposuresByStrike(sym, expiry)` (`:79`) → `unusual-whales.ts:444-449` calls `uwGetSafe<unknown>(`/api/stock/${ticker}/spot-exposures/expiry-strike`, {"expirations[]": expiry, limit})` → `uwGetSafe` → `uwGet` (`:139-175`) issues `trackedFetch(..., { cache: "no-store", ... })` (`:161`) — the exact URL shape (`spot-exposures/expiry-strike`, `expirations[]=<today>`, `limit=500`) and the exact `cache: "no-store"` directive that trips Next's dynamic-usage detector, inside a call stack that started in `page.tsx`'s own component body. |
| **Why this is airtight this time, not another pattern-match** | Every arrow above was confirmed by opening the named file and reading the named lines — not by matching a URL against a CloudWatch log line (the exact failure mode the prior retraction exists to correct). The RTH/0DTE gate at `spx-odte-gex-uw-overlay.ts:192` also explains the *intermittency* that a pure pattern-match couldn't: the overlay silently no-ops outside RTH (`no_odte_expiry`), so the no-store fetch only fires when (a) today has a live 0DTE expiry AND (b) both `buildPublicGexSnapshot`'s own outer cache (`sharedCacheGet`, TTL set by `CACHE_TTL_SEC` in `public-gex-snapshot.ts`) and the 5s scoped-ladder cache are simultaneously cold at request time — which is exactly the "recurs every 15-45 minutes, not every request" pattern seen in CloudWatch across multiple days. |
| **Blast radius / scope check done this pass** | `buildPublicGexSnapshot` has 3 callers: `src/app/(marketing)/page.tsx` (`revalidate = 3600`, no `dynamic` override — **this is the ISR page actually harmed**), `src/app/(marketing)/tools/gamma-snapshot/page.tsx` (**already `export const dynamic = "force-dynamic"`** — not ISR, so the same no-store fetch there is expected/harmless, not a bug), and `src/app/api/public/gex-snapshot/route.ts` (an API route, no static-generation to defeat). So the actual defect is scoped to exactly one route: `/`. `fetchUwOdteSpotExposuresByStrike`'s only other call site, inside `fetchUwOdteGexLadder` (`unusual-whales.ts:500`), is confirmed to have zero callers repo-wide — that half of the earlier correction's claim holds. |
| **Why not fixed directly this cycle** | The established narrow-fix pattern for this finding family (PR #3202/#3187: thread an optional `next.revalidate` override through exactly one call site) does not scale cleanly here. `finalizeHeatmapForServe` — where the UW overlay is invoked — sits on **every** `fetchGexHeatmap("SPX")` return path repo-wide, serving live member desks (`/heatmap`, `/terminal`, Thermal, Vector, SPX Slayer) that read this exact same function for real-time dealer-gamma data members trade on; those callers correctly want `no-store` freshness and must not be silently opted into a 3600s-cached UW ladder. A safe fix needs to thread an opt-in override through **nine** hops (`page.tsx` → `buildPublicGexSnapshot` → `fetchGexHeatmap` → `finalizeHeatmapForServe` → `applySpxOdteGexUwOverlay` → `getSpxOdteScopedUwLadderMap` → `fetchSpxOdteScopedUwLadder` → `fetchSpxOdteScopedUwLadderRest` → `fetchUwOdteSpotExposuresByStrike` → `uwGetSafe`/`uwGet`) while proving every OTHER caller of the same shared function is unaffected — larger and riskier than the Polygon `resolveSpotSnapshot` half of the original finding, which the prior correction already declined for being a 5-hop shared-infrastructure thread. Getting this wrong risks stale dealer-gamma levels reaching paying members, which is a materially worse outcome than a public marketing page occasionally falling off ISR. |
| **Suggested fix direction** | Two independent options for whoever owns this build path, neither attempted here: (1) give `page.tsx`/`buildPublicGexSnapshot` its own snapshot-building path that skips the UW 0DTE overlay entirely (the public free-tier snapshot arguably doesn't need overlay-precision King alignment the way a paying member's live desk does — check with product before assuming), avoiding the shared-code-path risk altogether; or (2) thread the opt-in `next.revalidate` override the full nine hops, defaulting to the current `no-store` behavior everywhere, and add a test asserting every non-homepage caller of `getSpxOdteScopedUwLadderMap`/`fetchGexHeatmap("SPX")` still observes `no-store` — the regression that would matter here is silent staleness on a member-facing desk, not a compile error. |
| **Status** | OPEN — root cause now fully confirmed and documented for the owning lane; no code change made this cycle |
