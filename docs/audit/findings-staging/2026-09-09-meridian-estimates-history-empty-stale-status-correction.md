## 2026-08-18 — Meridian ESTIMATES/HISTORY are empty on EVERY mega-cap while the same payload claims history — CORRECTED: already fixed same-day, still marked OPEN 3+ weeks later

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | DISCOVERY-cycle re-verification of older `FINDINGS.md` claims (standing instruction: never trust a status heading, verify against `main` directly). `FINDINGS.md`'s 2026-08-18 P1 entry "Meridian ESTIMATES/HISTORY are empty on EVERY mega-cap..." is still headed `OPEN`, with `Status: CONFIRMED + REPRODUCIBLE; validator shipped. Root cause NOT yet proven`. |
| **Re-verification (live, 2026-09-09)** | Read the current `loadMeridianEarningsEnrichment` (`src/lib/meridian/meridian-earnings-enrich.ts`) — its `.catch()` shapes no longer match the finding's described root cause (a six-way `Promise.all` where every branch silently caught to `{rows: [], entitled: true, error: "cache_error"}` and cached the empty result for 10 minutes). Pulled a real live `GET /api/market/meridian/event?id=earnings:TGT:2026-11-18` (the same TGT ticker the original finding measured) through a temp premium Clerk session: `enrichment.print_history` **8 rows** (not 0), `enrichment.street_estimates` **4 entries** (not empty), `enrichment.earnings_calendar` **fully populated** (not null), `enrichment.beat_rates` **real numbers** (75% EPS beat rate / 50% revenue / 63% combined — not null). The self-contradiction the finding described (`pack.history` populated, `enrichment.*` empty on the identical response) no longer reproduces — `pack.history` carried 6 rows alongside the now-populated `enrichment` block. |
| **Root cause of the staleness** | Traced via `git log --oneline -- src/lib/meridian/meridian-earnings-enrich.ts`: commit `7b3fb4d80` — **"fix(meridian): stop caching Benzinga failures, and make an outage say so"** — is dated **2026-08-18 07:24:27 UTC, the SAME DAY the finding was logged**, and its own commit message quotes the identical measured symptom ("8/8 mega-caps served enrichment.print_history 0 rows... while pack.history on the SAME response carried 4 prints"). The finding was almost certainly fixed within hours of being found — but the `FINDINGS.md` entry's heading/status was never updated to reflect it, leaving a P1 "OPEN, root cause NOT yet proven" claim sitting unnoticed for 3+ weeks. |
| **Correction** | Status should read **FIXED** (commit `7b3fb4d80`, 2026-08-18), not `OPEN`. The "root cause NOT yet proven" line is also stale — the fix commit's own message states the mechanism plainly (a failed Benzinga fetch was cached as an empty success for 10 minutes; the fix stops caching the failure and surfaces the outage instead), so the root cause WAS established, just not reflected back into this entry. |
| **Not re-investigated further** | The separate "A separate FAIL from the same run: TGT call_wall 150 is below put_wall 152.5" note at the bottom of the original entry is a distinct GEX-wall-inversion claim, not covered by this correction — out of scope for this pass; flagging for whoever owns that thread to verify separately if not already resolved. |
| **Status** | Documentation-only correction. No code change — the underlying bug is already fixed. |
