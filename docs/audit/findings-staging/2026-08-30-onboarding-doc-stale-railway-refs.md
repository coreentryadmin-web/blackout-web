## 2026-08-30 — [FINDING, P3 docs/onboarding] `ONBOARDING.md` still described Railway as live prod infra, contradicting its own correct sections — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Symptom** | `docs/ONBOARDING.md` — explicitly addressed to *"any engineer or AI assistant (Cursor, Claude) coming to this codebase fresh"* — carried two lines describing Railway as the current production platform, contradicting the document's own correct description elsewhere in the same file: §9 ("Hosting/CI") and §11 correctly state **AWS ECS Fargate**, ECR, EventBridge, and CloudWatch `/ecs/blackout-production` as the live infra; Railway was fully decommissioned per this repo's own standing operating notes. |
| **Concrete inconsistency #1** | The routes table said `cron/` routes are "hit by Railway cron services via Bearer `CRON_SECRET`" — two sections later (§8), the same document correctly says "EventBridge rules in AWS invoke Lambda `blackout-production-hit-cron`, which `GET`s each path with Bearer `CRON_SECRET`." The routes-table line was simply never updated when §8 was. |
| **Concrete inconsistency #2** | §13 ("Running locally") told a fresh reader they need ".env.local with... the public Railway proxy URLs" and a `RAILWAY_TOKEN` "for prod inspection" — actionable, wrong guidance: there is no Railway proxy to fetch a URL from anymore, and prod inspection goes through AWS (CloudWatch/ECS console), exactly as §11's "Debug a slow/red deploy" row already correctly states. |
| **What was NOT touched** | Two other `railway.*` mentions in the same doc (the `railway.<name>.toml` schedule-config files synced into `blackout-infra` via `sync-cron-schedules.mjs`, §8 and §11) are accurate and left alone — those files genuinely still exist at the repo root (confirmed: 40+ `railway.*.toml` files tracked in git) and remain the real source-of-truth format for cron schedule expressions, a naming convention that survived the platform migration rather than a live Railway dependency. Deleting or "fixing" those would have been wrong; they were correctly identified as accurate before editing anything. |
| **Fix** | Corrected both stale lines to match the document's own already-accurate §8/§9/§11 language: the routes table now says "hit by the AWS EventBridge → Lambda (`blackout-production-hit-cron`) dispatcher"; §13 now says local dev needs `.env.local` values from a teammate and that prod inspection goes through CloudWatch/ECS, pointing back to §9/§11. |
| **Why this matters** | This doc's own header states its purpose is to tell a fresh reader — human or AI — "where to look and what to look for before touching anything." An agent following its literal instructions today would go looking for a Railway proxy URL and a `RAILWAY_TOKEN` that don't exist, for a platform this repo's own CLAUDE.md operating notes explicitly confirm was "fully decommissioned." |
| **Gates** | Text-only change; no code, no tests to run. Verified no other doc-content test references `ONBOARDING.md` (repo-wide grep of `src/*.test.ts`). |
| **Status** | FIXED |
