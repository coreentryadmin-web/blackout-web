## 2026-08-30 — [FINDING, P4 docs] `API_INTEGRATION_MAP.md`'s Infra section still described production Redis/Postgres as Railway, with no staleness warning — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Symptom** | `docs/API_INTEGRATION_MAP.md`'s "Infra" section heading and its `✅ USED` line described production Redis as "Railway IPv6 internal DNS" with no qualification — factually wrong today. Railway was fully decommissioned 2026-07-25 (per this repo's own standing operating notes): production Redis is AWS ElastiCache (`blackout-production-redis-rg`), Postgres is AWS RDS (`blackout-production-postgres`). |
| **Why this is the same bug class as the ONBOARDING.md fix earlier today** | Unlike `docs/CLOUDFLARE_SETUP.md` and `docs/PGBOUNCER-SETUP.md`, which already carry an explicit "Railway decommissioned, these references are stale" note at the top, this doc had NO such warning — a reader hits "Railway IPv6 internal DNS — load-bearing, do not remove" presented as current, live guidance. |
| **Why it's still a smaller/different finding, not a rewrite** | This doc explicitly dates itself ("Last full pass: 2026-06-26," "maintained by the `api-integration-audit` job (daily)") — closer to a point-in-time audit-pass report than a "read this before touching anything" onboarding doc. The `family:0` IPv6 setting, the `'error'`-listener/Pool/query-parameterization behavior, and the historical "Fixed this pass" changelog line (`49cb17d`) are all left untouched — they're either still accurate today or correctly describe what was true AT THE TIME of that historical fix, not current-state claims. Only the section title and the one current-state "Railway IPv6 internal DNS" claim were actually wrong. |
| **Fix** | Removed "Railway" from the section title (now "Infra — Redis (ioredis 5.11.1) · Postgres (pg 8.21.0)"), added a brief staleness note pointing to the actual current infra and cross-referencing the same note already established in `CLOUDFLARE_SETUP.md`/`PGBOUNCER-SETUP.md`, and dropped "Railway" from the "IPv6 internal DNS" line. Left the historical "single-node Railway Redis" changelog line alone — explicitly noted why in the new staleness callout, so a future reader doesn't "fix" it again incorrectly. |
| **Gates** | Text-only doc change; no code, no tests to run — matches `docs/audit/findings-staging/2026-08-30-onboarding-doc-stale-railway-refs.md`'s gate pattern from earlier this session. |
| **Status** | FIXED |
