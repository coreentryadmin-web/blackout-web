## 2026-08-30 — Meridian `earnings_week_analytics` swallowed-failure finding — CORRECTED: duplicate entry, the bug was already fixed the same day (PR #3184)

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | DISCOVERY-cycle re-verification of older `FINDINGS.md` claims, continuing from the 2026-08-18 Meridian estimates/history stale-status correction filed earlier this session. |
| **The problem** | `FINDINGS.md` carries **two entries for the identical bug**, back to back: `## 2026-08-30 — [FINDING, P3 Meridian] earnings_week_analytics's historical-prints fetch is silently swallowed on failure — same design defect as the 2026-08-18 finding, different field — OPEN` (status: `OPEN — flagged for the Meridian lane, no code change made`), immediately followed by `## 2026-08-30 — [FINDING, P3 Meridian] earnings_week_analytics historical fetch silently swallowed on failure — FIXED` (status: `FIXED`, describing the exact same root cause in `loadBenzingaEarningsBundle` and the exact same suggested fix shape — threading an `earnings_week_analytics_error` field through the bundle). |
| **Verification** | Confirmed the fix is genuinely live in the current source, not just claimed: `earnings_week_analytics_error` is threaded end-to-end — `src/lib/meridian/meridian-benzinga-earnings.ts` (captures `hist.error`, lines 94/107/143/159) → `src/lib/meridian/meridian-snapshot.ts:86` → `src/lib/meridian/meridian-timeline-server.ts:88-90,216` → `src/features/meridian/lib/meridian-types.ts:889`. Traced to `git log`: commit `8b5add376`, **"fix(meridian): surface week analytics historical fetch failures" (PR #3184)**, dated 2026-08-29 22:23 PT — the evening before the OPEN entry's own 2026-08-30 date stamp. |
| **What happened** | The write-up-only `OPEN` entry was filed correctly per this repo's own "write up bigger findings, don't build unilaterally" policy — but the Meridian lane picked it up and shipped the fix almost immediately (same day or the evening before, going by commit vs. entry timestamps), and whoever landed the fix added a fresh `FIXED` entry documenting it rather than updating the original `OPEN` entry in place. Both entries have sat side-by-side since, with the stale `OPEN` one never removed or corrected — exactly the same "fixed but never marked" shape as the 2026-08-18 estimates/history finding corrected earlier this session, just a duplicate-entry variant instead of a single-entry stale-status variant. |
| **Correction** | The `OPEN` entry (`## 2026-08-30 — ...same design defect as the 2026-08-18 finding, different field — OPEN`) should be marked superseded by / merged into its own `FIXED` sibling immediately below it, or removed outright as a duplicate — whichever the coordinator's fold script handles more cleanly. No code change needed; the underlying bug is already fixed and already correctly documented once. |
| **Status** | Documentation-only correction. No code change. |
