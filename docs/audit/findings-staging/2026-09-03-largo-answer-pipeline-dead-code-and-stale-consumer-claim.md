## 2026-09-03 — [FINDING, P4 dead-code/doc-staleness, Largo] 3 complete answer-pipeline modules never wired in, `product-read.ts`'s own builder/validator unused, and a FINDINGS.md entry overstates one as a "live consumer" — write-up only, not fixed

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Standing dead-code/stale-docs sweep, deliberately scoped away from the already-documented `compose*Read` wiring gap (`docs/audit/FINDINGS.md`'s BIE composer-family entry) to find a fresh angle in `src/lib/largo/`. |
| **Genuinely dead code confirmed** | Three complete, individually-tested modules in `src/lib/largo/` have ZERO importers anywhere in `src/` outside their own file and their own test file — confirmed by grepping both the filename and every exported symbol name, with no dynamic `import()`, no barrel/index re-export, and no load-bearing `readFileSync` of the source text (the specific trap that made an earlier `bie/composers.ts` cleanup non-trivial): `follow-up-question-generator.ts` (`suggestFollowUpQuestions`/`formatFollowUpSuggestions`), `prose-template-builder.ts` (`getProseSectionsForIntent`/`formatProseDirectives`), `response-builder.ts` (`buildResponseComponents`/`formatComponentsAsMarkdown`). Additionally, `src/lib/largo/contract/product-read.ts`'s own envelope constructor/validator functions (`productRead`, `productUnavailable`, `contractViolations`, `signalViolations`) are unused — only that file's *types* (`ProductRead`, `Direction`, `ProductSignal`, `canonicalTicker`) are genuinely consumed elsewhere (`cross-product.ts`, `product-adapters.ts`). |
| **Why NOT deleted outright (unlike the earlier `bie/composers.ts`/`decompose.ts`/`dynamic-format.ts` precedent)** | That earlier cleanup (`docs/audit/FINDINGS.md`, "confirmed genuinely dead and deleted") was SUPERSEDED legacy code — an old router path the live system had moved on from. These four are different in kind: all are complete, passing-tests, forward-looking scaffolding for enhancing the Largo answer pipeline (follow-up-question suggestions, prose-section templating, structured response components, and the product contract's own reference envelope-builder), and two of the three self-describe in their own header comments as demonstrating an intended integration ("Example workflow: ... buildResponseComponents(enriched, question) ..."). This is the same shape as the already-written-up `compose*Read` wiring gap (10 of 12 composer functions built but unreachable) — real, deliberate groundwork sitting one integration step away from being live, not stray dead accidents. Deleting completed, forward-looking work is exactly the kind of unilateral architectural call the standing issue-handling policy reserves for a write-up, not a small self-contained fix — a coordinator or the lane that built this should decide whether to wire it in or retire it. |
| **Stale doc claim found in the same sweep** | `docs/audit/FINDINGS.md`'s "Largo: unanimous or single-system 'neutral' reads were reported as a fabricated cross-product 'conflicted' verdict — FIXED" entry (search: `consensus-read-extract.ts`) lists `follow-up-question-generator.ts` as one of four live consumers of `extractConsensusFromTools`'s output that "would render/reason from a false 'systems disagree' narrative" from the bug it fixed. That is true only as a TYPE-ONLY import (`import type { ConsensusMatrix } from "./consensus-read-extract"`) — the module itself is unreachable dead code per the finding above, so it never actually rendered or reasoned from anything, live or buggy. The other three named consumers (`visual-component-builder.ts`, `adaptive-response-orchestrator.ts`, `desk-read-decision.ts`) are genuinely live and the claim holds for them. This does not change that entry's `FIXED` status or its real fix — only its stated blast radius overstates live impact by one file. Per this repo's `docs/audit/findings-staging/README.md`, `FINDINGS.md` is never edited directly; this entry documents the correction for the coordinator's next fold/reconcile pass rather than editing it here. |
| **No action taken** | This is a documentation/write-up finding, not a fix — no branch, no deletion, no code change. Flagging for a coordinator or the owning Largo lane to decide: wire the three answer-pipeline modules and `product-read.ts`'s builder/validator into the live tool-calling surface (following the same integration `compose*Read`'s two wired functions already demonstrate), or retire them explicitly if the pipeline direction has changed since they were written. |
