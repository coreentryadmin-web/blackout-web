## 2026-09-01 — [cleanup, 0DTE discovery/breakout] dead discovery-events scaffolding + breakout helpers removed — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Context** | PR 5 of 6 from the dedicated dead-code cleanup audit. |
| **Root cause** | Four independent pieces of dead code: (1) `discovery-events.ts` (44 lines: `DiscoveryEventKind`, `DiscoveryEventPayload`, `discoveryObjectId`, `buildDiscoveryEvent`) — a Phase-1 event-sourcing scaffold **never imported anywhere in the repo** (not just unused exports — no file imports this module by path at all). Actively superseded, not just unused: `discovery-events-persist.ts` (the module actually wired into `scan.ts`) defines its own byte-identical private `objectId(sessionDate, ticker)` at line 44 rather than importing this file's `discoveryObjectId` — proof the real implementation was built independently and this file was never wired in. (2) `persistDiscoveryEventsFromScan()` — an unused bundling convenience wrapper; `scan.ts` imports and calls its three sub-functions (`persistDiscoveryDetectedEvents`, `persistDiscoveryGateBlockedEvents`, `persistDiscoveryCommitEvents`) individually at 3 separate pipeline points instead, each with its own `.catch()`. (3) `freshestMinuteBarMs()` (`breakout-intraday-breadth.ts`) — the module's two real production importers, `breakout-discovery.ts` and `swing-discovery/route.ts`, both import only `buildSessionBarFromMinuteBars`/`mergeMinuteRefreshedBars`, confirmed by reading their exact import blocks. (4) `BREAKOUT_DYNAMIC_CAP_DISABLED` (`breakout-cap.ts`) — a stale module-eval-snapshot const, its own doc comment already says "kept for back-compat... prefer `resolveBreakoutDynamicCapDisabled`"; the sole production consumer (`breakout-discovery.ts`) imports only `resolveBreakoutCandidateCap`, which resolves the disabled-state via the call-time function, never this const. |
| **Evidence** | Whole-repo grep for every symbol name (re-run immediately before deleting): `discovery-events.ts`'s exports and the file's own specifier (`discovery-events"`) — zero hits anywhere outside the file itself. `persistDiscoveryEventsFromScan` — one hit, its own definition. `freshestMinuteBarMs` — hits only in its own file + own test; confirmed both real importers' exact import lists exclude it. `BREAKOUT_DYNAMIC_CAP_DISABLED` — the only other repo hits are `breakout-cap.test.ts` lines that pass `{ BREAKOUT_DYNAMIC_CAP_DISABLED: "1" }` as a mock **env object property** to `resolveBreakoutDynamicCapDisabled()` (the env var name as a string key), never an `import` of the actual exported const binding — confirmed by reading the test file's import line, which pulls only `resolveBreakoutCandidateCap, resolveBreakoutDynamicCapDisabled`. The live function still reads the real env var by name at call time (`env.BREAKOUT_DYNAMIC_CAP_DISABLED`), completely unaffected by removing the stale const. |
| **Fix** | Deleted `discovery-events.ts` entirely. Removed `persistDiscoveryEventsFromScan()` from `discovery-events-persist.ts` (the 3 individual persist functions it wrapped are untouched, still called directly by `scan.ts`). Removed `freshestMinuteBarMs()` from `breakout-intraday-breadth.ts` and its dedicated test. Removed the stale `BREAKOUT_DYNAMIC_CAP_DISABLED` const from `breakout-cap.ts` — this left its sole helper, the private `envFlag()`, with zero remaining callers, so removed that too (a direct, in-scope consequence of this same removal, not a separate finding). |
| **What was deliberately NOT changed** | `resolveBreakoutDynamicCapDisabled()` and its live env-var-string read (`env.BREAKOUT_DYNAMIC_CAP_DISABLED`) — the actual, still-functioning kill-switch, completely untouched; only the redundant stale const binding of the same name went. `persistDiscoveryDetectedEvents`/`persistDiscoveryGateBlockedEvents`/`persistDiscoveryCommitEvents` — all three real, called directly from `scan.ts`, untouched. `buildSessionBarFromMinuteBars`/`mergeMinuteRefreshedBars` — live, untouched. |
| **Blast radius checked** | Grepped every removed symbol across the whole repo — zero surviving references. Confirmed `swing-discovery/route.ts` (the second real importer of `breakout-intraday-breadth.ts`) has no dependency on `freshestMinuteBarMs` by reading its import block directly. |
| **Regression guard** | `discovery-events-persist.test.ts` (3/3), `breakout-cap.test.ts` (13/13), `breakout-intraday-breadth.test.ts` (2/2), `breakout-discovery.test.ts` (14/14) — 32/32 pass individually and combined. Full `src/lib/zerodte/*.test.ts`: 1211 pass / 0 fail / 1 pre-existing skip (Node 20). `npx tsc --noEmit` clean. |
| **Status** | FIXED — PR pending (5 of 6 from the dead-code cleanup plan). |
