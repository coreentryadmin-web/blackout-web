## 2026-09-16 — [FINDING, P3 correctness — self-correction] `composeSpxDeskBrief` is dead code; two prior claims of a live caller were both wrong — CORRECTED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this resolves** | Corrects two independent, wrong claims that `composeSpxDeskBrief` (`src/lib/bie/spx-desk-brief.ts`) is live/member-facing: (1) this session's own PR #5051 (merged 2026-09-15, commit `5348b797`) fixed a real bug in that function and described it as "the deterministic Live Desk AI brief text members read on `/api/market/spx/desk`" — that characterization is wrong; (2) `docs/audit/FINDINGS.md`'s existing "Cascading orphan" entry (line ~14517, dated before this correction) states `composeSpxDeskBrief` "has a separate, genuinely live caller (`src/features/spx/lib/spx-commentary.ts`)" — also wrong, and itself an instance of the exact staleness trap `CLAUDE.md`'s Ask Largo section warns about repeatedly. |
| **Root cause** | Repo-wide grep (`grep -rn "composeSpxDeskBrief" src/ --include="*.ts" --include="*.tsx"`, excluding test files) shows the function has **zero callers**: the only non-test hits are its own `export function` declaration and three comments in `spx-commentary.ts`/`vector-desk-brief.ts` that *mention* it by name while describing the newer replacement composition (`generateSpxCommentary`, `src/features/spx/lib/spx-commentary.ts` — its own header explicitly calls the older one "the old composition (composeSpxDeskBrief)... This rewrite serves EXACTLY three things..."). The 2026-07-13 redesign that shipped `generateSpxCommentary` retired the caller but never removed the retired function itself, and it was never added to `src/repo-hygiene.test.ts`'s `known-orphaned modules` allowlist — so nothing in the repo's own tooling ever flagged it as dead. |
| **Evidence** | `grep -rn "composeSpxDeskBrief(" src/ --include="*.ts" \| grep -v "\.test\.ts"` returns only the function's own definition (`spx-desk-brief.ts:438`) — no call site anywhere in `src/app/` or elsewhere. Cross-checked the actual live path: `/api/market/spx/commentary` (`src/app/api/market/spx/commentary/route.ts`) calls `generateSpxCommentary`, which composes via `src/lib/bie/spx-live-voice.ts`'s `voiceSnapshotFromDesk`/`composeBiasVoice` — a completely separate code path that already handles the unresolved-gamma-flip case correctly: `aboveFlip: price != null && flip != null ? price >= flip : null` (line 186) returns `null`, not a defaulted `false`, and every downstream consumer (`s.aboveFlip === true`/`=== false` checks) treats `null` as "say nothing" rather than "confirmed negative gamma" — the live path never had the bug PR #5051 fixed. |
| **What's still true** | PR #5051's fix itself is real, correct, and harmless — `gammaTag()`/`buildWhy()`/the `NEXT 5M` ternary in `spx-desk-brief.ts` genuinely did conflate "flip unresolved" with "confirmed short gamma" before the fix, proven RED→GREEN. It just never shipped to any member because nothing calls the function it lives in. No user-facing regression exists to worry about either way. |
| **Follow-up (not done here, scope discipline)** | `spx-desk-brief.ts` (and its test file) look like a genuine dead-code removal candidate now that this is confirmed, but two of its exported types (`SpxDeskBriefCross`) are still imported (type-only) by `spx-desk-synthesis.ts`/`spx-invalidation.ts`, so removal needs to either inline those types or verify THEY are also unused before deleting the file outright — left as a separate, deliberately-scoped follow-up rather than bundling a deletion into a correction PR. |
| **Fix rationale for this entry** | Per `CLAUDE.md`'s own established pattern (the Ask Largo section's repeated self-corrections), a stale claim about what shipped/is-live gets corrected against actual source, not left to compound — this entry documents the correction for both PR #5051's own write-up and the pre-existing `FINDINGS.md` cascading-orphan entry, so a future session reading either doesn't repeat the same wrong "live caller" claim. |
| **Status** | CORRECTED (documentation-only; no code or gate changed). |
