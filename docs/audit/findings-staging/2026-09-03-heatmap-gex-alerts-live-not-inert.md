## 2026-09-03 — [FINDING, P3 docs/Thermal, live-feature-status] `HEATMAP_DATA_CONTRACT.md` described web-push GEX regime alerts as pending platform/infra activation — the feature is fully live in production, verified directly — FIXED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What prompted this** | Sweeping `ONBOARDING.md`'s "Infra" doc list for other stale-Railway references beyond `SDLC_AUTOMATION_PLAN.md` (this cycle's other fix). `HEATMAP_DATA_CONTRACT.md` isn't in that specific doc list, but a broad `grep -i railway` across `docs/` turned it up: a "Platform follow-ups to make web-push LIVE" checklist describing 3 activation steps as outstanding, one of them ("Register the cron schedule (railway)") naming a `railway.gex-alerts.toml` file — the same decommissioned-infra reference pattern already fixed twice elsewhere this session. |
| **Root cause / what was actually stale** | Beyond the Railway terminology, the checklist's underlying CLAIM was itself out of date: it described VAPID keys, `GEX_ALERTS_PUSH`, and cron registration as pending "PLATFORM-OWNED"/"INFRA-OWNED" work with the cron "inert-by-default," when in fact all three were already done in production. |
| **Verified live, not assumed** | (1) `web-push` is an installed dependency (`package.json:189`). (2) `blackout-production/app/env` (Secrets Manager) has `GEX_ALERTS_PUSH=1` and all three VAPID keys (`NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`) populated — checked via `boto3` `get_secret_value`, values masked/only lengths logged. (3) The EventBridge rule `blackout-production-gex-alerts` exists, is `State: ENABLED`, `ScheduleExpression: cron(*/5 11-21 ? * MON-FRI *)` — checked via `boto3` `events.describe_rule`, matching the doc's own "~every 5 min during market hours" description. (4) `src/lib/cron-registry.ts` already carries a `"gex-alerts"` entry (`market_hours_only: true`, description noting the same VAPID/`GEX_ALERTS_PUSH` gate) — the app-side mirror of the same registration. Cross-checking the route's own gate logic (`src/app/api/cron/gex-alerts/route.ts:110-111`, `GEX_ALERTS_PUSH === "1" \|\| "true"` AND `vapidConfigured()`) against these live values confirms the cron runs in its ACTIVE branch, not the `"inert (GEX_ALERTS_PUSH + VAPID)"` skip branch the doc implies is still current. |
| **Why this matters beyond terminology** | Unlike a pure "Railway→AWS" rename, this is a live-feature-status defect: the doc tells a reader (human or AI) that member-facing GEX regime web-push alerts (SPY/SPX/QQQ flip/wall-break broadcasts) are NOT yet active and require platform work to enable — when the feature is actually running and pushing real alerts to real subscribers today. A reader trusting the doc could either waste effort "activating" an already-active feature, or dismiss a real support report ("why did I get a push alert?") as impossible. |
| **Fix** | Corrected the inline schedule bullet to describe the actual EventBridge registration (rule name, cron expression, `blackout-infra` terraform + `cron-registry.ts` mirror) instead of the Railway TOML mechanism. Added a dated "verified live" note above the "Platform follow-ups" checklist, struck through items 1–3 as DONE with the verifying evidence inline, and updated the status table to reflect the live state — leaving item 4 (the per-ticker/per-user subscription model, a real still-open UI+data-model follow-up) exactly as before, since that one genuinely isn't done. |
| **Blast radius** | Single file, prose-only. No source code touched — this is a status-accuracy correction, not a behavior change; the feature's actual behavior (broadcast-to-all push, not per-user) is unchanged and was already correctly described for item 4. |
| **Regression guard** | None — documentation-only fix, matching the precedent of this session's other doc-staleness fixes (no test added for those either). |
| **Gates** | N/A — no source files changed. |
