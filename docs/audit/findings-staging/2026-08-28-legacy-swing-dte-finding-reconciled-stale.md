## 2026-08-28 — [FINDING, reconciliation] The 2026-08-06 P1 "Legacy→Swing promotion admits dte<5 contracts" finding is now STALE — the described gap does not exist in current code — CLOSED, no code change

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **Original finding** | `docs/audit/FINDINGS.md`, 2026-08-06, `[P1, NOT FIXED — flagged, architecturally significant] Legacy→Swing promotion path admits dte<5 contracts, reopening the dual-admission gap for a SECOND code path`. Claimed `legacy-confirm-promote.ts` never checks its picked contract's DTE against `HORIZONS.SWING.dteMin`/`SWING_SUB_LANES` before commit, evidenced by 5 live tickers (CRWV, SKHY, RDDT, NVDA, AMD) committed at dte 3–4 on the Swing board that day. Deferred fixing at the time because a separate in-flight PR (#1782, on this exact branch name `claude/wall-beads-data-validation-4re5wo`) was working the same admission-boundary surface. |
| **Why revisited today** | Standing "findings are work, not a graveyard" policy — re-verify old flagged-not-fixed entries rather than letting them rot unconfirmed. #1782 is now `closed, merged: false` (dead, no conflict risk), so this was the first opportunity to safely act on the entry's own "once #1782 lands, re-check" instruction. |
| **What re-investigation found** | The finding's root-cause reasoning ("confirmed by grep: zero `gate`/`taxonomy` imports in the file") was too narrow — it checked whether `legacy-confirm-promote.ts` imports `gates.ts`/`taxonomy.ts` directly, but missed that the file's own contract-picking call, `produceHorizonPlays()` (`src/lib/horizon-plays.ts`) → `fanOutChain()` → `fanOutContracts()` (`src/lib/horizon-fanout.ts:197`), **already filters every horizon's candidate contracts to `c.dte >= spec.dteMin && c.dte <= spec.dteMax`**, where `spec = HORIZONS[horizon]` — the SAME `HORIZONS.SWING.dteMin` the original 2026-08-06 `aef8d49b` fix set to 5. This filtering is generic and applies to EVERY caller of `produceHorizonPlays`, including the Legacy-promotion path, regardless of whether that caller imports `gates.ts` itself. |
| **Evidence — direct empirical test, not just a code read** | Called the real, unmodified `produceHorizonPlays()` with a synthetic ticker whose only two chain rows expire 3–4 calendar days out (below the 5-DTE floor): `set.SWING` returned `[]` (empty — no contract admitted). Traced the consuming code: `legacy-confirm-promote.ts:202-203` — `const swingPlay = playSet.SWING[0]; if (!swingPlay) return null;` — and the caller (`promoteConfirmedLegacyToSwing`) treats a `null` artifact as `skipped++`, not a commit. So a ticker whose nearest listed expiry is entirely below the Swing floor is cleanly SKIPPED end-to-end, not admitted with a bad DTE. |
| **Live confirmation** | `GET /api/market/nighthawk/horizons?view=swings` today (2026-08-28): all 19 current SWING plays (committed + watch) carry `contract.dte` between 6 and 29 — none below the 5-DTE floor. `git log -- src/lib/swing/legacy-confirm-promote.ts` shows no commit since 2026-08-06 touched DTE filtering directly (the file's own code — the hardcoded `intendedDte: 14` cosmetic label, no `gate`/`taxonomy` import — is unchanged), consistent with the fix being a SIDE EFFECT of `aef8d49b`'s shared `HORIZONS.SWING.dteMin` change rather than a direct edit to this file. |
| **Why the original finding's evidence (5 live dte-3/4 tickers) doesn't contradict this** | Those were captured 2026-08-06, the same day `aef8d49b` shipped — most plausibly either (a) committed before that fix's `HORIZONS.SWING.dteMin=5` change was live/deployed, or (b) admitted via a code path/cached dossier that predates the shared fan-out enforcement. Either way, they are not reproducible against current `main`. |
| **Status** | CLOSED as STALE/ALREADY-RESOLVED — no code change in this PR, doc-only. `legacy-confirm-promote.ts`'s cosmetic `intendedDte: 14` hardcode (used only for a display-label `subLaneForDte(14)` call, not for gating) is still slightly misleading and could be cleaned up as a P3 follow-up, but it no longer represents a real admission-control gap — the actual gate is the shared `fanOutContracts` window filter, confirmed protecting this path today. |
