## 2026-08-31 — [FINDING, P4 audit-hygiene] The Meridian `earnings_week_analytics` swallowed-error finding (2026-08-30) is marked OPEN but was fixed the same day one entry later — CORRECTED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this corrects** | `docs/audit/FINDINGS.md`'s 2026-08-30 entry **"[FINDING, P3 Meridian] `earnings_week_analytics`'s historical-prints fetch is silently swallowed on failure... — OPEN"** (heading + its own `**Status**` row both say OPEN, "flagged for the Meridian lane, no code change made"). The very next entry in the same file, same date — **"[FINDING, P3 Meridian] `earnings_week_analytics` historical fetch silently swallowed on failure — FIXED"** — describes and closes exactly the fix the OPEN entry's own "Suggested fix shape" proposed: capture `hist.error` into an `earnings_week_analytics_error` field, thread it through the bundle → timeline-server → snapshot → UI, and render an error state instead of a bare beat-rate-less headline. The OPEN entry was never updated to point at it. Same shape as the session-anchor `nowMs` self-correction earlier in this same file (and the one this session already corrected in PR #3249) — a real fix landed and was documented, just a heading-and-two-entries away from the claim it was still open. |
| **Confirmed live in the current codebase this pass, not just taken on the FIXED entry's word** | `src/lib/meridian/meridian-benzinga-earnings.ts:93-107` — the historical-prints fetch's `.catch()` now returns `{ rows: [], entitled: true, error: "cache_error" as const }` (not the old bare `{rows: []}}`) and captures it as `earnings_week_analytics_error = hist.error ?? null`, returned alongside `earnings_week_analytics` in the bundle (line 142-143). Threaded through `meridian-timeline-server.ts:88-90,216` and `meridian-snapshot.ts:86` into `MeridianTimelinePayload` (`meridian-types.ts:889`), and consumed on the UI side by `MeridianDesk.tsx`/`MeridianEarningsTabs.tsx` (both reference the field / the `meridian-feed-error` banner it drives). |
| **What was NOT independently re-verified this pass** | The FIXED entry's own "Evidence" field already noted "live validation deferred to Monday RTH (week grid populated off-hours)" and grepping for a dedicated unit test asserting on `earnings_week_analytics_error` found none — the FIXED entry's evidence was type-compile + the existing (unrelated) Meridian analytics test suite passing, not a test pinned to this specific field. This correction confirms the WIRING is genuinely present end-to-end (the OPEN entry's core claim — "no code change made" — is definitely false), not that a dedicated regression test exists for it; that gap, if worth closing, is a smaller, separate follow-up for the Meridian lane, not restated as part of this hygiene correction. |
| **Why this is a hygiene correction, not a new investigation** | Same shape as the immediately-preceding self-corrections already in this file (SPX Slayer Largo coverage; the session-anchor `nowMs` finding this session corrected in PR #3249): the fix and its evidence already exist in `FINDINGS.md` itself, one heading below — this doesn't add new evidence about the fix's existence, it points the OPEN entry at the FIXED one that already supersedes it, and independently re-confirms the wiring against live `main` rather than trusting the FIXED entry's claim outright. |
| **Action taken** | None to the product — already fixed and wired end-to-end. This entry exists so the next `findings-fold-staging.mjs` pass carries the corrected status forward and nobody re-does this plumbing believing it's still outstanding. |
| **Status** | CORRECTED — the 2026-08-30 `earnings_week_analytics` swallowed-error finding is FIXED (superseded same-day by the entry immediately below it; re-verified the field's wiring live against current `main` in this pass, across all 5 files in the chain). |
