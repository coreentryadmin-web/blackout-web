## 2026-08-30 — [FINDING, P3 audit-hygiene] Meridian ESTIMATES/HISTORY-empty defect (2026-08-18) re-verified FIXED — FINDINGS.md's OPEN status is stale — CORRECTED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this corrects** | `docs/audit/FINDINGS.md`'s 2026-08-18 entry **"Meridian ESTIMATES/HISTORY are empty on EVERY mega-cap while the same payload claims history — OPEN"** (P1, found by `scripts/audit/meridian-data-validator.mjs`) has sat marked OPEN for 12 days. Live re-verification today shows the defect no longer reproduces — this entry documents the correction per the repo's own stale-OPEN convention (see #3154, which corrected two other stale-OPEN entries the same way: a new dated entry, original left intact). |
| **Original finding** | 8/8 mega-caps (KEYS, HD, BHP, TJX, TGT, LOW, ADI, BABA) served `enrichment.print_history`, `street_estimates`, `earnings_calendar`, and `beat_rates` all empty/null, while `pack.history` (same response) carried real print data — e.g. TGT's pack claimed "4/4 EPS beats over last 4 prints" while the ESTIMATES/HISTORY tabs, fed from `enrichment`, rendered nothing. Leading hypothesis at the time: a `Promise.all` in `loadMeridianEarningsEnrichment` trips a timeout/rate-limit, each `.catch(() => ({ rows: [], entitled: true, error: "cache_error" }))` silently yields an empty result, and `serverCache` caches the empty result for 10 minutes. |
| **Live re-verification, today** | Ran the exact validator that found this (`scripts/audit/meridian-data-validator.mjs`) against production. The original 8 tickers (KEYS/HD/BHP/TJX/TGT/LOW/ADI/BABA) have all since reported and no longer appear on the live earnings timeline, so re-ran unscoped against today's 8 highest-importance upcoming events instead (PANW, MDT, AVGO, HPE, SNOW, CIEN, ADBE, ORCL — 2026-09-01 through 2026-09-11). The validator's `coherence:print_history` check — the exact check this defect originally tripped (`pack.history.length > 0 && enrichment.print_history.length === 0`) — fired **0 times** across all 8. Result: **0 FAIL, 2 WARN** (both WARN are an unrelated `max_pain_range` scope check on PANW/ADBE, not this defect). |
| **Source confirms a real fix, not just an empty sample** | `src/lib/meridian/meridian-earnings-enrich.ts`'s `loadMeridianEarningsEnrichment` no longer swallows the calendar-critical fetch into a silent `{rows: []}`: it now calls `loadBenzingaTickerEarnings` directly (not through a blanket `.catch(() => [])`) and surfaces `calendar_error: history.history_error ?? (benzingaRes as { error?: string | null }).error ?? null` on the payload, with an in-code comment explicitly citing "measured on 8/8 mega-caps on 2026-08-18" as the reason. `git log --follow` on that file shows the fix landed in #2270 ("Meridian: Benzinga earnings P0-P2 — calendar primary, tabs, architecture fixes"), with further hardening in #2338, #2476, #2608, #2614, #2618. |
| **Why the FINDINGS.md entry never got corrected** | The finding predates the folding/reconciliation tooling (`findings-fold-staging.mjs`, `findings-reconcile.mjs`) becoming the standing workflow, and unlike #3154's two corrections (found via a scheduled re-sweep), nothing had specifically gone back and re-run this validator against a currently-live earnings slate since the original 8 tickers rolled off the calendar — the OPEN status was never wrong to write, it just went stale silently once the underlying code changed weeks later. |
| **Action taken** | None to the product — already fixed. This entry exists solely so `docs/audit/FINDINGS.md`'s next fold pass records the corrected status, per the repo's "stale entries aren't edited in place — each correction is a new dated entry" convention. |
| **Status** | CORRECTED — re-verified FIXED (was stale OPEN). Live evidence: `meridian-data-validator.mjs` 0 FAIL / 2 unrelated WARN across 8 current earnings events, 2026-08-30. |
