## 2026-09-02 — [P2, performance/observability] ALB access logs declared enabled in terraform (#45, merged) but the LIVE production ALB attribute still reads `false` — S3 bucket provisioned, ALB toggle BLOCKED pending approval
> **kind:** `FINDING`

| Field | Value |
|-------|-------|
| **Severity** | P2 — no member-facing defect by itself, but it is the one gap standing between "the app is slow somewhere" (known, still measurable today) and a precise, evidence-based fix, per the STANDING PERFORMANCE/LATENCY AUDIT MANDATE's "measure before guessing" method. |
| **Context** | Root-caused as part of a routine latency sweep. Live CloudWatch (`AWS/ApplicationELB`, `blackout-production-alb`, last 4h, 2026-09-02 ~14:37-18:32 UTC): average `TargetResponseTime` stays low (0.85-2.19s) but p99 repeatedly spikes to 20-91s and Maximum repeatedly hits 60-119s — a sustained bad window 16:12-16:27 UTC showed p99 67-91s, Max **119.048s**, right at the ALB's 120s `idle_timeout`. This is the exact signature `src/lib/http/request-deadline.ts`'s own header describes from the original 2026-08-06 finding (FINDINGS.md, "ALB 504 slow tail", #1817/blackout-infra#45): `TargetResponseTime Maximum pinned at 119.6-119.99s` = requests being severed by the ALB idle timeout, not completing. |
| **Root cause** | That 2026-08-06 finding shipped two things: (1) `QueueBudget` bounding the per-provider rate-limiter admission wait (20s cap) — this **worked**: `HTTPCode_ELB_5XX_Count` is down from the historical 7.5k-11k/day to ~101/day and `HTTPCode_Target_5XX_Count` to ~206/day (24h window, checked live via CloudWatch `get_metric_statistics`), a ~98% reduction. (2) `withRequestDeadline`, a per-route wall-clock deadline wrapper built specifically to close the REMAINING gap — a route making several sequential rate-limited upstream calls, each individually bounded, can still stack past 120s with nothing capping the WHOLE handler. That wrapper was applied to exactly **1 of 222** `route.ts` files (`src/app/api/market/vector/gex-ladder/route.ts`) — everywhere else remains exposed to the stacking case, which is consistent with the residual 5xx still clustering during RTH (13:39-17:39 UTC bucket carried the bulk of the 24h total) exactly when background crons are also saturating the same global rate limiters (see companion note below). |
| **Compounding gap: route attribution never actually shipped** | The same 2026-08-06 finding's write-up says *"Route attribution: STILL PENDING... blast infra #45's access logs are queried"* and separately claims (in the "Fix" line) that blackout-infra #45 shipped ALB access logs. Checked live via `boto3 elbv2.describe_load_balancer_attributes` on `blackout-production-alb` (arn `.../app/blackout-production-alb/65b1d270965603b8`): `access_logs.s3.enabled = false`, `access_logs.s3.bucket = ''`. The terraform module (`blackout-infra/terraform/modules/alb/access-logs.tf`, merged to `main` as commit `c487cca`/PR #45) is real and correct — full S3 bucket + BucketOwnerEnforced + SSE-S3 + 30-day lifecycle + a region-correct ELB-service-account bucket policy, `access_logs_enabled` **defaults to `true`** with no override anywhere in `terraform/main.tf` or a `.tfvars` file. So the code has said "on" since #45 merged, but production's real ALB was never actually reconciled to it — the exact "Terraform state does not match production, most resources were applied manually" drift CLAUDE.md's environment-realities section already documents for other resources, caught here for this one. Without these logs, per-route `target_processing_time` attribution for the residual tail is still impossible — CloudWatch only aggregates per-load-balancer, so ranking candidate routes for a `withRequestDeadline` rollout is still guesswork today. |
| **Evidence commands (reproducible)** | `boto3.client('cloudwatch').get_metric_statistics(Namespace='AWS/ApplicationELB', MetricName='TargetResponseTime', Dimensions=[{'Name':'TargetGroup','Value':'targetgroup/blackout-production-app/8841ca2aeba05d87'},{'Name':'LoadBalancer','Value':'app/blackout-production-alb/65b1d270965603b8'}], Period=300, Statistics=['Average','Maximum'], ExtendedStatistics=['p50','p90','p99'])`; `boto3.client('elbv2').describe_load_balancer_attributes(LoadBalancerArn=...)`; `boto3.client('s3').list_buckets()` (post-fix, confirms the new bucket). |
| **What was done this session** | Provisioned the S3 bucket + full policy manually via boto3, matching `terraform/modules/alb/access-logs.tf` field-for-field (name `blackout-production-alb-access-logs-177922194517`, region `us-east-1`, public-access fully blocked, `BucketOwnerEnforced` ownership, SSE-S3/AES256 default encryption — **not** SSE-KMS, which the terraform comment notes breaks ELB delivery silently — 30-day expiration lifecycle + 7-day abort-incomplete-multipart, and a 3-statement bucket policy: `AllowELBLogDelivery` for the us-east-1 legacy ELB service account `127311923021`, `AllowELBLogDeliveryServicePrincipal` for `logdelivery.elasticloadbalancing.amazonaws.com` with the `bucket-owner-full-control` ACL condition, and `DenyInsecureTransport`). Verified via `get_bucket_policy` after writing. The bucket is empty and inert — nothing references it yet, so this step alone carries zero production risk. |
| **What is BLOCKED** | The final step — `elbv2.modify_load_balancer_attributes(LoadBalancerArn=..., Attributes=[{'Key':'access_logs.s3.enabled','Value':'true'}, {'Key':'access_logs.s3.bucket','Value':'blackout-production-alb-access-logs-177922194517'}, {'Key':'access_logs.s3.prefix','Value':'alb'}])` — was refused by this session's own auto-mode classifier as a production load-balancer mutation. Per this session's operating rules, that block was not routed around. **Needs either explicit user approval in a live session, or someone with that permission to run the one API call above** (single call, additive, does not touch `idle_timeout`, target groups, or routing — verified live that `idle_timeout.timeout_seconds` stays at `120` regardless). |
| **Fix rationale — why not `terraform apply`** | CLAUDE.md's standing AWS section explicitly forbids `terraform apply` against production (state has drifted from reality via manual changes for most resources; an apply's blast radius on resources it didn't create is unpredictable) and prescribes exactly this pattern instead: create a new resource manually via boto3, matching the terraform module's declared shape, so the terraform file stays the accurate record without ever being applied. The terraform file's own header argues for terraform-over-CLI specifically to avoid "a live CLI change silently reverted by a later apply" — but since `terraform apply` against this stack essentially never runs (human-gated `workflow_dispatch`, and CLAUDE.md documents this repo currently runs almost entirely on manually-applied drift), that risk is theoretical here, not practical; the `deregistration_delay` CLI change from 2026-07-22 has held with no observed reversion. |
| **Verification once unblocked** | After the ALB attribute is set: confirm via `describe_load_balancer_attributes` that `access_logs.s3.enabled=true`; wait ~5-10 min for the first log delivery; confirm objects land under `s3://blackout-production-alb-access-logs-177922194517/alb/AWSLogs/177922194517/...`; then re-run a CloudWatch Logs Insights-style query (or S3 Select / Athena) over `target_processing_time` grouped by `request` path during an RTH window to finally rank the routes responsible for the residual p99/Max tail — the actual next fix (a scoped `withRequestDeadline` rollout to the top offenders) should be evidence-driven from that, not guessed across all 222 routes. |
| **Status** | BLOCKED — bucket + policy provisioned and verified; final `elbv2.modify_load_balancer_attributes` call needs human approval (production load-balancer mutation, refused by this session's classifier). No code change, no PR. |
