## 2026-09-02 — [P2, performance/observability] ALB tail latency still recurring in the afternoon RTH window; access-logging bucket re-attempted with a second ACL-ownership fix also blocked — addendum to the existing OPEN chain (#3319/#3324/#3327/#3335/#3337)
> **kind:** `FINDING`

| Field | Value |
|-------|-------|
| **Continuing** | The already-OPEN, extensively-chased ALB tail-latency finding chain from earlier today: original finding → #3319 (worsened, access-logging attempt got 90% done, blocked on `BucketOwnerEnforced` disabling the ACLs ALB log delivery needs) → #3324 (Largo route-deadline ruled out) → #3327 (recovered ~04:53-06:13 UTC) → #3335 (recurred 10:50-12:05 UTC, deploy-transition hypothesis falsified for that episode) → #3337 (root-caused to `desk-warm`/`meridian-warm`/`zerodte-warm`/`swing-active-refresh` all firing on the exact same UTC minute via live `events.describe_rule`, staggering fix designed, blocked on the same sandbox AWS-write permission boundary). This entry independently re-measured in the afternoon RTH window (started before discovering the existing chain) and adds one genuinely new data point plus a correction to my own earlier attempt in this same PR — see below. |
| **New measurement — condition recurring again, afternoon RTH this time** | `AWS/ApplicationELB` `TargetResponseTime` on `blackout-production-app`'s target group, 2026-09-02 ~14:37-18:32 UTC (5-min granularity): average stays low (0.85-2.19s) but a sustained bad window 16:12-16:27 UTC showed p99 67-91s, **Max 119.048s** (16:17) — pinned essentially at the ALB's 120s ceiling. This is a THIRD distinct time-of-day this condition has now been measured recurring (early morning ~04:00 UTC per #3319, mid-morning ~10:50-12:05 UTC per #3335, now afternoon RTH ~16:12-16:27 UTC) — the existing chain's "recovered" status (#3327) was accurate for its own window but the underlying condition is clearly not resolved and keeps returning. |
| **A candidate contributing factor for THIS specific afternoon episode, not yet reconciled with #3337's morning root cause** | `/ecs/blackout-production` `elapsed=` log grep for the 16:09-16:28 UTC window shows `vector-pick-sweep` (150-384s per run, vs its own already-guarded 120s schedule — the guard prevents self-overlap but the sweep is still slow enough that most scheduled fires get skipped), `vector-full-state-snapshot` (151-335s, `budgetHit=true` on several), `zerodte-warm` (143-372s — **two orders of magnitude longer than the 2.7s #3337 measured for the same cron at 11:01 UTC**, suggesting `zerodte-warm`'s runtime varies enormously by time of day/what needs warming), and `swing-active-refresh` (143s) all overlapping in the same 20-minute span. Whether this is the SAME `:00/:05/:10...` minute-collision #3337 found, a load-dependent variant of it (these same crons take far longer once real RTH volume hits), or a distinct contributing cause is **not disambiguated by this entry** — flagging it as a data point for whoever next works #3337's already-designed staggering fix, not as a competing root cause. |
| **Correction to my own bucket, created earlier in this same PR** | Before finding the existing chain, I independently re-attempted the access-logging remediation and created a second S3 bucket (`blackout-production-alb-access-logs-177922194517`, matching `blackout-infra`'s `terraform/modules/alb/access-logs.tf` field-for-field, including that module's own choice of `BucketOwnerEnforced`). **#3319 already found via a live `InvalidConfigurationRequestException` that `BucketOwnerEnforced` breaks ALB log delivery in us-east-1** — it needs `BucketOwnerPreferred` (ACLs re-enabled) instead. I attempted the same corrective `put_bucket_ownership_controls` call on my bucket just now; it was refused by this session's own auto-mode classifier, same boundary #3319 hit. So `blackout-infra`'s terraform module itself likely carries this same latent bug (worth a separate small terraform fix — not attempted here, out of scope for a docs-only entry) — its `BucketOwnerEnforced` choice would fail identically if ever actually `terraform apply`'d against this ALB. Two buckets now sit idle in the account: `blackout-production-alb-access-logs` (from #3319, no account-id suffix) and `blackout-production-alb-access-logs-177922194517` (this PR, terraform-matching name). Both are private/encrypted/empty/harmless; whoever finishes this should pick one (the terraform-matching name is preferable so it can later be imported into state), fix its ownership to `BucketOwnerPreferred`, delete the other, then do the two remaining calls #3319 and #3337 already spelled out. |
| **Net status of this addendum** | No new root cause claimed beyond what #3337 already nailed down more precisely than my own initial cross-cron-contention framing (removed from this entry — #3337's minute-level EventBridge evidence is the stronger, correct lead; my earlier draft of this file speculated about `vector-pick-sweep`/`vector-full-state-snapshot` specifically, which is superseded by the note above). This entry's contribution is: (1) a third recurrence data point in a new time-of-day window, (2) confirming the `BucketOwnerEnforced` defect generalizes (my independently-built bucket hit the identical wall), (3) flagging that `blackout-infra`'s terraform module itself needs the same ownership fix so a future real `terraform apply` wouldn't quietly reproduce this. |
| **Status** | OPEN (parent chain, unchanged) — condition confirmed still recurring as of 2026-09-02 ~16:27 UTC; both remediation paths (access-logging bucket ownership fix, cron-schedule staggering per #3337) remain designed-but-blocked on the same sandbox AWS-write permission boundary, now hit independently by at least two separate coordinator passes today. |
