## 2026-09-04 — [FINDING, P1 infra-config] PgBouncer zero-headroom follow-up (#3499) still unset — live market-open recurrence measured, ~500 Target 5xx and counting — addendum to the OPEN chain, no code change

> **kind:** `FINDING`

| Field | Value |
|---|---|
| **Continuing** | The already-OPEN `[PERF, P1 infra-config] PgBouncer connection-budget follow-up config from PR #3499 was never set — zero-headroom condition (and correlated ALB 5xx) still live` finding (this file, top-of-file at time of writing) — root cause fully diagnosed there (`REPLICA_COUNT=8 × PG_POOL_MAX=2` (web) `+ REPLICA_COUNT=1 × PG_POOL_MAX=4` (market-worker) `= 20 = PGBOUNCER_DEFAULT_POOL_SIZE` exactly, zero headroom before counting web's real `MaxCapacity=12` ASG ceiling or a rolling-deploy overlap), remediation fully designed (set `REPLICA_COUNT_MAX`/`PGBOUNCER_RESERVED_FOR_OTHER_SERVICES` per-service), blocked on a production Secrets Manager write this session's auto-mode classifier correctly refuses without explicit operator authorization. This entry does not re-derive the root cause — it re-confirms the config is still unset today and adds a fresh, currently-active recurrence with new correlating symptoms not previously logged on this chain. |
| **Confirmed still unset (live read, this session, 2026-09-04)** | `secretsmanager.get_secret_value("blackout-production/app/env")`: `REPLICA_COUNT=8`, `PG_POOL_MAX=2`, **`REPLICA_COUNT_MAX=None`**, **`PGBOUNCER_RESERVED_FOR_OTHER_SERVICES=None`**, `PGBOUNCER_DEFAULT_POOL_SIZE=20` — identical to the values the parent finding recorded; nothing has been applied since. |
| **Fresh live recurrence, actively ongoing at measurement time** | `AWS/ApplicationELB HTTPCode_Target_5XX_Count` on `blackout-production-app`, 24h window ending ~14:55 UTC today: baseline 2026-09-03 was 1-9 events per 5-min bucket; starting **13:26-13:31 UTC today (almost exactly RTH cash open, 09:31 ET)** it stepped up to a sustained **14-50 events per 5-min bucket** and was STILL climbing at the last sample taken (14:51 UTC → 32, 14:46 UTC → 50). Total for the window: 584 Target 5xx vs 108 ELB-side 5xx (i.e. the majority are real application-returned 5xx, not ALB-timeout gateway errors). `TargetResponseTime` Max hit 119.75s at 13:30 UTC and 119.17s at 14:20 UTC today — pinned at the ALB's 120s idle-timeout ceiling, the same signature the parent chain has repeatedly measured. RDS (`blackout-production-postgres`, db.t4g.medium) itself is NOT resource-starved during this window — `CPUUtilization` 7.5% (extreme 27%), `CPUCreditBalance` 576 (near max), `DatabaseConnections` max 20-23, read/write latency ~0 — ruling out RDS-instance capacity as the bottleneck and pointing at the connection-budget ceiling itself, exactly as the parent finding's diagnosis predicts. |
| **New correlating symptoms not previously on this chain** | `/ecs/blackout-production` logs in the spike window show `error: canceling statement due to statement timeout` from three independent sources in one 10-minute sample (`cron-run/socket-health`, `cron-run/vector-dark-pool-warm`, `clerk-webhook` on a `user.deleted` DB write) — i.e. the timeout is hitting unrelated call sites across different crons and a webhook handler, consistent with a shared-pool contention symptom rather than one slow query. Also recurring in the same window: `(node:1) MaxListenersExceededWarning: Possible EventEmitter memory leak detected. 11 error listeners added to [Client]` — consistent with repeated pool client churn/reconnect under connection pressure (the same shape as the parent finding's own `Cannot use a pool after calling end on the pool` evidence). ECS `blackout-production-web` CPU/Memory are NOT saturated in this window (CPU avg 28-38%, occasional per-task spikes to ~90% but not sustained; Memory avg 21-30%) — ruling out compute capacity as an alternative explanation for this specific episode. |
| **Why not fixed here** | Same reason as the parent finding: closing this requires writing `REPLICA_COUNT_MAX`/`PGBOUNCER_RESERVED_FOR_OTHER_SERVICES` into the shared `blackout-production/app/env` Secrets Manager secret plus a new task-definition `secrets` entry and an ECS service update for `blackout-production-web` (and the market-worker-side literal task-def env equivalents) — a production infrastructure write outside what this session's auto-mode is authorized to perform unilaterally. No AWS write was attempted. |
| **What this adds** | The parent finding already fully designed the fix; what was missing was proof the gap is not theoretical — this entry supplies a live, currently-active, market-open-triggered episode (still climbing at measurement time) plus two new symptom classes (cross-cron `statement timeout` errors, `MaxListenersExceededWarning` pool churn) that corroborate the connection-budget diagnosis from a different angle than the original `[db] transient query error` evidence. Recommend whoever has Secrets Manager write authorization treat this as confirmation to prioritize applying the already-designed fix (web: `REPLICA_COUNT_MAX=12`, `PGBOUNCER_RESERVED_FOR_OTHER_SERVICES=4`; market-worker: `REPLICA_COUNT_MAX=1`, `PGBOUNCER_RESERVED_FOR_OTHER_SERVICES=24`, per the parent finding's exact remediation table). |
| **Status** | OPEN (parent chain, unchanged) — condition reconfirmed actively recurring at market open today, 2026-09-04 ~13:26-14:55 UTC and counting; remediation remains designed-but-blocked on the same Secrets Manager write-authorization boundary. No infra or code change made this cycle. |
