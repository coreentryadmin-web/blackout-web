## 2026-08-31 — [FINDING, P4 audit-hygiene] The `currentZerodteSessionAnchor` `nowMs`-ignored finding (2026-08-30) is marked OPEN but was fixed the same day two entries later — CORRECTED

> **kind:** `FINDING`

| Field | Detail |
|---|---|
| **What this corrects** | `docs/audit/FINDINGS.md`'s 2026-08-30 entry **"[FINDING, P3 Night Hawk/0DTE] `currentZerodteSessionAnchor`'s `nowMs` is silently ignored for `session_state`/`session_note` — OPEN, write-up only"** (heading + its own `**Status**` row both say OPEN, "flagged for the Night Hawk/0DTE lane, no code change made"). Two entries later in the same file, same date, sits **"[FINDING, P3 0DTE/Largo] `currentZerodteSessionAnchor` ignored `nowMs` for `session_state` — FIXED"** — describing exactly the fix the OPEN entry's own "Suggested fix shape" proposed (an optional `now: Date` on `nighthawk/lib/session.ts`'s `todayEt()`/`etNowParts()`, `currentZerodteSessionAnchor` passing `new Date(nowMs)` through). The OPEN entry was never updated to reflect that the fix landed. |
| **Confirmed live in the current codebase, not just taken on the later entry's word** | `src/features/nighthawk/lib/session.ts:50` — `export function todayEt(now: Date = new Date()): string`; `:114` — `export function etNowParts(now: Date = new Date())`. `src/lib/zerodte/session-phase.ts`'s `currentZerodteSessionAnchor` constructs `const now = new Date(nowMs)` and passes it to both. `session-phase.test.ts`'s `"injected nowMs drives session_state — not the real wall clock"` test (injected Friday 05:00 ET must return `PRE_MARKET`, not real-wall-clock `CLOSED`) passes: `node --import tsx --test --experimental-test-module-mocks src/lib/zerodte/session-phase.test.ts` → 4/4 pass, Node 20.20.2. |
| **Why this is a hygiene correction, not a new investigation** | Same shape as the immediately-preceding self-correction in this file ("SPX Slayer Largo coverage finding... heading/table Status said OPEN while its own blockquote already said FIXED"): the fix and its evidence already exist in `FINDINGS.md` itself, just under a different heading a few entries later — this doesn't add new evidence, it points the OPEN entry's status at the FIXED entry that already supersedes it, and independently re-confirms the fix against the live source rather than trusting the FIXED entry's own claim. |
| **Action taken** | None to the product — already fixed, tested, and green. This entry exists so the next `findings-fold-staging.mjs` pass carries the corrected status forward and nobody re-does this fix believing it's still outstanding. |
| **Status** | CORRECTED — the 2026-08-30 `currentZerodteSessionAnchor` `nowMs` finding is FIXED (superseded same-day by the entry two headings below it; re-verified live against current `main` in this pass). |
