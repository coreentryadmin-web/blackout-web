# SPX Slayer Certification — Coordinator Decisions

**Date:** 2026-08-24  
**Authority:** Autonomous SPX product ownership (verified 2026-08-24, directive: "make the decisions for me")

Three standing decisions made to unblock SPX certification and keep the product shipping.

## Decision 1: Production_eligible Gate — Keep 40% Conviction Threshold

**Question:** Should plays that fail the `production_eligible` gate (conviction < 40% OR far-OTM/macro-unavailable/earnings-all-ranks/cortex-veto) be permitted anyway?

**Options Considered:**
- A: Keep the 40% threshold (conservative, existing architecture)
- B: Remove conviction check, rely on other gates
- C: Relabel to "confidence estimate" with disclaimer

**Decision:** **Option A — Keep the 40% threshold**

**Rationale:**
- Confidence field is now omitted from Largo (PR #2827), so members don't see it as a percentage
- Members see conviction as "signal strength" on the desk, a 0–100 scale
- The gate was designed before the field was ever measured as anything other than "constant 96"
- Keeping it has zero downside: members still see plays, just not in the production list, until conviction climbs
- This is a conservative fail-closed gate that has already shipped in limited-live

**No code change required.** The gate remains as-is in `src/features/spx/lib/spx-play-engine.ts`.

---

## Decision 2: Vector #2693 Category Classification — Backtest Before A/B

**Question:** Should the proposed change to Vector grading logic (2x win vs two +50% wins) be shipped as a live A/B test, backtested offline, or shipped directly?

**Options Considered:**
- A: Live A/B test (50% of records get new grader, 3–4 weeks measurement)
- B: Backtest offline first (90 days historical records, then decide)
- C: Ship without testing (directionally sound, acceptable risk)

**Decision:** **Option B — Backtest first, then A/B only if signal survives**

**Rationale:**
- Low overhead: ~30 min backtest on real Vector plays with real outcomes
- Good confidence before live traffic: measure win-rate delta in isolation
- Allows quick pivot if data says no
- Preserves A/B option if backtest is promising

**Owner:** Vector lane (SPX coordination only)

---

## Decision 3: 12 Unaudited Crons Verification — Re-Audit + Recurring Checks

**Question:** Should the 12 crons marked "correct" by eyeball audit (2026-08-21) be declared verified, or re-checked?

**Options Considered:**
- A: Declare verified based on 2026-08-21 audit (risk: manual fix after that date goes unnoticed)
- B: Re-run audit on current deployed state (2 min, confirms no drift, adds one line to RUN-LOG.md)
- C: Set up recurring monthly/quarterly audit trigger (catches DST breakage automatically)

**Decision:** **Option B + C — Re-run audit now, set recurring trigger**

**Rationale:**
- Re-run is nearly free: ~2 min, closes any doubt about the 12 marked OK
- Recurring trigger (monthly/quarterly) catches DST regressions that a manual fix might introduce
- Known status: x-autopost still broken (0 EST fires) from 2026-08-21; banger-discovery has registry mismatch
- Audit completed 2026-08-24 and logged to RUN-LOG.md

**Action:** Create a Routine to re-run `cron-dst-audit.mjs` monthly. Store results in RUN-LOG.md.

---

## Status

All three decisions are **RESOLVED**. No blockers remain on SPX certification.

- **Decision 1:** No code change needed; gate stays as-is.
- **Decision 2:** Delegated to Vector lane; SPX acknowledges and will coordinate A/B rollout.
- **Decision 3:** Audit re-run completed 2026-08-24; recurring trigger setup pending (CronCreate).

---
_Generated by [Claude Code](https://claude.ai/code)_
