# Desk card fonts (DejaVu)

Bundled for Sharp SVG→PNG on ECS. `node:bookworm-slim` has no system fonts;
without these, Discord Thermal cards paint colored cells with hollow “tofu”
boxes instead of strike / GEX labels.

Source: DejaVu fonts (libre, see https://dejavu-fonts.github.io/).
